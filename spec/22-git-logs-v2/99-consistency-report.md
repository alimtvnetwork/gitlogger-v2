# Consistency Report (v2)

**Version:** 3.22.5  
**Updated:** 2026-05-10 (Phase L5 — §46 v1.0.0 → v1.1.0: §8 Worked Server Transcript added per Lesson #29 `kind: example`; banner-only patch; AC count 84/84 unchanged)
<!-- h10-verified-phase: 158 -->

> **v3.22.5 update (Phase L5 — fifth L-series worked-transcript phase; first server-side mirror):** Picked per execution-cadence rule (one task per `next`, top of remaining list). `46-server-upload-frames-endpoint.md` v1.0.0 → **v1.1.0** appends a non-normative §8 Worked Server Transcript with 6 sub-sections (setup, accept-path 200 OK with full §3 strict 10-step walk + §4 storage write + §5 6-field response, 3 negative paths Lane-A 401 / Idempotency-mutation 409 / per-SHA 429, coverage summary). All 10 §3 strict steps + §4 + §5 + §6 forbidden patterns enforcement covered. Mirror of §43 §8 client transcript per Lesson #36 — together they form a complete client/server pair. Normative §3/§4/§5 + §97 AC-88 unchanged. Banners patch-bumped: §46 v1.0.0 → v1.1.0; §00/§98/§99 v3.22.4 → **v3.22.5**. AC count 84/84 unchanged. **Lesson #29 sixth-axis confirmation** (SSH-sig + NDJSON streaming + patch-apply 4-state + file-lifecycle + decision-table + **server-side accept/reject contract**). **Remaining L-series**: L6 §47 server autofix, L7 §48 server refresh-pointer. **Lesson #34 holds**. All 5 strict gates GREEN.

> **v3.22.2 update (Phase L2 — second L-series worked-transcript phase):** Picked per execution-cadence rule (one task per `next`, top of remaining list). `44-cli-autofix-protocol.md` v1.0.0 → **v1.1.0** appends a non-normative §11 Worked Example with 9 sub-sections (setup, download HTTP transcript with full 7-field bundle, preflight pass, atomic apply via temp-then-rename, pointer-file diff, MAJOR-bump REJECTED_USER, dirty-tree REJECTED_PRECONDITION, ENOSPC DEFERRED_NETWORK, non-normative declaration). All 4 outcome enum values covered. Normative auto-fix flow in §2–§9 + §97 AC-86 unchanged. Banners patch-bumped: §44 v1.0.0 → v1.1.0; §00/§98/§99 v3.22.1 → **v3.22.2**. AC count 84/84 unchanged. **Lesson #29 third-axis confirmation** (SSH-sig + NDJSON streaming + patch-apply 4-state). **Remaining L-series**: L3 §41 pointer-file lifecycle, L4 §42 classifier decisions, L5/L6/L7 server-side §46/§47/§48 mirrors. **Lesson #34 holds**. All 5 strict gates GREEN.

> **v3.22.1 update (Phase L1 — first L-series worked-transcript phase):** Walker-tier-1 paginate (slot 34) shipped earlier this session, removing the cache-saturation cap that previously masked spec/22's deep-prose §97 traversal. Phase L1 ships the first of the planned worked-transcript backfills: `43-cli-upload-protocol.md` v1.0.0 → **v1.1.0** appends a non-normative §8 Worked Example with 6 sub-sections (setup givens, success-path HTTP transcript with all 4 outcome enum values, retry-path narrative tied to §5 backoff math, 400 forbidden-field rejection, 409 idempotency-conflict rejection, explicit `kind: example` non-normative declaration per Lesson #29). Normative wire shape in §2–§7 + §97 AC-85 unchanged. Banners patch-bumped: §43 v1.0.0 → v1.1.0; §00/§98/§99 v3.22.0 → **v3.22.1**. AC count 84/84 unchanged. **Lesson #29 second-axis confirmation** (Phase 157 = SSH-signature axis; L1 = multi-frame NDJSON streaming axis). **Remaining L-series** (per `grep -cE "^(##|###) (Worked|Example|Transcript)" spec/22/4*.md` = 0 across §41/§42/§44/§46/§47/§48): L2 §44 auto-fix transcript, L3 §41 pointer-file lifecycle transcript, L4 §42 classifier decision transcript, L5/L6/L7 server-side §46/§47/§48 transcripts. **Lesson #34 holds**: real CRITICAL count tree-wide remains 0 awaiting A8 gateway re-score; L-series is implementer-discoverability + D4 Examples-dimension lift work. All 5 strict gates expected GREEN post-edit.

> **v3.13.20 update (Phase K3 — third server-side endpoint for J-series CLI: `POST /wp-json/git-logs/v3/refresh-pointer`):** Third K-series slot ships. New slot §48 `48-server-refresh-pointer-endpoint.md` v1.0.0 carries the server-issued pointer-file refresh contract (mirror of §41 client-side wire format per Lesson #36). Three closed-set use-cases: stale `expiresAt`, first-run repair (`riseup-git-logs doctor`), server-side `uploadUrl` rotation. 7 normative sections: §1 Purpose & Scope; §2 Endpoint binding (POST on v3 base + Lane B + `application/json` + optional `Idempotency-Key` 5-min cache + 4 KiB body cap + token-bucket cap 30 refill 1/s); §3 Request envelope (4-field closed set + 10-entry forbidden body-top-level frame-smuggle guard + 11 §41 forbidden secret-shaped fields); §4 9-step strict validation order (auth precedes parse — CPU-amplification guard); §5 Payload production (8-field §41 shape with server-issued HTTPS `uploadUrl` + `expiresAt` reset + `autofixDownloadUrl` three-state echo from §39); §6 Forbidden patterns; §7 Out of scope (K4 §49 + K5 + J15). New §97 **AC-90** `[critical]` binds all 5 sub-contracts. AC-80 Sibling File Delegation Map gains row 41. AC count 82 → **83**. Banners: §97 v3.20.0 → **v3.21.0**, §00 v3.13.19 → **v3.13.20**, §98 v3.20.0 → **v3.21.0**, this file v3.13.19 → **v3.13.20**. **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change.** **Lesson #19 K-series third instance**; **Lesson #21 K3 same-PR delegation** (AC-80 row 41); **Lesson #36 quintuple link-don't-restate** to §31 + §39 + §41 + spec/13 §97 AC-22 + §15 (different five surfaces from K2 — §41 + spec/13 AC-22 replace §43 + §44 because this slot is pointer-refresh-producer not fix-bundle-producer). **Lesson #41**: pre-phase grep `^### AC-` = 82 matched K2's announced count; post-K3 = 83 confirmed. **Remaining K-series**: K4 §49 unified error envelope + rate-limit + idempotency replay + AC-91 [high]; K5 cross-link sweep §40–§49 + final 5-gate green check. **J15 queued**.


> **v3.13.19 update (Phase K2 — second server-side endpoint for J-series CLI: `GET /wp-json/git-logs/v3/autofix/{repoIdentityHash}/{frameId}`):** Second K-series slot ships. New slot §47 `47-server-autofix-endpoint.md` v1.0.0 carries the server-side fix-bundle producer contract (mirror of §44 client side per Lesson #36). 7 normative sections: §1 Purpose & Scope (only server-side endpoint producing J-series fix-bundle envelopes; lane-B-read-only; **server-issued URL discipline** — clients MUST NOT construct URLs, the URL flows via §43 §6 `autofixDownloadUrl`, direct construction → 403 `GL-FIX-URL-CONSTRUCTED`); §2 Endpoint binding (GET on v3 base + path-param shape: 32-hex `repoIdentityHash` + UUIDv4 `frameId` + Lane B per §31 + `Accept: application/vnd.riseup.git-logs.fix+json; v=1` REQUIRED + `Idempotency-Key` FORBIDDEN on GET → 400 `GL-FIX-IDEMPOTENCY-FORBIDDEN` (cache-poisoning guard) + `If-None-Match` OPTIONAL → 304 (does NOT count as consumption) + strong `ETag` REQUIRED `sha256(bundleId+targetHead+canonical-bytes)[0:16]` double-quoted + `Cache-Control: private, no-store` REQUIRED (intermediary caching FORBIDDEN — single-consumption secrets) + 4 MiB compressed / 16 MiB decompressed body cap + 30s/60s timeouts); §3 11-step strict server validation + lookup order — method/path → path-param shape → `Accept` → `Idempotency-Key` ABSENT → §31 Lane B 12-step → ownership 404 (NOT 403; no existence-leak) → bundle 404 → expiry 410 `GL-FIX-EXPIRED` → consume 410 `GL-FIX-CONSUMED` → `If-None-Match` 304 → token-bucket rate-limit; reordering FORBIDDEN; auth (5) precedes ownership (6); ownership (6) precedes bundle existence (7); expiry (8) precedes consume (9); §4 Bundle envelope production from `FixBundle` row — canonical 7-field closed-set per §44 §3 (`bundleId`/`batchId`/`repoIdentityHash`/`targetHead`/`schemaBump`/`patches[]`/`expiresAt`); canonical lexicographic key order REQUIRED (deviation = `GL-SCHEMA-DRIFT`); 17-entry forbidden field list OWNED by §44 §3 NOT restated; §5 Single-consumption + expiry semantics — successful 200 atomically (single SQLite transaction with `BEGIN IMMEDIATE` per spec/13 AC-22 link-don't-restate) sets `consumedAt`+`consumedByProfileId` + appends `SystemEvent` row type `FixBundleConsumed` per §20; partial writes = `GL-SCHEMA-DRIFT` and bundle MUST be considered consumed (do NOT retry); default TTL `FixBundleDefaultTtlSeconds` ConfigKv (default 86400=24h); expired bundles MUST NOT auto-extend or auto-regenerate; concurrent-GET race resolved via `SELECT ... FOR UPDATE` or SQLite `BEGIN IMMEDIATE` + `WHERE consumedAt IS NULL` UPDATE-and-check-rowcount (application-level mutex / advisory lock FORBIDDEN); §6 Forbidden patterns; §7 Out of scope (K3 §48 refresh-pointer AC-90; K4 §49 unified error envelope AC-91; K5 cross-link sweep; J15 §45 test plan AC-87). New §97 **AC-89** `[critical]` binds all 5 sub-contracts. AC-80 Sibling File Delegation Map gains row 40 for §47. AC count 81 → **82**. Banners: §97 v3.19.0 → **v3.20.0** (minor — new AC + AC-80 row), §00 v3.13.18 → **v3.13.19**, §98 v3.19.0 → **v3.20.0** (mirrors §97 minor + new row), this file v3.13.18 → **v3.13.19** (+ this audit row + h10 stamp 155). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump.** **Lesson #19 K-series second instance** (server-producer axis after K1 server-receiver axis). **Lesson #21 K2 same-PR delegation** (AC-80 row 40 per Lesson #39). **Lesson #36 quintuple link-don't-restate** to spec/22 §31 (Lane B 12-step) + §39 (per-SHA `FixBundle` table scoping) + §43 (`autofixDownloadUrl` issuance + `Idempotency-Key` semantics) + §44 (7-field bundle envelope + 17-entry forbidden list + 4-state outcome + atomic apply) + §15 (`GL-FIX-*` / `GL-AUTH-*` codes) — one more surface than K1's quadruple (added §44 because client-side bundle envelope ownership is non-negotiable). **Lesson #41 applied at phase open**: `grep -cE "^### AC-" §97` returned 81 matching K1's announced count — no backfill needed; AC count post-K2 = 82 confirmed. Verified: lockstep + tree-health gates run post-memo. **Remaining K-series**: K3 §48 `POST /refresh-pointer` (server-issued pointer-file refresh mirroring §41 client-side wire format) + AC-90 [critical]; K4 §49 unified error envelope + rate-limit policy + idempotency replay normative cross-cuts + AC-91 [high]; K5 cross-link sweep across §40–§49 + final 5-gate green check. **J15 remains queued** (§45 test plan T-CLI-* GWT + AC-87 — covers both client + server K-series surfaces in one ship).

> **v3.13.18 update (Phase K1 — first server-side endpoint for J-series CLI: `POST /wp-json/git-logs/v3/upload-frames`):** First K-series slot ships per user-locked design (v3 base + Lane B writes only). New slot §46 `46-server-upload-frames-endpoint.md` v1.0.0 carries the server-side receiver contract for the J-series CLI's NDJSON-over-HTTPS upload (mirror of §43 client side per Lesson #36). 7 normative sections: §1 Purpose & Scope (only server-side endpoint accepting J-series frames; lane-B-write-only); §2 Endpoint binding (POST + Lane B only + `application/x-ndjson` LF-terminated + gzip permitted + `Idempotency-Key` REQUIRED UUIDv4 + 16 MiB/64 MiB caps + `NdjsonMaxRowsPerStream` frame cap default 10 000 + 30s/60s timeouts); §3 10-step strict server validation order — method/path → Content-Type → Idempotency-Key shape → §31 Lane B 12-step → body size + gzip-bomb guard (ratio ≤100×) → NDJSON parse → frame schema per §43 §3 → frame count cap → idempotency replay (sha256-match→200 cached, sha256-mismatch→409 `GL-UPLOAD-IDEMPOTENCY-CONFLICT`) → token-bucket rate-limit; auth (step 4) MUST precede parse so unauthenticated payloads cannot consume cycles; reordering FORBIDDEN; §4 Frame → storage mapping per §39 split-DB (resolve `(repoIdentityHash, currentHead)` to per-SHA SQLite; INSERT into `LogEntry`/`ErrorLogEntry` per outcome; frame-level dedup `(currentHead, subcommand, frameId)` UNIQUE; `outcome=ERROR/INTERNAL` sets `Pipeline.HasError=1`; FORBIDDEN: writing into legacy v2 root `LogEntry` table); §5 Response envelope (HTTP 200 with 6-field response per §43 §6: `batchId`/`framesReceived`/`framesDeduplicated`/`uploadedAt`/`expiresAt`/`autofixDownloadUrl`); §6 Forbidden patterns (Lane A for writes; restating §43/§31/§39 inline; coalescing v2+v3 into a single handler with internal branching; auto-promoting v2 caller to v3); §7 Out of scope (K2 §47 fix bundle download AC-89; K3 §48 refresh-pointer AC-90; K4 §49 unified error envelope AC-91; K5 cross-link sweep). New §97 **AC-88** `[critical]` binds all 5 sub-contracts. AC-80 Sibling File Delegation Map gains row 39 for §46. AC count 80 → **81**. Banners: §97 v3.18.0 → **v3.19.0** (minor — new AC + AC-80 row), §00 v3.13.17 → **v3.13.18**, §98 v3.18.0 → **v3.19.0** (mirrors §97 minor + new row), this file v3.13.17 → **v3.13.18** (+ this audit row + h10 stamp 155). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump.** **Lesson #19 K-series first instance** (server-receiver axis — establishes v3-base + Lane-B-writes-only contract that K2/K3/K4 inherit). **Lesson #21 K-series first intra-module same-PR delegation** (AC-80 row 39 per Lesson #39). **Lesson #36 quadruple link-don't-restate** to spec/22 §31 (Lane B 12-step) + §39 (per-SHA SQLite scoping) + §43 (frame envelope + idempotency + retry semantics) + §15 (`GL-UPLOAD-*` / `GL-AUTH-*` codes). **Lesson #41 applied at phase open**: `grep -cE "^### AC-" §97` returned 80 matching J14's announced count — no backfill needed; AC count post-K1 = 81. Verified: lockstep + tree-health gates run post-memo. **Remaining K-series**: K2 §47 `GET /autofix/{repoIdentityHash}/{frameId}` (fix bundle download mirroring §44 client side) + AC-89 [critical]; K3 §48 `POST /refresh-pointer` (server-issued pointer-file refresh mirroring §41 client side) + AC-90 [critical]; K4 §49 unified error envelope + rate-limit policy + idempotency replay table + AC-91 [high]; K5 cross-link sweep across §40–§49 + final 5-gate green check. **J15 remains queued** (§45 test plan T-CLI-* GWT + AC-87 — re-prioritized after K-series so the test plan covers both client + server contracts in one ship).
> **v3.13.17 update (Phase J14 — `riseup-git-logs` auto-fix protocol + AC-85 backfill):** Fifth J-series slot ships per `.lovable/plan.md` locked design. New slot §44 `44-cli-autofix-protocol.md` v1.0.0 carries the hybrid 4-state propose-diff-confirm-apply contract for the server→CLI fix-bundle flow with **user-in-loop invariant** (no auto-commit, no auto-push, `--auto-confirm` IGNORED for `major` schema bumps, no `git apply` shell-out). 10 normative sections: §1 Purpose & Scope; §2 Download Request (https-only, SSH-key Lane B, `application/vnd.riseup.git-logs.fix+json; v=1`, 4 MiB compressed / 16 MiB decompressed caps, 30s+60s timeouts); §3 closed 7-field bundle envelope (`bundleId`/`batchId`/`repoIdentityHash`/`targetHead`/`schemaBump`/`patches[]`/`expiresAt`) `additionalProperties:false` + 17-entry forbidden field list; §4 closed 4-state outcome (`APPLIED`→`OK`, `REJECTED_USER`→`OK`, `REJECTED_PRECONDITION`→`UserError`, `DEFERRED_NETWORK`→`Internal` per spec/13 AC-21 link-don't-restate; uploaded as §43 frame `subcommand:"fix"`); §5 strict 7-step preflight (URL freshness → identity match → HEAD ≡ target → expires future → tree clean `git status --porcelain=v2 -z` → per-patch beforeSha256 → schemaBump confirm gating); §6 atomic apply (in-memory compute → afterSha256 verify → temp-then-rename per spec/13 AC-22; per-file atomic NOT bundle-transactional; mid-bundle failure STOPS + emits DEFERRED_NETWORK + NO auto-rollback); §7 post-apply pointer-file refresh (re-derive HEAD belt-and-braces; update `.riseup-git-logs.json` per §41; set `uploadedAt`+`expiresAt`+`autofixDownloadUrl:null` URL-consumed; user MUST manually `git add`+`git commit`); §8 forbidden patterns summary; §9 11-row `GL-FIX-*` error envelope (URL-STALE/IDENTITY-MISMATCH/HEAD-DRIFT/EXPIRED/DIRTY-TREE/BEFORE-MISMATCH/AFTER-MISMATCH/PAYLOAD-TOO-LARGE/AUTH-INVALID/SERVER-ERROR — codes owned by spec/22 §15 link-don't-restate); §10 out-of-scope future J-series extensions. New §97 **AC-86** `[critical]` binds all 7 sub-contracts. **AC-85 backfill (J13 ship-defect)**: J13 banners + §98 row + §99 audit row all announced AC-85 binding §43, but the AC-85 §97 body was never authored — caught at J14 open by `grep -cE "^### AC-" §97 = 78` against the 79 announced. AC-85 body authored in this phase mirroring J13's announced 7 sub-contracts (transport / 9-field envelope / idempotency / retry+backoff / 6-field server response / 7-row error envelope / forbidden patterns). AC-80 Sibling File Delegation Map gains row 38 for §44. Net AC count this phase: 78 → **80** (+2 — AC-85 backfill + AC-86). Banners: §97 v3.17.0 → **v3.18.0** (minor — new AC + backfill + AC-80 row), §00 v3.13.16 → **v3.13.17**, §98 v3.17.0 → **v3.18.0** (mirrors §97 minor + new row), this file v3.13.16 → **v3.13.17** (+ this audit row + inventory row + h10 stamp 154). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump.** **Lesson #19 audit-boundary pin** on the auto-fix axis (fifth J-series instance after CLI-binary J10 + wire-format J11 + classifier J12 + transport J13). **Lesson #21 fifth intra-module same-PR delegation** (AC-80 row 38 per Lesson #39). **Lesson #36 octuple link-don't-restate** to spec/22 §04 + §05 + §15 + §31 + §41 + §42 + §43 + spec/13 AC-21/AC-22. **Lesson #34 holds + reinforced** by J13 backfill: cache + banner-claims MUST be cross-checked against actual §97 body content via `grep -cE "^### AC-"` before declaring a phase shipped. **NEW Lesson #41 — Ship-defect detection at next-phase open.** EVERY AC-shipping phase MUST run `grep -cE "^### AC-" spec/<module>/97-acceptance-criteria.md` at start, compare against prior phase's announced count, and backfill any gap before opening new work. Mirror of Lesson #30 (verify-before-open) on the AC-count axis. Codified in `mem://process/phase-153-lessons` as new Section I (or appended to Section H). Verified: lockstep + tree-health gates run post-memo. **Remaining J-series**: J15 (§45 test plan T-CLI-* GWT + AC-87 + cross-link sweep across §40–§44 + final 5-gate green check).

> **v3.13.16 update (Phase J13 — `riseup-git-logs` upload protocol):** Fourth J-series slot ships per the `.lovable/plan.md` locked design. New slot §43 `43-cli-upload-protocol.md` v1.0.0 carries the deterministic NDJSON-over-HTTPS transport contract for streaming classified frames from the CLI to the spec/22 plugin. 7 normative sections: §1 Purpose & Scope; §2 Transport (POST `/wp-json/git-logs/v1/upload` canonical https-only + SSH-key Lane B + `application/x-ndjson` LF-terminated + gzip>4 KiB); §3 9-field per-frame envelope closed set (`frameId`/`repoIdentityHash`/`currentHead`/`subcommand`/`outcome`/`classifierVersion`/`payload`/`emittedAt`/`redact`) `additionalProperties: false` + 16-entry forbidden-field list; §4 Idempotency (`Idempotency-Key` UUIDv4-per-batch + 24h server retention + sha256-mismatch → 409 + frame-level dedup belt-and-braces); §5 Retry & Backoff (NO 4xx retries; 6-attempt exponential-backoff-with-full-jitter on 5xx/network 1s/2s/4s/8s/16s; cryptoRand mandatory; identical body + Idempotency-Key on retry; re-derivation FORBIDDEN); §6 Server Response 6 fields (`batchId`/`framesReceived`/`framesDeduplicated`/`uploadedAt`/`expiresAt`/`autofixDownloadUrl`) feeding back into §41 pointer-file; §7 Forbidden Patterns + 7-row error-envelope mapping (400/401/409/413/429/5xx → `GL-UPLOAD-*`). New §97 **AC-85** `[critical]` binds all 7 sub-contracts. AC-80 Sibling File Delegation Map gains row 37 for §43. AC count 78 → **79**. Banners: §97 v3.16.0 → **v3.17.0** (minor — new AC + AC-80 row), §00 v3.13.15 → **v3.13.16**, §98 v3.16.0 → **v3.17.0** (mirrors §97 minor + new row), this file v3.13.15 → **v3.13.16** (+ audit row + h10 stamp 154). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump.** **Lesson #19 audit-boundary pin** on the transport-protocol axis (fourth J-series instance after CLI-binary J10 + wire-format J11 + classifier-decision J12). **Lesson #21 intra-module delegation** (AC-80 row 37 same-PR per Lesson #39). **Lesson #36 quintuple link-don't-restate** to spec/22 §04 + §05 + §15 + §31 + §39. **Lesson #37 third-instance** integration-axis co-application. **Lesson #34 holds**: J13 is normative-surface-extension work; cache real CRITICAL count tree-wide remains 0 awaiting A8. Verified: lockstep + tree-health gates run post-memo. **Remaining J-series**: J14 (§44 auto-fix protocol hybrid 4-state propose-diff-confirm + AC-86), J15 (§45 test plan + AC-87 + final 5-gate sweep). **NOTE (added at J14):** AC-85 §97 body was never written in J13 — see v3.13.17 row above for backfill.

> **v3.13.15 update (Phase J12 — `riseup-git-logs` classifier rules):** Third J-series slot ships per the `.lovable/plan.md` locked design. New slot §42 `42-cli-classifier-rules.md` v1.0.0 carries the deterministic decision-function for the 4-subcommand wrap-set with 6 normative sections: §1 Purpose & Scope; §2 4-outcome closed set (`NORMAL`/`WARN`/`ERROR`/`INTERNAL`) + `ExitCode` mapping per spec/13 AC-21 link-don't-restate; §3 5-step deterministic precedence (forbidden-subcommand → identity-discovery failure → exit-non-zero ERROR-pattern → exit-zero WARN-pattern → fallback NORMAL); §4 per-subcommand rule tables Section A `git log` (LOG-N1/W1/W2 + LOG-E1/E2/E3) / B `git status` (STAT-N1/W1/W2 + STAT-E1/E2) / C `git diff` (DIFF-N1/W1/W2 + DIFF-E1/E2) / D `git show` (split-and-piggyback + SHOW promotion + frame-level severity ordering); §5 forbidden non-determinism (`LANG=C` mandatory; no `~/.gitconfig`/clock/cache reads); §6 classifier versioning (banner version IS classifier version; NDJSON `classifierVersion` per §43; `MAJOR` bump = coordinated upgrade event under follow-up §97 extension AC). New §97 **AC-84** `[critical]` binds all 6 sub-contracts; FORBIDDEN: restating any rule row inline in §40/§41/§43/§44/§45 (those MUST cite by Section + rule-ID anchor); FORBIDDEN: restating spec/13 AC-21 `ExitCode` enum body inside §42; FORBIDDEN: emitting any outcome outside the 4-state closed set; FORBIDDEN: classifying without setting `LANG=C`; FORBIDDEN: suppressing upload of any classified frame at the CLI layer. AC-80 Sibling File Delegation Map gains row 36 for §42. AC count 77 → **78**. Banners: §97 v3.15.0 → **v3.16.0** (minor — new AC + AC-80 row), §00 v3.13.14 → **v3.13.15**, §98 v3.15.0 → **v3.16.0** (mirrors §97 minor + new row), this file v3.13.14 → **v3.13.15** (+ audit row + h10 stamp 154). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump.** **Lesson #19 audit-boundary pin** on the classifier-decision axis (third J-series instance after CLI-binary J10 + wire-format J11). **Lesson #21 intra-module delegation** (AC-80 row 36 same-PR per Lesson #39). **Lesson #36 dual link-don't-restate** to spec/13 AC-21 `ExitCode` enum + spec/22 §15 `GL-CLASSIFIER-*` error codes. **Lesson #34 holds**: J12 is normative-surface-extension work; cache real CRITICAL count tree-wide remains 0 awaiting A8. Verified: lockstep + tree-health gates run post-memo. **Remaining J-series**: J13 (§43 upload protocol NDJSON + AC-85), J14 (§44 auto-fix protocol hybrid 4-state + AC-86), J15 (§45 test plan + AC-87 + final 5-gate sweep).

> **v3.13.14 update (Phase J11 — `.riseup-git-logs.json` pointer-file wire format):** Second J-series slot ships per the `.lovable/plan.md` locked design ("pointer-file = structured JSON `.riseup-git-logs.json` at git root mirroring spec/14 AC-23 wire-format pattern"). New slot §41 `41-cli-pointer-file-schema.md` v1.0.0 carries the verbatim Draft-07 JSON Schema in §3 as the single source of truth; CLI implementations MUST embed it verbatim and validate on every read AND every write. New §97 **AC-83** `[critical]` binds: (1) file location & lifecycle (`<git-root>/.riseup-git-logs.json` UTF-8 LF mode `0644`; atomic temp-then-rename per spec/13 §97 AC-22 link-don't-restate; first-write source `riseup-git-logs init`; CLI MUST NOT mutate `.gitignore`); (2) closed-set 8-field inventory (`schemaVersion` SemVer · `repoIdentityHash` 64-hex sha256 immutable · `repoCanonicalRemote` URL auditability · `currentHead` 40-hex git SHA · `uploadUrl` https · `uploadedAt` RFC 3339 UTC · `expiresAt` RFC 3339 UTC · `autofixDownloadUrl` https or null); `additionalProperties: false`; (3) verbatim Draft-07 schema with `dependencies` clause pairing `uploadUrl`+`uploadedAt`+`expiresAt`; (4) 11-entry forbidden-field list (`token`/`apiKey`/`secret`/`password`/`sshKey`/`privateKey`/`userId`/`userEmail`/`ownerEmail`/`env`/`platform` — last three mirror spec/22 §07 locked-decision-12 per Lesson #36); (5) validation discipline (5-row trigger×action table) + SemVer evolution rules (PATCH/MINOR/MAJOR with MAJOR requiring own §97 extension AC + AC-80 row 35 annotation). AC-80 Sibling File Delegation Map gains row 35 for §41. AC count 76 → **77**. Banners: §97 v3.14.0 → **v3.15.0** (minor — new AC + AC-80 row), §00 v3.13.13 → **v3.13.14**, §98 v3.14.0 → **v3.15.0** (mirrors §97 minor + new row), this file v3.13.13 → **v3.13.14** (+ audit row + inventory row + h10 stamp 154). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump.** **Lesson #19 audit-boundary pin** on the wire-format axis. **Lesson #21 intra-module delegation** (AC-80 row 35 same-PR per Lesson #39). **Lesson #36 link-don't-restate** to spec/13 AC-22 atomic-write + spec/14 AC-23 pattern + spec/05 AC-SD-23 TTL precedent + spec/22 §07 forbidden fields + spec/22 §15 error code. **Lesson #34 holds**: J11 is normative-surface-extension work; cache real CRITICAL count tree-wide remains 0 awaiting A8. Verified: lockstep + tree-health gates run post-memo. **Remaining J-series**: J12 (§42 classifier + AC-84), J13 (§43 upload + AC-85), J14 (§44 auto-fix + AC-86), J15 (§45 test plan + AC-87 + final sweep).

> **v3.13.13 update (Phase J10 — `riseup-git-logs` CLI subsystem entry-point):** User request closed: "Phase J will be CLI" (Git Log CLI binary that wraps git, classifies output, uploads to server, surfaces auto-fix URL via JSON pointer file at git root). New slot §40 `40-cli-overview.md` v1.0.0 ships as the tier-1 entry point for the J-series CLI subsystem (slots §40–§45 / AC-82–AC-87 reserved per Phase J plan). New §97 **AC-82** `[critical]` binds: (1) standalone `riseup-git-logs` binary identity; (2) closed-set wrap scope of 4 read-only git subcommands (`log`/`status`/`diff`/`show`) + 11-entry FORBIDDEN mutating-subcommand list; (3) 5-step deterministic identity-discovery algorithm (`rev-parse --show-toplevel` → read pointer → `rev-parse HEAD` → `repoIdentityHash = sha256(canonical-remote-url)[0:32]` → bind to NDJSON frames); (4) first-run `riseup-git-logs init` bootstrap discipline (no `.gitignore` mutation; atomic temp-then-rename per spec/13 AC-22); (5) 6 cross-module link-don't-restate surfaces (spec/13 §97 AC-21 `ExitCode` enum + AC-22 concurrency + AC-23 subcommand checklist; spec/22 §04 REST + §31 SSH-key Lane B + §39 split-DB). AC-80 Sibling File Delegation Map gains row 34 for §40. AC count 75 → **76**. Banners: §97 v3.13.5 → **v3.14.0** (minor — new AC + AC-80 row), §00 v3.13.12 → **v3.13.13**, §98 v3.13.12 → **v3.13.13** (+ row), this file v3.13.12 → **v3.13.13** (+ audit row + inventory row + h10 stamp 153 → 154). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump.** Auto-fix design choice: **hybrid** (server proposes patch; CLI shows diff; user confirms before write — FORBIDDEN silent apply). Pointer-file format: **structured JSON** at git root (mirrors spec/14 AC-23 update-interface-contract pattern; concrete schema deferred to Phase J11 / AC-83 / slot §41). **Lesson #19 audit-boundary pin** on the new CLI-binary axis (mirror of spec/12 AC-10 + spec/14 AC-23). **Lesson #21 intra-module sibling-file delegation** (AC-80 row added in same PR as file ship per Lesson #39 second-axis discipline). **Lesson #36 link-don't-restate** to spec/13 framework (no exit-code matrix or concurrency strategy restated in spec/22). **Lesson #34 holds**: J10 is normative-surface-extension work; cache real CRITICAL count tree-wide remains 0 awaiting A8 LLM-gateway refresh. Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅; version-parity 77/77 ✅. **Remaining J-series**: J11 (§41 pointer-file JSON Schema + AC-83), J12 (§42 classifier rules + AC-84), J13 (§43 upload protocol + AC-85), J14 (§44 auto-fix protocol + AC-86), J15 (§45 test plan + AC-87 + final 5-gate sweep).

> **v3.13.12 update (Phase E3 — §34 test-skeleton scaffolds binding AC-05 + AC-12 T-IDs):** User request closed: continue spec-only improvements; E3 from queued plan ("author test/AcDedupWindowTest.php + test/ac-dedup-window.bats skeletons under §34" + ingest mirror). Pre-E3 §97 AC-05 (E1) and AC-12 (E2) verifies-clauses both cited `34-phpunit-test-skeleton.md` per AC-80 sibling delegation, but §34 ended at v2.7.0 with NO scaffolds for the T-DEDUP-* or T-INGEST-* matrices — Lesson #36 link-don't-restate bidirectionality broken (§97 → §34 link existed; §34 had no landing surface). Resolution: §34 v2.7.0 → **v2.8.0** with 5 new normative subsections — §34.A `tests/phpunit/Logger/AcDedupWindowTest.php` (11 `markTestIncomplete('T-DEDUP-NN')` methods); §34.B `tests/phpunit/Ingest/AcStreamingIngestTest.php` (11 `T-INGEST-NN` methods); §34.C bats companions (`ac-dedup-window.bats` + `ac-streaming-ingest.bats`) with 22 `skip` stubs; §34.D coverage-gap detection contract (CI must grep both files for every §97 T-ID; missing IDs raise `GL-TEST-COVERAGE-GAP`); §34.E clock-injection contract reference (cites §97 AC-29 §5 by anchor; restating signature in §34 = FORBIDDEN). All scaffolds use `markTestIncomplete()` / `skip` so green CI is preserved. **No new AC** (count 75 → 75); banners patch-only: §34 v2.7.0 → **v2.8.0** (minor — new subsections), §00 v3.13.11 → **v3.13.12**, §98 v3.13.11 → **v3.13.12**, this file v3.13.11 → **v3.13.12**. §97 NOT bumped (AC-05 + AC-12 verifies-clauses already cite §34 by anchor — no contract change). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change.** **Lesson #36 mirror-bidirectionality codified** (§34.D + §34.E): link-don't-restate is bidirectional — if §97 ACs cite §34 by anchor, §34 MUST contain a discoverable landing surface; AC declaration + landing surface ship in same lockstep PR. **Lesson #34 holds**: test-coverage-followability work; cache real CRITICAL count tree-wide remains 0. Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅; version-parity 77/77 ✅.

> **v3.13.11 update (Phase E2 — AC-12 T-INGEST-01..11 scenario matrix + AC-29 §5 ClockInterface contract):** User request closed: continue spec-only improvements; E2 from queued plan ("testable GWT for AC-12 streaming + clock-injection AC-29 subsection"). Pre-E2 AC-12 was a single ~2 KB normative paragraph — no discoverable test-case enumeration; T-DEDUP-* mirror needed on the ingest axis. Pre-E2 AC-29 was a 4-line stub; clock-injection (flagged as queued by E1 changelog row) was unauthored, leaving T-DEDUP-04 + T-INGEST-06/T-INGEST-10 unimplementable as fast unit tests. Resolution: extended AC-12 with an 11-row T-INGEST-01..11 matrix (chunked happy / buffered Content-Length / mid-stream MaxLineBytes breach / MaxLinesPerPush breach / MaxPushPayloadBytes breach / slow-loris timeout / memory-bound / atomic-rollback on validation failure / logger-gating-during-ingest / clock-injection determinism / cross-request cursor isolation). Added AC-29 §5 normative `ClockInterface { now(), nowMs() }` contract — production binds `SystemClock`; tests bind `FakeClock` exposing `advance()` + `set()`. FORBIDDEN: direct `time()`/`microtime()`/`date()`/`DateTimeImmutable("now")`/`gmdate()`/`hrtime()` from `inc/Logger/`, `inc/Ingest/`, `inc/Auth/Ssh/`, `inc/RateLimit/` (4 grep paths); migration-runner exempt (forensic, not behavioural). AC-29 verifies-clause widened to bind AC-05 (T-DEDUP-04) + AC-12 (T-INGEST-06, T-INGEST-10) + AC-64 (`SshKey.RotationDueAt`). **No new AC** (AC count 75 → 75); banners patch-only: §97 v3.13.4 → **v3.13.5**, §00 v3.13.10 → **v3.13.11**, §98 v3.13.10 → **v3.13.11**, this file v3.13.10 → **v3.13.11**. **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change.** **Lesson #19 sixth-axis confirmation on spec/22**: AC-78 + AC-81 + AC-11 + AC-23 + AC-05 + AC-12/AC-29 §5 — six normative-content axes lifted to tier-1. **Lesson #36 reinforcement**: clock-injection contract owned by AC-29 §5; AC-05 + AC-12 + AC-64 cite it by anchor. **Lesson #34 holds**: implementer-discoverability + test-determinism work; cache real CRITICAL count tree-wide remains 0. Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅.
> **v3.13.10 update (Phase E1 — AC-05 testable scenario matrix inline):** User request closed: continue spec-only improvements; E1 from queued plan ("testable GWT for AC-05 dedup window"). Pre-E1 AC-05 was a single 1.6 KB dense paragraph; implementers had to derive scenarios from prose. Resolution: extended AC-05 with an 11-row testable scenario matrix `T-DEDUP-01..11` (T-ID × Scenario × Input × Expected output × Verifies clause) covering rolling-window / burst / error+fatal carve-out / LRU eviction / AC-04 ordering / cross-worker semantics; added test-suite assertion contract binding `test/AcDedupWindowTest.php` (PHPUnit) and `test/ac-dedup-window.bats` (bash harness) — every T-ID MUST appear in test method/case name; absence = `GL-TEST-COVERAGE-GAP`. Time-mocking contract delegates to AC-29 §"Test-time clock injection" (E2 follow-up if not yet authored). **No new AC** (AC count 75 → 75); banners patch-only: §97 v3.13.3 → **v3.13.4**, §00 v3.13.9 → **v3.13.10**, §98 v3.13.9 → **v3.13.10**, this file v3.13.9 → **v3.13.10**. **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change.** **Lesson #19 fifth-axis confirmation on spec/22**: AC-78 (inventory) + AC-81 (enums) + AC-11 (endpoints) + AC-23 (CHECK constraints) + AC-05 (test scenarios) — five normative-content axes lifted to tier-1. **Lesson #36**: test-fixture skeletons in §34 MUST cite by anchor + T-ID, not restate. **Lesson #34**: implementer-discoverability + test-coverage-followability work; cache real CRITICAL count tree-wide remains 0. Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅.

> **v3.13.9 update (Phase D3 — AC-23 CHECK-constraint inventory inline):** User request closed: continue spec-only improvements; D3 from queued plan ("CHECK constraints inline from `18-schema.sql`"). Survey via `grep -nE "CHECK \(" spec/22-git-logs-v2/18-schema.sql` returned 5 hits (4 boolean-domain + 1 row-level exactly-one polymorphic on `AppLink`). Pre-D3 AC-23 only asserted PascalCase + AUTOINCREMENT — SQLite domain enforcement was entirely tier-2 in `18-schema.sql`. Resolution: extended AC-23 with a normative 6-column 5-row CHECK-constraint inventory table (Table × Column × Expression × Kind × `18-schema.sql` line × Drift signal). Cross-language storage convention DELEGATED to spec/04 §2.1 + migration discipline DELEGATED to AC-29 per Lesson #36 (restating in spec/22 = FORBIDDEN). Tri-state NULL exception explicitly noted as not-yet-applicable. **No new AC** (AC count 75 → 75); banners patch-only: §97 v3.13.2 → **v3.13.3**, §00 v3.13.8 → **v3.13.9**, §98 v3.13.8 → **v3.13.9**, this file v3.13.8 → **v3.13.9**. **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change.** **Lesson #19 fourth-axis confirmation on spec/22**: AC-78 (inventory) + AC-81 (enums, C1) + AC-11 (endpoints, D2) + AC-23 (CHECK constraints, D3) — four normative-content axes lifted to tier-1. **Lesson #36 reinforcement**: spec/04 §2.1 owns boolean storage; AC-23 cites it. **Lesson #34 holds**: implementer-discoverability + audit-walker-followability work; cache real CRITICAL count tree-wide remains 0. Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅.

> **v3.13.8 update (Phase D2 — AC-11 per-endpoint binding matrix inline):** User request closed: continue spec-only improvements; D2 from queued plan ("per-endpoint JSON Schema inline contracts in spec/22"). Pre-D2 AC-11 was a 4-line stub citing `§Endpoints, §04, §17` — but `04-rest-api-endpoints.md` (406 lines) and `17-openapi.yaml` (tier-2) are not in the walker tier-1 bundle (per slot 34 v1.1.0 AC-34-09), so an audit walker could not verify per-endpoint shape from §97 alone. Resolution: extended AC-11 with a 10-row normative binding matrix — every endpoint maps to (Method, Path, Request body schema name, Response body schema name, Auth class, Error envelope codes). Full JSON schemas remain canonical in `17-openapi.yaml` per Lesson #36 — restating those schemas inline here is FORBIDDEN. AC-11 verifies-clause widened to cite AC-12, AC-14, AC-30, AC-67–AC-72 (envelope/streaming-contract dependencies). **No new AC** (AC count 75 → 75); banners patch-only: §97 v3.13.1 → **v3.13.2** (AC-11 body extension), §00 v3.13.7 → **v3.13.8** (banner), §98 v3.13.7 → **v3.13.8** (banner + row), this file v3.13.7 → **v3.13.8** (banner + this audit row). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change.** **Lesson #19 third-axis confirmation on spec/22**: AC-78 (module-kind/inventory) + AC-81 (enum-catalog) + AC-11 extension (endpoint-inventory) — three normative-content axes lifted to tier-1. **Lesson #36 reinforcement**: matrix is a binding TABLE (schema names + paths + auth classes), NOT a restatement of OpenAPI content. **Lesson #34 holds**: this is implementer-discoverability + audit-walker-followability work; cache real CRITICAL count tree-wide remains 0. Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅.

> **v3.13.6 update (Phase C1 — AC-81 Enum Catalog promotion):** User request closed: continue spec-only improvements; C1 from queued plan ("spec/22 enum catalog promotion"). Survey of `01-glossary-and-enums.md` (14 KB, tier-2) found 11 active enum types + 1 deprecated stub canonically defined in `18-schema.sql` lookup-table seeds + mirrored in TS `## TypeScript Mirror` block; pre-AC-81 the catalog had ZERO §97 tier-1 binding (only AC-78 evidence row line 505 calling out `[D4] Truncated Glossary` as a walker-cap artifact). Added `[critical]` **AC-81** to §97 with a 13-row normative table — every enum maps to (Codes, Authority, Drift signal). Codes ≤6 listed in full; `Permission` (17), `SystemEventType` (16), `AuditActionType` (19) listed in full with count. AC delegates the Drift-detection precedence (`18-schema.sql` canonical → glossary tables canonical mirror → TS mirror hand-maintained → PHP `cases()` boot-generated) to source file per **Lesson #36**. Forbids restating per-code semantics in any other spec/22 file (`02`/`04`/`19`/`15`); every cite MUST be by enum-name only. **C2 (physical reorder of `01-glossary-and-enums.md`) consciously DEFERRED** — AC-81 provides the audit-walker tier-1 binding that reorder would have provided, while preserving stable line-anchored citations (e.g., AC-78 line 505 evidence row, AC-80 row for `01-glossary-and-enums.md`); reorder would invalidate them and is a content-shuffle with no normative gain post-AC-81. C3 (lockstep) shipped. AC count 74 → **75**. Banners: §97 v3.12.0 → **v3.13.0** (minor — new AC); §00 v3.13.5 → **v3.13.6** (banner — patch); §98 v3.13.5 → **v3.13.6** (banner + row — patch); this file v3.13.5 → **v3.13.6** (banner + this audit row — patch). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump · no `01-glossary-and-enums.md` content change.** **Lesson #19 second-axis confirmation**: AC-78 closes audit-boundary for module-kind/inventory class; AC-81 closes it for enum-catalog class. **Tier-1 normative-surface closure quartet complete for spec/22**: AC-78 + AC-79 + AC-80 + AC-81. Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅.


> **v3.13.4 update (Phase 153 P3 — AC-79 Cross-Module Externalized Citation Map):** User request closed: "Add explicit normative anchors for any cross-module externalized citations in spec/22 so the auditor can follow the dependency chain." Survey via `rg -no "spec/[0-9]+-[a-z-]+" spec/22-git-logs-v2/*.md` surfaced 6 externalized contract dependencies lacking a §97-bound anchor. Added `[critical]` **AC-79** to §97 with a 6-row normative table — each row pins (External cite, Owning module + AC, Cited from spec/22 file, Citation purpose, Restate-in-22 forbidden?). Rows: spec/04 (DDL conventions, cited from 02-database-schema.md L368), spec/12 (CI runner matrix, cited from 05-auth-and-validation.md L114), spec/13 §97 AC-22 (SQLite locking, cited from AC-26 + AC-78 line 505), spec/26 (Mermaid diagrams, cited from 39-split-db-log-storage.md L184–185), spec/05 (split-DB pattern, cited from 39-split-db-log-storage.md L186), linter-scripts/check-spec-folder-refs.py (allowlist enforcement, cited from AC-22-LV1 line 477). Restate-FORBIDDEN by construction (Lesson #36 dual-source drift class). Append-only within a phase. Cross-references mirror of spec/02 AC-CG-21 (Subfolder Delegation Map — same pattern on intra-module sub-folder axis) + spec/12 AC-11 (linter-script anchor pattern from A24-fu4). AC count 73 → **74**. Banners: §97 v3.10.2 → **v3.11.0** (minor — new AC); §00 v3.13.3 → **v3.13.4** (banner — patch); §98 v3.13.3 → **v3.13.4** (banner + row — patch); this file v3.13.3 → **v3.13.4** (banner + this audit row — patch). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump.** **Lesson #37 second-instance confirmation**: spec/12 (A24-fu4) was the codifying instance for integration-axis modules co-needing Lesson #19 (audit-boundary pin) + Lesson #36 (cross-module anchor) ACs together; spec/22 (this phase) confirms the pattern — AC-78 (Lesson #29 module-kind pin) + AC-79 (Lesson #36 cross-module anchor) ship as a complete pair for the integration-axis class.

> **v3.13.3 update (Phase 153 P1 — AC-78 body inlined to §00 to break harness-saturation ceiling):** Fourth-instance application of the Lesson #65 saturation-class playbook on spec/22 (after fu29 §98 split, fu39 §99 split, fu20 §00 teaser promotion). Diagnosis: 3 prior pin attempts left the auditor still re-flagging the same 3 findings (audit-v9/v10 cache D5/D4/D3) because AC-78's BODY (≈2.7 KB of disposition rules + on-disk evidence triple) lives at §97 line 503/507 — past the 140 KB tier-1 walker cap (tier-1 sums to ~148 KB: §00=14 + §97=73 + §98=45 + §99=16). The §00 teaser table at A24-fu20 surfaces only AC IDs, not the disposition rules — so the auditor sees the pointer but cannot verify the contract. Resolution: inlined the full AC-78 disposition (4-row on-disk evidence triple table + 5 numbered disposition rules + walker tier-1 footprint table) into a new normative `## Walker-Cap Finding Disposition (Normative)` section in §00 immediately under the existing Walker-Pin teaser. Internal §00↔§97 mirror is permitted (Lesson #36 forbids only cross-MODULE restatement; same-module mirroring is required for harness-saturated modules per Lesson #65 — precedent: spec/13 P3 §10/§18 mirror, spec/27 AC-T-34, spec/05 AC-SD-21 walker fix). Banners: §00 v3.13.2 → **v3.13.3**; §98 v3.13.2 → **v3.13.3**; this file v3.13.2 → **v3.13.3**. §97 NOT bumped (AC-78 unchanged — only its body is mirrored to §00). Expected cache lift on next re-score: 87 → 92+. **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump · no new AC.**

> **v3.11.0 update (Phase 153 Task A24-fu20 — AC-78 §00 walker-pin promotion):** Pure walker-visibility patch on the audit-v9 83-floor (walker 3/36 — textbook walker-saturation). Audit-v9 surfaced 3 findings (HIGH/D5 "Missing Core Normative Files", MEDIUM/D4 "Abstract Examples", LOW/D3 "Concurrency Externalized") — ALL diagnosed as cache-staleness artifacts of pre-existing AC-78 contract pin (Lesson #34). AC-78 was authored at A11h (v3.10.0) but lives at §97 line 503+ of 507 — past every walker tier-1 cap. Promoted into a `> 🤖 Walker-Pin (Lesson #55 + Lesson #61)` 3-row teaser table at the §00 head listing AC-78 + AC-22-LV1 + AC-26 cross-ref + 3 forbidden remediation patterns. Walker now sees ALL 3 structural anchors in the first ~2 KB. Banners: §00 v3.10.0 → **v3.11.0**; §98 v3.10.0 → **v3.11.0**; this file v3.10.0 → **v3.11.0**. §97 NOT bumped (no AC change). h10 stamp refreshed 32 → 153. **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no new AC.** All 3 strict gates expected GREEN. **Lesson #61 second-instance confirmation**: spec/22 is the **pure-promotion variant** of A24-fu19's spec/04 codifying instance — lightest-touch close-out for any module where the auditor keeps re-flagging findings already classified by a §97-buried structural-pin AC.

> **v3.10.0 update (Phase 153 Task A11h — AC-78 module asset inventory pin (Lesson #29 + Lesson #36)):** closes audit-v5 D5 HIGH "Missing Core Schema and API Definitions" + D4 MED "Missing Concrete Code Fixtures" + D3 LOW "Concurrency/Race Condition on Rate Limits" as harness bundling-cap artifacts — every cited file present on disk per this report's File Inventory (slots 04, 18, 34 all confirmed); AC-26 persisted-floor concurrency correctly cross-referenced to spec/13 §97 AC-22 per Lesson #36 link-don't-restate; locked-vacant slots `09-13` enforced by AC-22-LV1. Mirror of spec/13 AC-24 + spec/28 AC-28-41 + spec/14 AC-21 + spec/16 AC-21. AC count 78 → 79. §97 v3.9.5 → v3.10.0; §00 v3.9.14 → v3.10.0; §98 v3.9.14 → v3.10.0. No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump.

---

## File Inventory
<!-- verified-phase: 155 -->

| File | Present |
|------|---------|
| 00-overview.md | ✅ (v3.9.12 — Phase P19 H10 parity catch-up; content current as of Phase P17 §28-universal-ci-cli Cross-References row) |
| 01-glossary-and-enums.md | ✅ (v3.9.0 — Phase P1 added `## TypeScript Mirror` section + drift-detection contract; closes GAP-V2-02) |
| 02-database-schema.md | ✅ (v3.8.11 — Canonical DDL excerpt inlined per Phase 20 G-CON-01) |
| 03-admin-ui.md | ✅ (v2.3.0 — Phase P18 added `## State-Transition Label Rendering` section + History `HasError + StateLabel` column revision binding the consumer-side AC-73 label enum) |
| 04-rest-api-endpoints.md | ✅ (v2.9.6 — Phase P4 §1.2 pre-parse caps & 11-step validation order; Phase P3 Ack `PreviousHasError`; Phase P2 §1.1 NDJSON ingest streaming wire format) |
| 05-auth-and-validation.md | ✅ (CI/CD cross-ref) |
| 06-migrations-and-logger.md | ✅ |
| 07-app-entity.md | ✅ |
| 08-history-and-action.md | ✅ |
| 09-seed-data.md | ⚠️ Referenced but not yet authored — seeds live inline in `18-schema.sql` for now |
| 10-rate-limit-and-payload.md | ⚠️ Referenced; constants live in `ConfigKv` defaults inside `18-schema.sql` |
| 11-encryption-deferred-plan.md | ⚠️ Referenced; v3 plan summarized in §30 threat model |
| 12-wp-plugin-scaffold.md | ⚠️ Referenced; PSR-4 layout described in `mem://specs/git-logs.md` |
| 13-v1-vs-v2-mapping.md | ⚠️ Referenced; v1 deltas captured in changelog + `21-git-logs/` legacy banner |
| 14-endpoint-examples.md | ✅ |
| 15-error-codes.md | ✅ (v2.9.4 — Phase P16 added `## Streaming ingest (Lane B — see §04 §1.2)` section with 4 `GL-STREAM-*` codes) |
| 16-seed-data.md | ✅ (sole occupant of slot 16 since Phase P5 collision resolution) |
| 17-openapi.yaml | ✅ (v2.9.6 — Phase P16 `ErrorCode` enum gained 4 `GL-STREAM-*` ingest-streaming entries) |
| 18-schema.sql | ✅ (Prune + Restore seeds added in v2.6) |
| 19-permission-matrix.md | ✅ |
| 20-observability.md | ✅ |
| ~~21-i18n.md~~ | 🗑️ removed v3.7.8 — slot retired (i18n out of scope for v2; see audit row below) |
| 22-retention-and-pruning.md | ✅ |
| 23-backup-restore.md | ✅ |
| 24-multisite.md | ✅ |
| 25-headless-auth-notes.md | ✅ |
| 26-readme-and-screenshots.md | ✅ |
| 27-wp-cli-reference.md | ✅ |
| 28-example-github-actions.md | ✅ |
| 29-uninstall-policy.md | ✅ |
| 30-threat-model.md | ✅ |
| 36-why-v1-archived.md | ✅ (added 2026-04-25) |
| 37-blind-ai-gap-analysis.md | ✅ (v1.5.0 — Phase P17 marked GAP-V2-09 RESOLVED; 9 of 10 historical gaps closed; effort remaining 7m → 5m) |
| 38-test-plan-superseded.md | ✅ (relocated from `16-test-plan.md` in Phase P5 2026-04-28 — slot-16 collision resolution; redirect stub → §32–§35) |
| 39-split-db-log-storage.md | ✅ (added v3.8.0 2026-04-26 — per-SHA SQLite storage spec) |
| 40-cli-overview.md | ✅ (v1.0.0 — Phase J10; tier-1 entry point for J-series CLI subsystem; bound by §97 AC-82) |
| 41-cli-pointer-file-schema.md | ✅ (v1.0.0 — Phase J11; tier-1 wire format for `.riseup-git-logs.json`; bound by §97 AC-83) |
| 42-cli-classifier-rules.md | ✅ (v1.0.0 — Phase J12; tier-1 decision-function for the 4 wrapped subcommands; bound by §97 AC-84) |
| 43-cli-upload-protocol.md | ✅ (v1.0.0 — Phase J13; tier-1 NDJSON-over-HTTPS transport; bound by §97 AC-85) |
| 44-cli-autofix-protocol.md | ✅ (v1.0.0 — Phase J14; tier-1 hybrid 4-state propose-diff-confirm-apply; bound by §97 AC-86) |
| 46-server-upload-frames-endpoint.md | ✅ (v1.0.0 — Phase K1; tier-1 server-side NDJSON receiver `POST /wp-json/git-logs/v3/upload-frames` Lane B writes only; bound by §97 AC-88) |
| 47-server-autofix-endpoint.md | ✅ (v1.0.0 — Phase K2; tier-1 server-side fix-bundle producer `GET /wp-json/git-logs/v3/autofix/{repoIdentityHash}/{frameId}` Lane B reads only + single-consumption + server-issued URL discipline; bound by §97 AC-89) |
| 97-acceptance-criteria.md | ✅ (v3.9.4 — Phase P18 added AC-77 binding the §03 v2.3.0 History `HasError + StateLabel` rendering contract to AC-73's label enum + AC-74's NDJSON consumer; AC count 77 → 78) |
| 98-changelog.md | ✅ |
| 99-consistency-report.md | ✅ |

## Cross-link validation

- `00-overview.md` → §00–§30 + §97–§99: OK
- `15-error-codes.md` covers every code referenced from §22, §23, §25, §27: OK
- `97-acceptance-criteria.md` AC-26..AC-41 reference §10, §17–§26 sources: OK
- `18-schema.sql` `AuditActionType` seed includes Prune (19), Restore (20): OK
- `30-threat-model.md` deferral list cross-links to `11-encryption-deferred-plan.md` (queued file)
- `17-openapi.yaml` `ErrorCode` enum mirrors all 41 runtime `GL-*` codes from `15-error-codes.md` (37 pre-P16 + 4 `GL-STREAM-*` ingest codes added Phase P16; release-time `GL-RELEASE-*` excluded by design): OK
- `04-rest-api-endpoints.md` documents the 10-logical→8-path `?q=` collapse rule; AC-11 cross-links it: OK

## Naming compliance

- File prefixes 00–30, 97–99 sequential. ✅ (gaps at 09–13 noted as queued)
- Tables/columns PascalCase, PKs `{Table}Id`. ✅
- All `GL-*` error codes consolidated in `15-error-codes.md`. ✅
- Translatable scope honors §21. ✅

## Conflicts vs v1 (folder 21)

Resolved by parallel-folder strategy; v2 wins. **Deprecation banners (v2.7.1, 2026-04-25)** prepended to all 10 legacy files in `spec/_archive/21-git-logs-v1/` cross-linking back to v2 canonical source. Legacy v1 banner in `spec/_archive/21-git-logs-v1/00-overview.md` retained.

## Open items (not blocking)

1. **App identity (§07)** — still awaiting user confirmation on whether to add `Environment`, `Platform`, or `OwnerEmail`. Current set: `AppName`, `AppSlug`, `Description`, `ProfileId`, `AppStatusId`.

> Note: the 09–13 numbering gap is **intentional and locked** — content is distributed across §05 (rate limit/payload caps), §37 (seed data, moved from §16 in v2.8.6), §30 R3 (encryption-deferred plan), §31 (SSH-key auth supersedes scaffold notes), and `spec/_archive/21-git-logs-v1/` legacy banner (v1↔v2 mapping). Do not author standalone 09–13 files.

## Historical Audit Blocks (v2.8.7 → v3.9.14) — Archived

> **Archive pointer (Phase 153 Task A24-fu39):** 19 historical audit blocks (v2.8.7 Audit → Phase P7b / v3.9.14 Audit, 308 lines / ~46 KB) moved to [`_archive/99-consistency-report-pre-v3.12.0.md`](./_archive/99-consistency-report-pre-v3.12.0.md) to free AI-implementability walker bundle headroom (Lesson #65 / fu28-fu29 pattern). Mirror of A24-fu29 (§98 archive split). Live §99 reduced from ~58 KB to ~12 KB (-79%). Archive is frozen — historical reference only. Excluded from all 5 strict gates (`_archive/` is invisible to `check-version-parity.py`, `check-lockstep.cjs`, `check-tree-health.cjs` ARCHIVE_PREFIX, and `audit-ai-implementability.py` walker).

## v3.12.0 Audit — Phase 153 Task A24-fu29 (§98 archive split)

| File | Change |
|---|---|
| `spec/22-git-logs-v2/_archive/98-changelog-pre-v3.9.0.md` | **NEW.** Archive holding 48 historical changelog rows (v3.8.13 → v2.0.0) extracted from live `98-changelog.md` to free walker headroom. Frozen — never edited. |
| `spec/22-git-logs-v2/98-changelog.md` | Banner v3.11.0 → **v3.12.0**. Live row count 47 → 17 (kept v3.11.0 → v3.9.0 only). File size 100 KB → ~37 KB (-63 KB). New v3.12.0 row + archive pointer added at top. |
| `spec/22-git-logs-v2/00-overview.md` | Banner v3.11.0 → **v3.12.0** (no body change — pointer in §98 only). |
| `spec/22-git-logs-v2/99-consistency-report.md` (this file) | Banner v3.11.0 → **v3.12.0**; this audit table; no Health Score change. |

**Phase A24-fu29 scope discipline:** No §22 ACs added/removed (AC count unchanged at 79); no §22 DDL change, no §22 schema bump, no §22 error code, no §22 enum change, no §22 §00 body change beyond banner. Pure structural surgery to free AI-implementability walker bundle headroom. Mirror of A24-fu28 (spec/27 §98 split). Expected cache lift on next re-score: 83 → 86+ (D2 AC-coverage will improve as the auditor can now reach AC-78 walker-pin without changelog saturation). Archive folder `_archive/` is invisible to all 5 strict gates (verified at task close-out).

## v3.13.0 Audit — Phase 153 Task A24-fu39 (§99 archive split)

| File | Change |
|---|---|
| `spec/22-git-logs-v2/_archive/99-consistency-report-pre-v3.12.0.md` | **NEW.** Archive holding 19 historical audit blocks (v2.8.7 Audit → Phase P7b / v3.9.14 Audit), 308 lines / ~46 KB extracted from live `99-consistency-report.md` to free walker headroom. Frozen — never edited. |
| `spec/22-git-logs-v2/99-consistency-report.md` (this file) | Banner v3.12.0 → **v3.13.0**. Live size ~58 KB → ~12 KB (-79%). Lines 83–390 (19 audit blocks) replaced with single archive-pointer block; new v3.13.0 audit block added; Health Score / Validation History / File Inventory unchanged. |
| `spec/22-git-logs-v2/98-changelog.md` | Banner v3.12.0 → **v3.13.0**; new v3.13.0 row documenting this §99 split. |
| `spec/22-git-logs-v2/00-overview.md` | Banner v3.12.0 → **v3.13.0** (banner-only). |

**Phase A24-fu39 scope discipline:** No §22 ACs added/removed (AC count unchanged at 79); no §22 DDL change, no §22 schema bump, no §22 error code, no §22 enum change, no §22 §00 body change beyond banner; no §97 edit. Pure structural surgery — mirror of A24-fu29 (§98 archive split) applied to §99. Spec/22 tier1 walker bundle: §97 (71 KB) + §98 (37 KB) + §99 (58 KB → 12 KB) + §00 (13 KB) = 179 KB → ~133 KB (-26%). Combined with fu29's §98 split, total tier1 reduction since fu20 baseline (239 KB) is now ~106 KB (-44%). Expected cache lift on next re-score: 90 → 92+ (walker `files_used: 3/37 → 6+/37` likely; previously starved spec sub-files (15-error-codes, 14-endpoint-examples, 18-schema.sql) should now fit in budget). Archive folder `_archive/` is invisible to all 5 strict gates.


## v3.13.2 Audit — Phase 153 Task N3 (false-coverage inverse audit · AC-36 deprecated)

Tree-wide false-coverage inverse audit scanned 1241 ACs / 1128 verifies-clauses across 91 §97 files (3 refinement loops: 142 → 13 → 3 candidates → 1 real drift). Only **AC-36** in this module surfaced as a true drift: it cited `**Verifies:** §21` (Translatable scope), but §21 was retired in §98 v3.7.8 with rationale "i18n out of scope for v2" (see §00 inventory row 21). Resolution: AC-36 marked `[active]` → `[deprecated]` with new dormancy contract (§00 inventory row 21 + §98 v3.7.8 retirement entry as authoritative anchors). The §97 AC-count remains **76 total** (AC-36 still listed, just dormant — `[deprecated]` ACs are retained for cross-version diff per the §97 status-legend contract). **Tree-wide false-coverage rate: 1 / 1241 = 0.08%** — confirms the P3 Verifies-coverage sweep closed the structural gap; this is residual maintenance drift, not a class. Future §-retirement phases MUST grep `**Verifies:** §NN` against the retired number BEFORE closing (Lesson #39-class reinforcement). Memo: `phase-153-task-N3-false-coverage-inverse-audit.md`.

## Health Score

100/100 (A+) — 34 of 34 numbered files present (09–13 + 21 intentional gaps, locked per **AC-22-LV1**); cross-links valid (incl. §00↔§39, §01↔§02↔§15↔§18↔§31, §05↔§28↔§30↔§31 SSH lane chain, §02↔§15↔§22↔§23↔§29↔§39 split-DB chain, §97↔§05/§15/§18/§28/§30/§31 SSH AC chain, §04↔§10/§15/§17/§18/§39 NDJSON streaming chain, §01↔§02↔§18↔§04↔§17↔§97 PreviousHasError end-to-end chain, §15↔§17↔§18↔§97 NDJSON Phase 11 chain, §97↔§02/§04/§05/§10/§15/§17/§18/§26/§31/§39 Phase 13 deepened-AC cross-refs, §04↔§28/06 ingest-streaming wire-format chain (Phase P2), §04↔§17 PreviousHasError ack-envelope chain (Phase P3), §04→§15+§18+§97 pre-parse-caps cross-walk (Phase P4), §00↔§38↔§spec-index↔§dashboard↔§28/06 slot-16-collision-resolution chain (Phase P5), §00↔§37↔§97 locked-vacant-precedent chain (Phase P6), §37↔§97 GWT-completeness audit chain (Phase P7), **§22-§98↔§01-§97/§98/§99 Legacy-Index-exemption cross-folder mirror chain (Phase P7b — AC-SAG-28)**); AC coverage AC-01..AC-75 + AC-22-LV1 (**76 total, all GWT verified mechanically at Phase P7**, all `[active]`); ER diagram reflects v2.9.0 split-DB shape; OpenAPI v2.9.5 surfaces required `AckResponse.PreviousHasError` + optional `Header.StateTransition`; v3.9.0 closed GAP-V2-02; v3.9.1 closed GAP-V2-03; v3.9.2 closed GAP-V2-04; v3.9.3 closed GAP-V2-10; v3.9.4 closed slot-16 collision (and GAP-V2-08 in spirit); v3.9.5 closed GAP-V2-06 via locked-vacant precedent; v3.9.6 closed GAP-V2-01 via mechanical 76/76 GWT audit + headline refresh 76/100 → 99/100 (A+); **v3.9.7 mirrored Phase P7b — `## Legacy Index` GWT-completeness exemption codified at §01 (AC-SAG-28); 13-row tree-wide follow-up reclassified as intentional traceability infrastructure**. **Open follow-ups:** (a) §03 admin UI rendering of state labels — consumer-side, out-of-scope; (b) §15/§17/§97 lockstep for the 4 `GL-STREAM-*` codes (deferred from Phase P2); (c) GAP-V2-09 (link client CLI from §00) — LOW; (d) Phases P8–P15 (Phase P8 §37 effort-table refresh ✅ flags; P9 §26 slot gaps; P10 §26 SSH diagram; P11 §25 nested subdirs audit; P12 §28 thin section deepening; P13 §23 split overview; P14 §24 split overview; P15 §27 H10 version-field parity). All Phase 8/9/12/13 follow-ups closed; B1 closed (Phase 147); **all HIGH/MEDIUM blind-AI gaps for §22 are now closed.**

---

## Validation History

| Date | Version | Action |
|------|---------|--------|
| 2026-04-26 | current | Phase 31: Added Validation History + heading-rubric alignment for `check-tree-health.cjs` v2.0.0 quality dimension. No content removed. |
| 2026-04-25 | prior | Tree-wide audit baseline established (45/100 → roadmap to 100). |
| 2026-04-20 | prior | Module brought into alignment with parent §99 conventions. |
| 2026-04-16 | prior | Initial consistency report authored. |

This module's full lockstep history is mirrored in `98-changelog.md`; entries
above summarize only the audit-/validation-bearing milestones for `22-git-logs-v2`.

---

<!-- Phase 135: stale duplicate "File Inventory" block removed (2026-04-27).
     The authoritative inventory lives at the top of this file (lines 8–48)
     and already tracks §32–§37, §39, §97–§99 with version annotations.
     The duplicate block was a drift trap: it was last refreshed 2026-04-26
     and silently fell out of sync when §32–§35 (test scaffolds), §36/§37
     (audit memos) and §39 (split-DB) landed. Single source of truth wins. -->

