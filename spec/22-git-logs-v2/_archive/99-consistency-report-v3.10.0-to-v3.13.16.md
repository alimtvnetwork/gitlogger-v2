# Consistency-Report Audit-Block Archive — v3.10.0 → v3.13.16 (spec/22 git-logs-v2)

> **Frozen archive** — historical reference only; invisible to all 5 strict gates.
> 15 audit blocks promoted out of `99-consistency-report.md` to keep §99 within the audit walker tier-1 budget cap (Lesson #16 + Lesson #19 + Lesson #34 + Lesson #65 saturation-class playbook).
> Active tail (v3.13.17 onward — J14 + K-series + L-series) lives in [`../99-consistency-report.md`](../99-consistency-report.md).
> Earlier blocks (v2.8.7 → v3.9.14) live in [`./99-consistency-report-pre-v3.12.0.md`](./99-consistency-report-pre-v3.12.0.md).

---

> **v3.13.16 update (Phase J13 — `riseup-git-logs` upload protocol):** Fourth J-series slot ships per the `.lovable/plan.md` locked design. New slot §43 `43-cli-upload-protocol.md` v1.0.0 carries the deterministic NDJSON-over-HTTPS transport contract for streaming classified frames from the CLI to the spec/22 plugin. 7 normative sections: §1 Purpose & Scope; §2 Transport (POST `/wp-json/git-logs/v1/upload` canonical https-only + SSH-key Lane B + `application/x-ndjson` LF-terminated + gzip>4 KiB); §3 9-field per-frame envelope closed set (`frameId`/`repoIdentityHash`/`currentHead`/`subcommand`/`outcome`/`classifierVersion`/`payload`/`emittedAt`/`redact`) `additionalProperties: false` + 16-entry forbidden-field list; §4 Idempotency (`Idempotency-Key` UUIDv4-per-batch + 24h server retention + sha256-mismatch → 409 + frame-level dedup belt-and-braces); §5 Retry & Backoff (NO 4xx retries; 6-attempt exponential-backoff-with-full-jitter on 5xx/network 1s/2s/4s/8s/16s; cryptoRand mandatory; identical body + Idempotency-Key on retry; re-derivation FORBIDDEN); §6 Server Response 6 fields (`batchId`/`framesReceived`/`framesDeduplicated`/`uploadedAt`/`expiresAt`/`autofixDownloadUrl`) feeding back into §41 pointer-file; §7 Forbidden Patterns + 7-row error-envelope mapping (400/401/409/413/429/5xx → `GL-UPLOAD-*`). New §97 **AC-85** `[critical]` binds all 7 sub-contracts. AC-80 Sibling File Delegation Map gains row 37 for §43. AC count 78 → **79**. Banners: §97 v3.16.0 → **v3.17.0** (minor — new AC + AC-80 row), §00 v3.13.15 → **v3.13.16**, §98 v3.16.0 → **v3.17.0** (mirrors §97 minor + new row), this file v3.13.15 → **v3.13.16** (+ audit row + h10 stamp 154). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump.** **Lesson #19 audit-boundary pin** on the transport-protocol axis (fourth J-series instance after CLI-binary J10 + wire-format J11 + classifier-decision J12). **Lesson #21 intra-module delegation** (AC-80 row 37 same-PR per Lesson #39). **Lesson #36 quintuple link-don't-restate** to spec/22 §04 + §05 + §15 + §31 + §39. **Lesson #37 third-instance** integration-axis co-application. **Lesson #34 holds**: J13 is normative-surface-extension work; cache real CRITICAL count tree-wide remains 0 awaiting A8. Verified: lockstep + tree-health gates run post-memo. **Remaining J-series**: J14 (§44 auto-fix protocol hybrid 4-state propose-diff-confirm + AC-86), J15 (§45 test plan + AC-87 + final 5-gate sweep). **NOTE (added at J14):** AC-85 §97 body was never written in J13 — see v3.13.17 row above for backfill.

> **v3.13.15 update (Phase J12 — `riseup-git-logs` classifier rules):** Third J-series slot ships per the `.lovable/plan.md` locked design. New slot §42 `42-cli-classifier-rules.md` v1.0.0 carries the deterministic decision-function for the 4-subcommand wrap-set with 6 normative sections: §1 Purpose & Scope; §2 4-outcome closed set (`NORMAL`/`WARN`/`ERROR`/`INTERNAL`) + `ExitCode` mapping per spec/13 AC-21 link-don't-restate; §3 5-step deterministic precedence (forbidden-subcommand → identity-discovery failure → exit-non-zero ERROR-pattern → exit-zero WARN-pattern → fallback NORMAL); §4 per-subcommand rule tables Section A `git log` (LOG-N1/W1/W2 + LOG-E1/E2/E3) / B `git status` (STAT-N1/W1/W2 + STAT-E1/E2) / C `git diff` (DIFF-N1/W1/W2 + DIFF-E1/E2) / D `git show` (split-and-piggyback + SHOW promotion + frame-level severity ordering); §5 forbidden non-determinism (`LANG=C` mandatory; no `~/.gitconfig`/clock/cache reads); §6 classifier versioning (banner version IS classifier version; NDJSON `classifierVersion` per §43; `MAJOR` bump = coordinated upgrade event under follow-up §97 extension AC). New §97 **AC-84** `[critical]` binds all 6 sub-contracts; FORBIDDEN: restating any rule row inline in §40/§41/§43/§44/§45 (those MUST cite by Section + rule-ID anchor); FORBIDDEN: restating spec/13 AC-21 `ExitCode` enum body inside §42; FORBIDDEN: emitting any outcome outside the 4-state closed set; FORBIDDEN: classifying without setting `LANG=C`; FORBIDDEN: suppressing upload of any classified frame at the CLI layer. AC-80 Sibling File Delegation Map gains row 36 for §42. AC count 77 → **78**. Banners: §97 v3.15.0 → **v3.16.0** (minor — new AC + AC-80 row), §00 v3.13.14 → **v3.13.15**, §98 v3.15.0 → **v3.16.0** (mirrors §97 minor + new row), this file v3.13.14 → **v3.13.15** (+ audit row + h10 stamp 154). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump.** **Lesson #19 audit-boundary pin** on the classifier-decision axis (third J-series instance after CLI-binary J10 + wire-format J11). **Lesson #21 intra-module delegation** (AC-80 row 36 same-PR per Lesson #39). **Lesson #36 dual link-don't-restate** to spec/13 AC-21 `ExitCode` enum + spec/22 §15 `GL-CLASSIFIER-*` error codes. **Lesson #34 holds**: J12 is normative-surface-extension work; cache real CRITICAL count tree-wide remains 0 awaiting A8. Verified: lockstep + tree-health gates run post-memo. **Remaining J-series**: J13 (§43 upload protocol NDJSON + AC-85), J14 (§44 auto-fix protocol hybrid 4-state + AC-86), J15 (§45 test plan + AC-87 + final 5-gate sweep).

> **v3.13.14 update (Phase J11 — `.riseup-git-logs.json` pointer-file wire format):** Second J-series slot ships per the `.lovable/plan.md` locked design ("pointer-file = structured JSON `.riseup-git-logs.json` at git root mirroring spec/14 AC-23 wire-format pattern"). New slot §41 `41-cli-pointer-file-schema.md` v1.0.0 carries the verbatim Draft-07 JSON Schema in §3 as the single source of truth; CLI implementations MUST embed it verbatim and validate on every read AND every write. New §97 **AC-83** `[critical]` binds: (1) file location & lifecycle (`<git-root>/.riseup-git-logs.json` UTF-8 LF mode `0644`; atomic temp-then-rename per spec/13 §97 AC-22 link-don't-restate; first-write source `riseup-git-logs init`; CLI MUST NOT mutate `.gitignore`); (2) closed-set 8-field inventory (`schemaVersion` SemVer · `repoIdentityHash` 64-hex sha256 immutable · `repoCanonicalRemote` URL auditability · `currentHead` 40-hex git SHA · `uploadUrl` https · `uploadedAt` RFC 3339 UTC · `expiresAt` RFC 3339 UTC · `autofixDownloadUrl` https or null); `additionalProperties: false`; (3) verbatim Draft-07 schema with `dependencies` clause pairing `uploadUrl`+`uploadedAt`+`expiresAt`; (4) 11-entry forbidden-field list (`token`/`apiKey`/`secret`/`password`/`sshKey`/`privateKey`/`userId`/`userEmail`/`ownerEmail`/`env`/`platform` — last three mirror spec/22 §07 locked-decision-12 per Lesson #36); (5) validation discipline (5-row trigger×action table) + SemVer evolution rules (PATCH/MINOR/MAJOR with MAJOR requiring own §97 extension AC + AC-80 row 35 annotation). AC-80 Sibling File Delegation Map gains row 35 for §41. AC count 76 → **77**. Banners: §97 v3.14.0 → **v3.15.0** (minor — new AC + AC-80 row), §00 v3.13.13 → **v3.13.14**, §98 v3.14.0 → **v3.15.0** (mirrors §97 minor + new row), this file v3.13.13 → **v3.13.14** (+ audit row + inventory row + h10 stamp 154). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump.** **Lesson #19 audit-boundary pin** on the wire-format axis. **Lesson #21 intra-module delegation** (AC-80 row 35 same-PR per Lesson #39). **Lesson #36 link-don't-restate** to spec/13 AC-22 atomic-write + spec/14 AC-23 pattern + spec/05 AC-SD-23 TTL precedent + spec/22 §07 forbidden fields + spec/22 §15 error code. **Lesson #34 holds**: J11 is normative-surface-extension work; cache real CRITICAL count tree-wide remains 0 awaiting A8. Verified: lockstep + tree-health gates run post-memo. **Remaining J-series**: J12 (§42 classifier + AC-84), J13 (§43 upload + AC-85), J14 (§44 auto-fix + AC-86), J15 (§45 test plan + AC-87 + final sweep).

> **v3.13.13 update (Phase J10 — `riseup-git-logs` CLI subsystem entry-point):** User request closed: "Phase J will be CLI" (Git Log CLI binary that wraps git, classifies output, uploads to server, surfaces auto-fix URL via JSON pointer file at git root). New slot §40 `40-cli-overview.md` v1.0.0 ships as the tier-1 entry point for the J-series CLI subsystem (slots §40–§45 / AC-82–AC-87 reserved per Phase J plan). New §97 **AC-82** `[critical]` binds: (1) standalone `riseup-git-logs` binary identity; (2) closed-set wrap scope of 4 read-only git subcommands (`log`/`status`/`diff`/`show`) + 11-entry FORBIDDEN mutating-subcommand list; (3) 5-step deterministic identity-discovery algorithm (`rev-parse --show-toplevel` → read pointer → `rev-parse HEAD` → `repoIdentityHash = sha256(canonical-remote-url)[0:32]` → bind to NDJSON frames); (4) first-run `riseup-git-logs init` bootstrap discipline (no `.gitignore` mutation; atomic temp-then-rename per spec/13 AC-22); (5) 6 cross-module link-don't-restate surfaces (spec/13 §97 AC-21 `ExitCode` enum + AC-22 concurrency + AC-23 subcommand checklist; spec/22 §04 REST + §31 SSH-key Lane B + §39 split-DB). AC-80 Sibling File Delegation Map gains row 34 for §40. AC count 75 → **76**. Banners: §97 v3.13.5 → **v3.14.0** (minor — new AC + AC-80 row), §00 v3.13.12 → **v3.13.13**, §98 v3.13.12 → **v3.13.13** (+ row), this file v3.13.12 → **v3.13.13** (+ audit row + inventory row + h10 stamp 153 → 154). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump.** Auto-fix design choice: **hybrid** (server proposes patch; CLI shows diff; user confirms before write — FORBIDDEN silent apply). Pointer-file format: **structured JSON** at git root (mirrors spec/14 AC-23 update-interface-contract pattern; concrete schema deferred to Phase J11 / AC-83 / slot §41). **Lesson #19 audit-boundary pin** on the new CLI-binary axis (mirror of spec/12 AC-10 + spec/14 AC-23). **Lesson #21 intra-module sibling-file delegation** (AC-80 row added in same PR as file ship per Lesson #39 second-axis discipline). **Lesson #36 link-don't-restate** to spec/13 framework (no exit-code matrix or concurrency strategy restated in spec/22). **Lesson #34 holds**: J10 is normative-surface-extension work; cache real CRITICAL count tree-wide remains 0 awaiting A8 LLM-gateway refresh. Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅; version-parity 77/77 ✅. **Remaining J-series**: J11 (§41 pointer-file JSON Schema + AC-83), J12 (§42 classifier rules + AC-84), J13 (§43 upload protocol + AC-85), J14 (§44 auto-fix protocol + AC-86), J15 (§45 test plan + AC-87 + final 5-gate sweep).

> **v3.13.12 update (Phase E3 — §34 test-skeleton scaffolds binding AC-05 + AC-12 T-IDs):** User request closed: continue spec-only improvements; E3 from queued plan ("author test/AcDedupWindowTest.php + test/ac-dedup-window.bats skeletons under §34" + ingest mirror). Pre-E3 §97 AC-05 (E1) and AC-12 (E2) verifies-clauses both cited `34-phpunit-test-skeleton.md` per AC-80 sibling delegation, but §34 ended at v2.7.0 with NO scaffolds for the T-DEDUP-* or T-INGEST-* matrices — Lesson #36 link-don't-restate bidirectionality broken (§97 → §34 link existed; §34 had no landing surface). Resolution: §34 v2.7.0 → **v2.8.0** with 5 new normative subsections — §34.A `tests/phpunit/Logger/AcDedupWindowTest.php` (11 `markTestIncomplete('T-DEDUP-NN')` methods); §34.B `tests/phpunit/Ingest/AcStreamingIngestTest.php` (11 `T-INGEST-NN` methods); §34.C bats companions (`ac-dedup-window.bats` + `ac-streaming-ingest.bats`) with 22 `skip` stubs; §34.D coverage-gap detection contract (CI must grep both files for every §97 T-ID; missing IDs raise `GL-TEST-COVERAGE-GAP`); §34.E clock-injection contract reference (cites §97 AC-29 §5 by anchor; restating signature in §34 = FORBIDDEN). All scaffolds use `markTestIncomplete()` / `skip` so green CI is preserved. **No new AC** (count 75 → 75); banners patch-only: §34 v2.7.0 → **v2.8.0** (minor — new subsections), §00 v3.13.11 → **v3.13.12**, §98 v3.13.11 → **v3.13.12**, this file v3.13.11 → **v3.13.12**. §97 NOT bumped (AC-05 + AC-12 verifies-clauses already cite §34 by anchor — no contract change). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change.** **Lesson #36 mirror-bidirectionality codified** (§34.D + §34.E): link-don't-restate is bidirectional — if §97 ACs cite §34 by anchor, §34 MUST contain a discoverable landing surface; AC declaration + landing surface ship in same lockstep PR. **Lesson #34 holds**: test-coverage-followability work; cache real CRITICAL count tree-wide remains 0. Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅; version-parity 77/77 ✅.

> **v3.13.11 update (Phase E2 — AC-12 T-INGEST-01..11 scenario matrix + AC-29 §5 ClockInterface contract):** User request closed: continue spec-only improvements; E2 from queued plan ("testable GWT for AC-12 streaming + clock-injection AC-29 subsection"). Pre-E2 AC-12 was a single ~2 KB normative paragraph — no discoverable test-case enumeration; T-DEDUP-* mirror needed on the ingest axis. Pre-E2 AC-29 was a 4-line stub; clock-injection (flagged as queued by E1 changelog row) was unauthored, leaving T-DEDUP-04 + T-INGEST-06/T-INGEST-10 unimplementable as fast unit tests. Resolution: extended AC-12 with an 11-row T-INGEST-01..11 matrix (chunked happy / buffered Content-Length / mid-stream MaxLineBytes breach / MaxLinesPerPush breach / MaxPushPayloadBytes breach / slow-loris timeout / memory-bound / atomic-rollback on validation failure / logger-gating-during-ingest / clock-injection determinism / cross-request cursor isolation). Added AC-29 §5 normative `ClockInterface { now(), nowMs() }` contract — production binds `SystemClock`; tests bind `FakeClock` exposing `advance()` + `set()`. FORBIDDEN: direct `time()`/`microtime()`/`date()`/`DateTimeImmutable("now")`/`gmdate()`/`hrtime()` from `inc/Logger/`, `inc/Ingest/`, `inc/Auth/Ssh/`, `inc/RateLimit/` (4 grep paths); migration-runner exempt (forensic, not behavioural). AC-29 verifies-clause widened to bind AC-05 (T-DEDUP-04) + AC-12 (T-INGEST-06, T-INGEST-10) + AC-64 (`SshKey.RotationDueAt`). **No new AC** (AC count 75 → 75); banners patch-only: §97 v3.13.4 → **v3.13.5**, §00 v3.13.10 → **v3.13.11**, §98 v3.13.10 → **v3.13.11**, this file v3.13.10 → **v3.13.11**. **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change.** **Lesson #19 sixth-axis confirmation on spec/22**: AC-78 + AC-81 + AC-11 + AC-23 + AC-05 + AC-12/AC-29 §5 — six normative-content axes lifted to tier-1. **Lesson #36 reinforcement**: clock-injection contract owned by AC-29 §5; AC-05 + AC-12 + AC-64 cite it by anchor. **Lesson #34 holds**: implementer-discoverability + test-determinism work; cache real CRITICAL count tree-wide remains 0. Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅.
> **v3.13.10 update (Phase E1 — AC-05 testable scenario matrix inline):** User request closed: continue spec-only improvements; E1 from queued plan ("testable GWT for AC-05 dedup window"). Pre-E1 AC-05 was a single 1.6 KB dense paragraph; implementers had to derive scenarios from prose. Resolution: extended AC-05 with an 11-row testable scenario matrix `T-DEDUP-01..11` (T-ID × Scenario × Input × Expected output × Verifies clause) covering rolling-window / burst / error+fatal carve-out / LRU eviction / AC-04 ordering / cross-worker semantics; added test-suite assertion contract binding `test/AcDedupWindowTest.php` (PHPUnit) and `test/ac-dedup-window.bats` (bash harness) — every T-ID MUST appear in test method/case name; absence = `GL-TEST-COVERAGE-GAP`. Time-mocking contract delegates to AC-29 §"Test-time clock injection" (E2 follow-up if not yet authored). **No new AC** (AC count 75 → 75); banners patch-only: §97 v3.13.3 → **v3.13.4**, §00 v3.13.9 → **v3.13.10**, §98 v3.13.9 → **v3.13.10**, this file v3.13.9 → **v3.13.10**. **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change.** **Lesson #19 fifth-axis confirmation on spec/22**: AC-78 (inventory) + AC-81 (enums) + AC-11 (endpoints) + AC-23 (CHECK constraints) + AC-05 (test scenarios) — five normative-content axes lifted to tier-1. **Lesson #36**: test-fixture skeletons in §34 MUST cite by anchor + T-ID, not restate. **Lesson #34**: implementer-discoverability + test-coverage-followability work; cache real CRITICAL count tree-wide remains 0. Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅.

> **v3.13.9 update (Phase D3 — AC-23 CHECK-constraint inventory inline):** User request closed: continue spec-only improvements; D3 from queued plan ("CHECK constraints inline from `18-schema.sql`"). Survey via `grep -nE "CHECK \(" spec/22-git-logs-v2/18-schema.sql` returned 5 hits (4 boolean-domain + 1 row-level exactly-one polymorphic on `AppLink`). Pre-D3 AC-23 only asserted PascalCase + AUTOINCREMENT — SQLite domain enforcement was entirely tier-2 in `18-schema.sql`. Resolution: extended AC-23 with a normative 6-column 5-row CHECK-constraint inventory table (Table × Column × Expression × Kind × `18-schema.sql` line × Drift signal). Cross-language storage convention DELEGATED to spec/04 §2.1 + migration discipline DELEGATED to AC-29 per Lesson #36 (restating in spec/22 = FORBIDDEN). Tri-state NULL exception explicitly noted as not-yet-applicable. **No new AC** (AC count 75 → 75); banners patch-only: §97 v3.13.2 → **v3.13.3**, §00 v3.13.8 → **v3.13.9**, §98 v3.13.8 → **v3.13.9**, this file v3.13.8 → **v3.13.9**. **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change.** **Lesson #19 fourth-axis confirmation on spec/22**: AC-78 (inventory) + AC-81 (enums, C1) + AC-11 (endpoints, D2) + AC-23 (CHECK constraints, D3) — four normative-content axes lifted to tier-1. **Lesson #36 reinforcement**: spec/04 §2.1 owns boolean storage; AC-23 cites it. **Lesson #34 holds**: implementer-discoverability + audit-walker-followability work; cache real CRITICAL count tree-wide remains 0. Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅.

> **v3.13.8 update (Phase D2 — AC-11 per-endpoint binding matrix inline):** User request closed: continue spec-only improvements; D2 from queued plan ("per-endpoint JSON Schema inline contracts in spec/22"). Pre-D2 AC-11 was a 4-line stub citing `§Endpoints, §04, §17` — but `04-rest-api-endpoints.md` (406 lines) and `17-openapi.yaml` (tier-2) are not in the walker tier-1 bundle (per slot 34 v1.1.0 AC-34-09), so an audit walker could not verify per-endpoint shape from §97 alone. Resolution: extended AC-11 with a 10-row normative binding matrix — every endpoint maps to (Method, Path, Request body schema name, Response body schema name, Auth class, Error envelope codes). Full JSON schemas remain canonical in `17-openapi.yaml` per Lesson #36 — restating those schemas inline here is FORBIDDEN. AC-11 verifies-clause widened to cite AC-12, AC-14, AC-30, AC-67–AC-72 (envelope/streaming-contract dependencies). **No new AC** (AC count 75 → 75); banners patch-only: §97 v3.13.1 → **v3.13.2** (AC-11 body extension), §00 v3.13.7 → **v3.13.8** (banner), §98 v3.13.7 → **v3.13.8** (banner + row), this file v3.13.7 → **v3.13.8** (banner + this audit row). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change.** **Lesson #19 third-axis confirmation on spec/22**: AC-78 (module-kind/inventory) + AC-81 (enum-catalog) + AC-11 extension (endpoint-inventory) — three normative-content axes lifted to tier-1. **Lesson #36 reinforcement**: matrix is a binding TABLE (schema names + paths + auth classes), NOT a restatement of OpenAPI content. **Lesson #34 holds**: this is implementer-discoverability + audit-walker-followability work; cache real CRITICAL count tree-wide remains 0. Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅.

> **v3.13.6 update (Phase C1 — AC-81 Enum Catalog promotion):** User request closed: continue spec-only improvements; C1 from queued plan ("spec/22 enum catalog promotion"). Survey of `01-glossary-and-enums.md` (14 KB, tier-2) found 11 active enum types + 1 deprecated stub canonically defined in `18-schema.sql` lookup-table seeds + mirrored in TS `## TypeScript Mirror` block; pre-AC-81 the catalog had ZERO §97 tier-1 binding (only AC-78 evidence row line 505 calling out `[D4] Truncated Glossary` as a walker-cap artifact). Added `[critical]` **AC-81** to §97 with a 13-row normative table — every enum maps to (Codes, Authority, Drift signal). Codes ≤6 listed in full; `Permission` (17), `SystemEventType` (16), `AuditActionType` (19) listed in full with count. AC delegates the Drift-detection precedence (`18-schema.sql` canonical → glossary tables canonical mirror → TS mirror hand-maintained → PHP `cases()` boot-generated) to source file per **Lesson #36**. Forbids restating per-code semantics in any other spec/22 file (`02`/`04`/`19`/`15`); every cite MUST be by enum-name only. **C2 (physical reorder of `01-glossary-and-enums.md`) consciously DEFERRED** — AC-81 provides the audit-walker tier-1 binding that reorder would have provided, while preserving stable line-anchored citations (e.g., AC-78 line 505 evidence row, AC-80 row for `01-glossary-and-enums.md`); reorder would invalidate them and is a content-shuffle with no normative gain post-AC-81. C3 (lockstep) shipped. AC count 74 → **75**. Banners: §97 v3.12.0 → **v3.13.0** (minor — new AC); §00 v3.13.5 → **v3.13.6** (banner — patch); §98 v3.13.5 → **v3.13.6** (banner + row — patch); this file v3.13.5 → **v3.13.6** (banner + this audit row — patch). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump · no `01-glossary-and-enums.md` content change.** **Lesson #19 second-axis confirmation**: AC-78 closes audit-boundary for module-kind/inventory class; AC-81 closes it for enum-catalog class. **Tier-1 normative-surface closure quartet complete for spec/22**: AC-78 + AC-79 + AC-80 + AC-81. Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅.


> **v3.13.4 update (Phase 153 P3 — AC-79 Cross-Module Externalized Citation Map):** User request closed: "Add explicit normative anchors for any cross-module externalized citations in spec/22 so the auditor can follow the dependency chain." Survey via `rg -no "spec/[0-9]+-[a-z-]+" spec/22-git-logs-v2/*.md` surfaced 6 externalized contract dependencies lacking a §97-bound anchor. Added `[critical]` **AC-79** to §97 with a 6-row normative table — each row pins (External cite, Owning module + AC, Cited from spec/22 file, Citation purpose, Restate-in-22 forbidden?). Rows: spec/04 (DDL conventions, cited from 02-database-schema.md L368), spec/12 (CI runner matrix, cited from 05-auth-and-validation.md L114), spec/13 §97 AC-22 (SQLite locking, cited from AC-26 + AC-78 line 505), spec/26 (Mermaid diagrams, cited from 39-split-db-log-storage.md L184–185), spec/05 (split-DB pattern, cited from 39-split-db-log-storage.md L186), linter-scripts/check-spec-folder-refs.py (allowlist enforcement, cited from AC-22-LV1 line 477). Restate-FORBIDDEN by construction (Lesson #36 dual-source drift class). Append-only within a phase. Cross-references mirror of spec/02 AC-CG-21 (Subfolder Delegation Map — same pattern on intra-module sub-folder axis) + spec/12 AC-11 (linter-script anchor pattern from A24-fu4). AC count 73 → **74**. Banners: §97 v3.10.2 → **v3.11.0** (minor — new AC); §00 v3.13.3 → **v3.13.4** (banner — patch); §98 v3.13.3 → **v3.13.4** (banner + row — patch); this file v3.13.3 → **v3.13.4** (banner + this audit row — patch). **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump.** **Lesson #37 second-instance confirmation**: spec/12 (A24-fu4) was the codifying instance for integration-axis modules co-needing Lesson #19 (audit-boundary pin) + Lesson #36 (cross-module anchor) ACs together; spec/22 (this phase) confirms the pattern — AC-78 (Lesson #29 module-kind pin) + AC-79 (Lesson #36 cross-module anchor) ship as a complete pair for the integration-axis class.

> **v3.13.3 update (Phase 153 P1 — AC-78 body inlined to §00 to break harness-saturation ceiling):** Fourth-instance application of the Lesson #65 saturation-class playbook on spec/22 (after fu29 §98 split, fu39 §99 split, fu20 §00 teaser promotion). Diagnosis: 3 prior pin attempts left the auditor still re-flagging the same 3 findings (audit-v9/v10 cache D5/D4/D3) because AC-78's BODY (≈2.7 KB of disposition rules + on-disk evidence triple) lives at §97 line 503/507 — past the 140 KB tier-1 walker cap (tier-1 sums to ~148 KB: §00=14 + §97=73 + §98=45 + §99=16). The §00 teaser table at A24-fu20 surfaces only AC IDs, not the disposition rules — so the auditor sees the pointer but cannot verify the contract. Resolution: inlined the full AC-78 disposition (4-row on-disk evidence triple table + 5 numbered disposition rules + walker tier-1 footprint table) into a new normative `## Walker-Cap Finding Disposition (Normative)` section in §00 immediately under the existing Walker-Pin teaser. Internal §00↔§97 mirror is permitted (Lesson #36 forbids only cross-MODULE restatement; same-module mirroring is required for harness-saturated modules per Lesson #65 — precedent: spec/13 P3 §10/§18 mirror, spec/27 AC-T-34, spec/05 AC-SD-21 walker fix). Banners: §00 v3.13.2 → **v3.13.3**; §98 v3.13.2 → **v3.13.3**; this file v3.13.2 → **v3.13.3**. §97 NOT bumped (AC-78 unchanged — only its body is mirrored to §00). Expected cache lift on next re-score: 87 → 92+. **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump · no new AC.**

> **v3.11.0 update (Phase 153 Task A24-fu20 — AC-78 §00 walker-pin promotion):** Pure walker-visibility patch on the audit-v9 83-floor (walker 3/36 — textbook walker-saturation). Audit-v9 surfaced 3 findings (HIGH/D5 "Missing Core Normative Files", MEDIUM/D4 "Abstract Examples", LOW/D3 "Concurrency Externalized") — ALL diagnosed as cache-staleness artifacts of pre-existing AC-78 contract pin (Lesson #34). AC-78 was authored at A11h (v3.10.0) but lives at §97 line 503+ of 507 — past every walker tier-1 cap. Promoted into a `> 🤖 Walker-Pin (Lesson #55 + Lesson #61)` 3-row teaser table at the §00 head listing AC-78 + AC-22-LV1 + AC-26 cross-ref + 3 forbidden remediation patterns. Walker now sees ALL 3 structural anchors in the first ~2 KB. Banners: §00 v3.10.0 → **v3.11.0**; §98 v3.10.0 → **v3.11.0**; this file v3.10.0 → **v3.11.0**. §97 NOT bumped (no AC change). h10 stamp refreshed 32 → 153. **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no new AC.** All 3 strict gates expected GREEN. **Lesson #61 second-instance confirmation**: spec/22 is the **pure-promotion variant** of A24-fu19's spec/04 codifying instance — lightest-touch close-out for any module where the auditor keeps re-flagging findings already classified by a §97-buried structural-pin AC.

> **v3.10.0 update (Phase 153 Task A11h — AC-78 module asset inventory pin (Lesson #29 + Lesson #36)):** closes audit-v5 D5 HIGH "Missing Core Schema and API Definitions" + D4 MED "Missing Concrete Code Fixtures" + D3 LOW "Concurrency/Race Condition on Rate Limits" as harness bundling-cap artifacts — every cited file present on disk per this report's File Inventory (slots 04, 18, 34 all confirmed); AC-26 persisted-floor concurrency correctly cross-referenced to spec/13 §97 AC-22 per Lesson #36 link-don't-restate; locked-vacant slots `09-13` enforced by AC-22-LV1. Mirror of spec/13 AC-24 + spec/28 AC-28-41 + spec/14 AC-21 + spec/16 AC-21. AC count 78 → 79. §97 v3.9.5 → v3.10.0; §00 v3.9.14 → v3.10.0; §98 v3.9.14 → v3.10.0. No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change · no DDL change · no schema bump.

