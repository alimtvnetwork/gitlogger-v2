---
kind: ac-detail
content_axis: normative-ac-detail-delegation-maps
axis_rationale: "Detailed normative bodies for audit-boundary meta ACs (AC-79/AC-80/AC-81) promoted out of §97-acceptance-criteria.md per Lesson #65 (structural surgery) + Lesson #36 (link-don't-restate). Slot 50 = sibling (01–96), not tier-1 — keeps §97 within walker cap."
---

# §97 AC — Delegation Map Detail (AC-80 + future AC-79/AC-81)

**Version:** 1.0.0
**Updated:** 2026-05-10
**Slot type:** sibling (non-tier1) — bound from `97-acceptance-criteria.md` AC-80 (and future AC-79/AC-81 promotions)
**Promotion rationale:** AC-80 alone was an 18 KB delegation table that pushed `97-acceptance-criteria.md` past the 140 KB walker cap (`linter-scripts/audit-bundle-budget.py` → OVER class). Per **Lesson #65 — structural surgery > pure-promotion**, the table body is moved here; §97 retains a slim binding stub. Same pattern as `49-ac-section-a-detail.md`.

> **Status:** Normative tier-2 (sibling). The detail blocks below are the authoritative normative source for AC-80. Restating the delegation table in §97 or any other sibling is FORBIDDEN per **Lesson #19** (audit-boundary pin). The §97 entry MUST link here by anchor.

---

## AC-80 — Sibling File Delegation Map (DETAIL)  `[critical]`

### AC-80 — Sibling File Delegation Map (Lesson #21 — intra-module audit-boundary pin for 33 sibling files)  `[critical]`

**Given** spec/22-git-logs-v2 contains 33 normative sibling files (`01-*` through `08-*`, `14-*` through `37-*`, `39-*`) outside the tier-1 audit bundle (`{00,97,98,99}-*.md`) — per AC-34-09, the auditor's tier-1 walker (~90 KB cap) does NOT see these files unless their normative content is explicitly bound from §97; AND a binding-density survey (`grep -cE "\bNN-" 97-acceptance-criteria.md` per file, 2026-05-06) found 13 of the 33 sibling files have ZERO §97-bound ACs (`03-admin-ui.md`, `16-seed-data.md`, `19-permission-matrix.md`, `20-observability.md`, `23-backup-restore.md`, `24-multisite.md`, `25-headless-auth-notes.md`, `27-wp-cli-reference.md`, `29-uninstall-policy.md`, `32-cli-test-plan.md`, `36-why-v1-archived.md`, plus 2 marginal cases `06-migrations-and-logger.md` and `07-app-entity.md` with 1 each); AND this is the textbook **Lesson #21 audit-boundary < verification-boundary gap** that AC-CG-21 (spec/02) closes for cross-language sub-folders — applied here to the intra-module sibling-file axis;

**When** an AI auditor walks spec/22 §97 and asks "for each sibling file in this module, which §97 AC governs it?" OR a downstream contributor opens any of the 33 sibling files and asks "what §97 AC must I keep this file consistent with?",

**Then** the table below MUST provide the canonical delegation — every sibling file maps to (a) its content kind, (b) the §97 AC family that governs it, (c) whether the file is auditor-tier-1-visible (bundled) or tier-2 (siblings-cap-bound), (d) restatement-forbidden status per Lesson #36:

| Sibling file | Content kind | Governing §97 AC family | Tier visibility | Restate-in-§97 forbidden? |
|---|---|---|---|---|
| `01-glossary-and-enums.md` | normative-glossary | AC-01..AC-08 (UI/Menu inventory cites enum labels) + AC-22-LV1 (locked-vacant terminology) | tier-2 (14 KB; bundled if budget allows) | **YES** — enum catalog lives in this file; §97 cites enum names, never re-lists them |
| `02-database-schema.md` | normative-schema-narrative | AC-09..AC-15 (schema-driven ACs) + AC-79 row 1 (spec/04 DDL conventions cite) | tier-2 (~28 KB) | **YES** — table DDL narrative lives here; §97 binds table names + invariants, never re-emits CREATE TABLE |
| `03-admin-ui.md` | normative-ui-layout | AC-01..AC-08 (top-level menu) + AC-25 (`format:hide` rendering) + AC-39 (RBAC hide) | tier-2 (no current §97 row by name — governed via AC-25 + AC-39) | **YES** — UI projection rules live here; §97 binds outcome contracts, never re-renders DOM |
| `04-rest-api-endpoints.md` | normative-api-contract | AC-04 (REST endpoint inventory) + AC-79 row 2 (spec/12 CI runner matrix cite) | tier-2 (~30 KB; canonical OpenAPI lives in `17-openapi.yaml`) | **YES** — endpoint signatures live here + `17-openapi.yaml`; §97 binds contract invariants only |
| `05-auth-and-validation.md` | normative-auth-flow | AC-05 (auth lanes A/B/C) + AC-06 + AC-39 (RBAC) + AC-79 row 2 (CI runner matrix) | tier-2 (~21 KB) | **YES** — lane decision tree lives here; §97 binds lane invariants |
| `06-migrations-and-logger.md` | normative-migration-policy | AC-09..AC-12 (schema versioning) + AC-78 (asset inventory pin) | tier-2 (1 §97 reference today — promote to AC-NN if ever expanded) | **YES** — migration script ordering lives here; §97 binds version-bump invariants |
| `07-app-entity.md` | normative-entity-model | AC-22-LV1 (locked decision 12 PERMANENT — App identity envelope) + AC-08 | tier-2 (1 §97 reference today — Locked Decision 12 is the binding contract) | **YES** — App identity envelope is locked in §07; §97 cites the locked decision, never re-derives the envelope |
| `08-history-and-action.md` | normative-audit-trail | AC-08 (History/Action menu items) + AC-26 (rate-limit floor cites History persistence) | tier-2 | **YES** — History/Action schema + retention live here; §97 binds menu + retention contracts |
| `14-endpoint-examples.md` | example-fixtures | AC-04 (REST endpoint inventory cites these examples) + AC-78 (asset inventory pin) | tier-2 (informative; no normative new contracts) | **NO** — examples MAY be restated in §97 if they illustrate an invariant; otherwise leave as fixtures |
| `15-error-codes.md` | normative-error-catalog | AC-15 + AC-error-* family + AC-78 | tier-2 (~15 KB; canonical error catalog) | **YES** — error code numeric ranges + dispositions live here; §97 binds catalog completeness |
| `16-seed-data.md` | normative-bootstrap-fixtures | AC-28 (first-run bootstrap form gating) + AC-78 (asset inventory pin) | tier-2 (no current §97-by-name row — governed via AC-28 bootstrap path) | **YES** — seed values + first-run idempotency live here |
| `17-openapi.yaml` | normative-api-schema (non-md) | AC-04 (REST inventory) + AC-78 (asset inventory pin includes `.yaml`) | tier-2 non-md (canonical OpenAPI; auditor must `cat`-include) | **YES** — OpenAPI is single source of truth for endpoint shapes; §97 binds inventory only |
| `18-schema.sql` | normative-DDL-fixture (non-md) | AC-09..AC-15 + AC-78 (asset inventory pin includes `.sql` fixtures) | tier-2 non-md (canonical DDL; 465 lines) | **YES** — CREATE TABLE statements are canonical here; §97 binds table + column invariants |
| `19-permission-matrix.md` | normative-RBAC-matrix | AC-06 + AC-39 (permission gates) | tier-2 (no current §97-by-name row — governed via AC-06/AC-39) | **YES** — Role × Capability matrix lives here; §97 binds matrix invariants (no role escalation, no capability widening) |
| `20-observability.md` | normative-telemetry-contract | AC-26 (rate-limit floor includes observability counters) + AC-78 | tier-2 (no current §97-by-name row — governed via AC-26 counters) | **YES** — counter names + dimensions live here; §97 binds observable invariants |
| `22-retention-and-pruning.md` | normative-retention-policy | AC-26 (rate-limit + persisted floor) + retention sub-ACs | tier-2 (7 §97 references — well-bound) | **YES** — retention windows + prune algorithm live here |
| `23-backup-restore.md` | normative-DR-procedure | AC-26 (persisted floor implies backup) + AC-78 | tier-2 (no current §97-by-name row — promote to AC-NN if DR becomes a phase target) | **YES** — backup cadence + restore-test schedule live here |
| `24-multisite.md` | normative-multisite-isolation | AC-39 (RBAC) + AC-22-LV1 (App identity scoping per site) + AC-78 | tier-2 (no current §97-by-name row — governed via AC-39 + AC-22-LV1 site-scoping) | **YES** — per-site DB isolation contract lives here |
| `25-headless-auth-notes.md` | normative-headless-auth | AC-05 (auth lanes) + AC-79 row 2 (CI runner matrix cite) | tier-2 (no current §97-by-name row — governed via AC-05 Lane B/C) | **YES** — headless caller patterns live here |
| `26-readme-and-screenshots.md` | informative-distribution-collateral | AC-78 (asset inventory pin) + 4 §97 cites for screenshot contracts | tier-2 (informative; no normative new contracts) | **NO** — README copy MAY be restated in §97 onboarding ACs if any are added |
| `27-wp-cli-reference.md` | normative-CLI-surface | AC-04 (REST inventory mirror via WP-CLI) + AC-78 | tier-2 (no current §97-by-name row — governed via AC-04 surface mirror) | **YES** — WP-CLI command names + flags live here; §97 binds parity-with-REST invariant |
| `28-example-github-actions.md` | example-CI-fixtures | AC-79 row 2 (spec/12 CI runner matrix cite) + AC-78 | tier-2 (informative example; canonical CI lives in spec/12) | **NO** — example MAY be restated in §97 if illustrating a contract |
| `29-uninstall-policy.md` | normative-uninstall-contract | AC-78 (asset inventory pin) — uninstall data-erasure contract | tier-2 (no current §97-by-name row — promote to AC-NN if data-erasure becomes audited) | **YES** — what-is-deleted-on-uninstall list lives here |
| `30-threat-model.md` | normative-security-baseline | AC-05 (auth) + AC-06 + AC-39 (RBAC) + AC-78 | tier-2 (1 §97 reference today — well-anchored via auth + RBAC ACs) | **YES** — STRIDE table + mitigations live here |
| `31-ssh-key-auth.md` | normative-Lane-B-reference | AC-05 (auth lanes — Lane B SSH-key) + AC-78 | tier-2 (1 §97 reference today — well-anchored via Lane B) | **YES** — SSH key generation + rotation contract lives here |
| `32-cli-test-plan.md` | normative-test-plan | AC-78 + AC-04 (REST mirror) | tier-2 (no current §97-by-name row — promote to AC-NN if CLI becomes a separately-graded surface) | **YES** — test matrix lives here; §97 binds coverage targets |
| `33-bats-test-skeleton.md` | normative-test-fixture (bash) | AC-78 (asset inventory pin includes test skeletons) + AC-32 (cli test plan) | tier-2 (canonical Bats skeleton) | **YES** — Bats test structure lives here; §97 binds fixture completeness |
| `34-phpunit-test-skeleton.md` | normative-test-fixture (PHP) | AC-78 (asset inventory pin) + AC-32 | tier-2 (canonical PHPUnit skeleton; 311 lines) | **YES** — PHPUnit class skeleton lives here |
| `35-reference-ci-yml.md` | normative-CI-fixture (yaml) | AC-78 + AC-79 row 2 (CI runner matrix cite) | tier-2 (canonical CI YAML; informative-with-contract status) | **YES** — CI YAML structure lives here; spec/12 owns the runner-matrix contract |
| `36-why-v1-archived.md` | informative-historical-rationale | AC-78 (asset inventory pin) — historical narrative only | tier-2 informative (no normative contract — exempt from delegation per AC-AI-10 quoted-evidence rule) | **NO** — historical narrative; restating is permitted but unnecessary |
| `37-blind-ai-gap-analysis.md` | informative-historical-resolution-narrative | AC-78 + 3 §97 cites for Phase 39b resolution markers | tier-2 informative (TODO markers are quoted historical artifacts per `kind: future-spec` front-matter `todo_audit_exempt: true`) | **NO** — historical resolution narrative; quoted, not actionable |
| `39-split-db-log-storage.md` | normative-split-DB-application | AC-79 rows 4+5 (spec/26 diagrams + spec/05 pattern cites) | tier-2 (~13 KB) | **YES** — split-DB *application* to git-logs lives here; pattern itself lives in spec/05 |
| `40-cli-overview.md` | normative-cli-binary-contract (J-series tier-1 entry) | AC-82 (binary identity + wrap scope + 5-step identity algorithm + cross-refs to spec/13 framework) + AC-78 (asset inventory) | tier-1 (~7 KB) | **YES** — `riseup-git-logs` architecture overview lives here; spec/13 owns the framework contract per Lesson #36 |
| `41-cli-pointer-file-schema.md` | normative-cli-pointer-file-wire-format (J-series tier-1) | AC-83 (JSON Schema Draft-07 + 8-field closed inventory + forbidden-field list + validation discipline + SemVer rules) + AC-78 (asset inventory) | tier-1 (~8 KB) | **YES** — `.riseup-git-logs.json` wire format lives here; spec/13 AC-22 owns atomic-write discipline; spec/14 AC-23 is pattern reference |
| `42-cli-classifier-rules.md` | normative-cli-classifier-rules (J-series tier-1) | AC-84 (4-outcome closed set + deterministic precedence + per-subcommand rule tables A/B/C/D + forbidden non-determinism + classifier versioning) + AC-78 (asset inventory) | tier-1 (~9 KB) | **YES** — per-subcommand classifier rules live here; spec/13 AC-21 owns `ExitCode` enum; spec/22 §15 owns `GL-CLASSIFIER-*` error codes |
| `43-cli-upload-protocol.md` | normative-cli-upload-wire-protocol (J-series tier-1) | AC-85 (NDJSON-over-HTTPS transport + 9-field per-frame envelope closed set + idempotency-key contract + 6-attempt exponential-backoff-with-jitter retry + 6-field server response contract + 7-row error-envelope mapping + forbidden patterns) + AC-78 (asset inventory) | tier-1 (~10 KB) | **YES** — CLI→server NDJSON streaming wire format lives here; spec/22 §04 owns endpoint catalog, §05 owns rate-limit + payload caps, §15 owns `GL-UPLOAD-*` error codes, §31 owns SSH-key Lane B auth, §39 owns split-DB scoping |
| `44-cli-autofix-protocol.md` | normative-cli-autofix-protocol (J-series tier-1) | AC-86 (download request shape + 7-field bundle envelope closed set + 4-state outcome closed set + 7-step preflight strict order + atomic apply discipline + post-apply pointer-file refresh + 11-row `GL-FIX-*` error mapping + forbidden patterns) + AC-78 (asset inventory) | tier-1 (~11 KB) | **YES** — server→CLI fix-bundle apply contract lives here; spec/22 §04 owns endpoint catalog, §15 owns `GL-FIX-*` error codes, §31 owns SSH-key Lane B auth, §41 owns pointer-file shape, §42 owns classifier outcomes, §43 owns upload envelope; spec/13 AC-21 owns `ExitCode`, AC-22 owns atomic write |
| `46-server-upload-frames-endpoint.md` | normative-server-endpoint-receiver (K-series tier-1 entry) | AC-88 (10-step server validation order + v3 base + Lane-B-writes-only + frame→storage mapping per §39 + 6-field response per §43 §6 + forbidden patterns) + AC-78 (asset inventory) | tier-1 (~10 KB) | **YES** — server-side `POST /wp-json/git-logs/v3/upload-frames` receiver lives here; §31 owns Lane B 12-step validation, §39 owns split-DB storage scoping, §43 owns frame envelope + idempotency + retry semantics, §15 owns `GL-UPLOAD-*` / `GL-AUTH-*` error codes — all link-don't-restate per Lesson #36 |
| `47-server-autofix-endpoint.md` | normative-server-endpoint-fix-bundle-producer (K-series tier-1) | AC-89 (11-step server validation + lookup order + v3 base + Lane-B-reads-only + server-issued URL discipline + 7-field bundle envelope per §44 §3 + single-consumption + expiry semantics + 304/410/404 ordering + forbidden patterns) + AC-78 (asset inventory) | tier-1 (~13 KB) | **YES** — server-side `GET /wp-json/git-logs/v3/autofix/{repoIdentityHash}/{frameId}` fix-bundle producer lives here; §31 owns Lane B 12-step, §43 owns `autofixDownloadUrl` issuance, §44 owns 7-field bundle envelope + 4-state outcome + atomic apply, §15 owns `GL-FIX-*` / `GL-AUTH-*` codes, spec/13 AC-22 owns atomic single-consumption transaction — all link-don't-restate per Lesson #36 |
| `48-server-refresh-pointer-endpoint.md` | normative-server-endpoint-pointer-refresh-producer (K-series tier-1) | AC-90 (9-step server validation + lookup order + v3 base + Lane-B-writes-only + 4-field identity envelope + 8-field §41 payload production with server-issued `uploadUrl`/`expiresAt` reset + `autofixDownloadUrl` three-state echo + 10-entry forbidden body-top-level fields frame-smuggle guard + forbidden patterns) + AC-78 (asset inventory) | tier-1 (~14 KB) | **YES** — server-side `POST /wp-json/git-logs/v3/refresh-pointer` pointer-refresh producer lives here; §31 owns Lane B 12-step, §39 owns per-SHA `FixBundle` lookup scoping, §41 owns 8-field pointer-file wire format + JSON Schema + forbidden top-level fields, spec/13 AC-22 owns CLI-side atomic write of the response body, §15 owns `GL-PTR-*` / `GL-AUTH-*` codes — all link-don't-restate per Lesson #36 |
| `45-cli-test-plan.md` | normative-cli-test-plan (J-series tier-1 closure) | AC-87 (two-layer L1+L2 architecture + closed 5-suite matrix T-CLI-IDENT/PTR/CLASSIFY/UPLOAD/AUTOFIX with floors 7/9/11/13/12 + per-suite GWT bound 1:1 to AC-82..AC-86 sub-sections + fixture taxonomy + L1≥85% + L2≥52 + GL-* + ExitCode coverage 100% + spec/12 runner matrix CI binding + 6-row forbidden-patterns) + AC-78 (asset inventory) | tier-1 (~14 KB) | **YES** — `riseup-git-logs` CLI binary test plan lives here; spec/12 §97 AC-10 owns runner matrix, spec/13 §97 AC-21 owns ExitCode, AC-22 owns atomic write, §32 owns WP-CLI/PHP server-side test plan (orthogonal — NOT this file), §33 Bats skeleton is reference L2 harness — all link-don't-restate per Lesson #36 |

**AND** the 11 files marked "no current §97-by-name row" above are GOVERNED-BUT-UNNAMED — the auditor MUST trace them to the listed AC family, NOT report them as orphans; any future expansion that adds a normative contract surface to these files MUST add a §97-by-name AC row in the same phase as the file edit (Lesson #21 audit-boundary == verification-boundary invariant);

**AND** the 4 files marked "informative" (`14-endpoint-examples.md`, `26-readme-and-screenshots.md`, `28-example-github-actions.md`, `36-why-v1-archived.md`, `37-blind-ai-gap-analysis.md`) are EXPLICITLY EXEMPT from §97-by-name binding — they illustrate or narrate, never normate; an auditor finding "informative file lacks §97 AC" against any of these 5 MUST be classified a stale-cache artifact (Lesson #34);

**AND** the table is **append-only within a phase** (rows MAY be added when new sibling files ship, never reworded; the "Governing §97 AC family" cell may be updated when an AC family number changes via §97-WINS evolution but the file's content kind is immutable per Lesson #25);

**AND** any new sibling file added to spec/22 in a future phase MUST add a row to this table BEFORE the file ships — the table IS the intra-module audit-followable dependency chain (mirror of AC-79's cross-module externalized citation map; together AC-79 + AC-80 form the complete audit-boundary closure for spec/22).

- **Verifies:** the intra-module sibling-file delegation contract for spec/22 — every one of the 33 sibling files (`01-*` through `08-*`, `14-*` through `37-*`, `39-*`) has an explicit row in the delegation map above with content-kind + governing-§97-AC-family + tier-visibility + restate-forbidden columns. Closes the **Lesson #21 intra-module audit-boundary < verification-boundary gap** that AC-CG-21 (spec/02) closes for cross-language sub-folders. Mirror-pair with **AC-79** (cross-module externalized citation map) — together AC-79 + AC-80 + AC-78 (module-kind pin) form the **complete tier-1 audit-followability triplet** for any integration-axis module per Lesson #37. Codifies the Lesson #21 pattern at the intra-module sibling-file axis (spec/22's 33 siblings) for future replication on similarly-large modules (spec/13's 28 siblings, spec/02's 16 sub-folders already done by AC-CG-21, spec/04's 14 siblings — candidates for future delegation-map sweeps). Until A8 (LLM-gateway re-score) unblocks, this AC declares any "sibling file orphan" or "missing §97 AC for file NN" finding against the 33 listed files a stale-cache artifact per Lesson #34.


---

## Cross-References

- [§97 — Acceptance Criteria](./97-acceptance-criteria.md) (binding parent — AC-80 stub)
- [§49 — Section A AC detail](./49-ac-section-a-detail.md) (sibling pattern reference)

---

## AC-79 — Cross-Module Externalized Citation Map (DETAIL)  `[critical]`

### AC-79 — Cross-Module Externalized Citation Map (Lesson #36 + Lesson #37 — link-don't-restate anchor table)  `[critical]`

**Given** spec/22-git-logs-v2 is an **integration-axis module** (per Lesson #37: d2≤0.83 + d5≥1.10) whose normative content references contract surfaces owned by other top-level spec modules — concretely (a) `02-database-schema.md` line 368 cites "spec/04-database-conventions" as the engine of its DDL conventions, (b) `05-auth-and-validation.md` line 114 routes Lane B CI authentication callers to "spec/12-cicd-pipeline-workflows/00-overview.md", (c) `39-split-db-log-storage.md` lines 184–186 anchor the split-boundary diagrams + pattern to "spec/26-gitlogs-diagrams/01-er-diagram.mmd" + "spec/26-gitlogs-diagrams/02-domain-design.mmd" + "spec/05-split-db-architecture/00-overview.md", (d) AC-26's persisted rate-limit floor depends on "spec/13 §97 AC-22" (SQLite WAL + busy_timeout + retry+jitter — already cited inline in AC-78), and (e) AC-22-LV1's locked-vacant enforcement is mechanically caught by "linter-scripts/check-spec-folder-refs.py" (a spec/27 slot-02-bound script);

**When** an AI auditor walks spec/22 §97 (the tier-1 contract surface per AC-34-09) and encounters any of these externalized citations OR a downstream contributor needs to follow the dependency chain to verify a normative claim,

**Then** the auditor MUST find the canonical anchor for each external citation in the table below — every row is a Lesson-#36 link-don't-restate boundary (the citation lives ONCE in its owning module's §97; spec/22 cites it but never restates it):

| External cite | Owning module + AC | Cited from spec/22 file | Citation purpose | Restate-in-22 forbidden? |
|---|---|---|---|---|
| spec/04-database-conventions | spec/04 §00 + §02-schema-design.md AC-09 | `02-database-schema.md` L368 (DDL header comment) | Engine selection (SQLite 3.35+) + cross-language boolean storage convention (P48-2) | **YES** — boolean storage table + per-engine DDL conventions live in spec/04 §02 v3.4.1; spec/22 DDL inherits, never restates |
| spec/12-cicd-pipeline-workflows | spec/12 §97 AC-09 (slot-collision pin) + AC-10 (technical-interface) | `05-auth-and-validation.md` L114 (Lane B CI runner guidance) | Runner matrix + secrets schema + permissions for CI callers using Lane B SSH-key auth | **YES** — runner/secrets/env/permissions matrix lives in spec/12 `11-technical-interface.md` (bound by AC-10 per A24-fu4); spec/22 routes callers, never restates |
| spec/13-generic-cli §97 AC-22 | spec/13 §97 AC-22 (concurrency contract) + spec/13/10-database.md (implementer mirror) + spec/13/18-batch-execution.md (concurrency-discipline subsection) | AC-26 (persisted floor) + AC-78 line 505 (already inline) | SQLite locking strategy for persisted rate-limit floor — `journal_mode=WAL`, `busy_timeout=5000`, `BEGIN IMMEDIATE`, retry 3×100ms±25% jitter | **YES** — full PRAGMA + retry contract lives in spec/13 §97 AC-22 + mirrored to spec/13/10-database.md per Phase 153 P3; spec/22 cites the AC, never restates the PRAGMA table |
| spec/26-gitlogs-diagrams (`01-er-diagram.mmd` + `02-domain-design.mmd`) | spec/26 §97 (Mermaid diagram contract) + spec/26 §00 inventory | `39-split-db-log-storage.md` L184–185 (split-boundary visual anchor) + line 265 above (Mermaid companion folder ref) | Visual anchor for the §39 split-DB boundary — diagrams are normative diagrams owned by spec/26, brought to v2.1.0 by Phase 10 | **YES** — Mermaid source-of-truth lives in spec/26; spec/22 references the file paths, never inlines the diagram code |
| spec/05-split-db-architecture | spec/05 §00 + §97 AC-SD-21..23 (Phase 153 A6) | `39-split-db-log-storage.md` L186 (Split-DB pattern reference) | The split-DB *pattern* (separation of operational DB vs log-storage DB) is defined in spec/05; spec/22 §39 *applies* the pattern to the git-logs domain | **YES** — pattern definition + SQL identifier double-quoting + Go struct tags + busy_timeout retry contract live in spec/05 §97 AC-SD-21/22/23; spec/22 §39 cites the pattern, never re-derives it |
| linter-scripts/check-spec-folder-refs.py | spec/27 slot 02 §00 + §97 AC-62-01..04 (allowlist parser contract) | AC-22-LV1 line 477 (locked-vacant enforcement) | Mechanical PR-time gate that catches new slot-09..13 stub creations against the locked-vacant invariant | **YES** — script + allowlist parser contract live in spec/27 slot 02; spec/22 cites the script, never replicates the allowlist parser logic |

**AND** any future contributor authoring new normative content in spec/22 that references a contract surface owned by another top-level module MUST add a row to the table above (citation + owning module/AC + spec/22 file + purpose + restate-forbidden flag) before the new content ships — the table IS the audit-followable dependency chain;

**AND** any auditor finding citing "external dependency unresolved" or "dangling cross-module reference" against any of the 6 rows above MUST be classified as a stale-cache artifact (the anchor exists in the cited owning module's §97 — verify with `rg -n "AC-XX" spec/NN-*/97-acceptance-criteria.md` before flagging);

**AND** the table is **append-only** within a phase (rows MAY be added, never reworded; a row's "Owning module + AC" cell may be updated when the owning AC's number changes via §97-WINS evolution but the citation purpose is immutable per Lesson #25);

**AND** restating any of the linked contracts inline within spec/22 is **FORBIDDEN** (Lesson #36 dual-source drift class) — every row's "Restate-in-22 forbidden?" cell is `YES` by construction; future amendments that drop the FORBIDDEN flag require a new locked-decision in spec/22 §07 + a §99 audit row.

- **Verifies:** the cross-module externalized citation contract for spec/22 — every external dependency (spec/04 + spec/12 + spec/13 + spec/26 + spec/05 + linter-scripts/check-spec-folder-refs.py) has an explicit normative anchor in the table above with owning-AC + cite-from-file + purpose + restate-forbidden columns; closes the audit-followability gap surfaced when an AI walker finds an external cite in spec/22 prose but cannot determine whether the cited contract exists in the owning module without leaving the §97 tier-1 bundle. Codifies **Lesson #36** (link-don't-restate cross-module boundary) + **Lesson #37** (integration-axis modules co-need both Lesson #19 audit-boundary pin AND Lesson #36 cross-module anchor map). Mirror of spec/02 AC-CG-21 (Subfolder Delegation Map — same pattern applied to intra-module sub-folder axis) + spec/12 AC-11 (linter-script anchor pattern from A24-fu4). Until A8 LLM-gateway re-score runs cleanly, this AC declares any cross-module-dangling finding against the 6 listed citations a stale-cache artifact per Lesson #34.

---
