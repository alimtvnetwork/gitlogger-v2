# AC-86 Detail — `riseup-git-logs` CLI Auto-Fix Protocol

> **Sibling-file detail** for `97-acceptance-criteria.md` AC-86. Promoted out of
> §97 per AC-80 sibling-file delegation map to keep §97 within the tier-1 audit
> walker budget cap (Lesson #16 + Lesson #19 + Lesson #34). Body below is
> byte-identical to the previous §97 AC-86 inline body and remains tier-1 binding.
> Future J-series detail promotions (AC-82..AC-85, AC-87) MAY be appended to this
> file or split into sibling `54-ac-j-series-cli-detail.md` per AC-80 row updates.

### AC-86 — `riseup-git-logs` CLI auto-fix protocol (J-series tier-1; Lesson #19 + Lesson #21 + Lesson #36)  `[critical]`

- **Given** a successful upload per AC-85 §6 returns `autofixDownloadUrl` (https or null), and when non-null the CLI MAY download a server-prepared fix bundle and apply it to the working tree under a hybrid 4-state propose-diff-confirm-apply flow with the user kept in the loop;
- **When** an AI auditor or implementer walks spec/22 §97 and seeks the binding contract for the auto-fix protocol — download request, fix-bundle envelope, 4-state outcome enumeration, preflight preconditions, atomic apply, post-apply pointer-file refresh, error envelope,
- **Then** the auditor MUST find the following seven normative sub-contracts pinned in `44-cli-autofix-protocol.md` (delegated by name from this AC, not restated here per Lesson #21 discipline):

  1. **Download request** — `GET <autofixDownloadUrl>` (no rewriting; no query-param mutation); `https`-only (HTTP redirect rejected with `INTERNAL`); SSH-key Lane B auth per spec/22 §31 (link-don't-restate); `Accept: application/vnd.riseup.git-logs.fix+json; v=1`; `Accept-Encoding: gzip`; conditional `If-None-Match: <etag>`; max body 4 MiB compressed / 16 MiB decompressed; 30s connect + 60s body timeout. FORBIDDEN: cross-origin redirects, re-using §43 `Idempotency-Key`, persisting body before validation passes.
  2. **Fix-bundle envelope (closed 7-field set)** — `bundleId` (UUIDv4) · `batchId` (UUIDv4 — MUST equal triggering §43 batch) · `repoIdentityHash` (64-hex sha256) · `targetHead` (40-hex SHA) · `schemaBump` (enum {`patch`,`minor`,`major`}) · `patches` (array of `{path, mode, beforeSha256, afterSha256, unifiedDiff}` — `additionalProperties: false`) · `expiresAt` (RFC 3339 UTC). 17-entry forbidden-field list (mirrors §43 §3 16-entry + `executablePath`).
  3. **4-state outcome (closed set)** — `APPLIED` → `ExitCode.OK`; `REJECTED_USER` → `ExitCode.OK`; `REJECTED_PRECONDITION` → `ExitCode.UserError`; `DEFERRED_NETWORK` → `ExitCode.Internal` (per spec/13 AC-21 link-don't-restate). Every emitted state MUST be uploaded as an §43 frame with `subcommand: "fix"` and `outcome` mapped to `NORMAL`/`WARN`/`ERROR`/`INTERNAL` respectively.
  4. **Preflight preconditions (strict 7-step order)** — (1) `autofixDownloadUrl` freshness · (2) `repoIdentityHash` match · (3) `targetHead` ≡ current HEAD · (4) `expiresAt` future · (5) working tree clean (`git status --porcelain=v2 -z` empty) · (6) per-patch `beforeSha256` match · (7) `schemaBump == "major"` REQUIRES interactive confirm (`--auto-confirm` IGNORED for major; `REJECTED_USER`); first failure short-circuits to `REJECTED_PRECONDITION` + matching `GL-FIX-*` code. Reordering FORBIDDEN. Auto-`git stash` FORBIDDEN.
  5. **Atomic apply discipline** — compute all `(path, afterContent)` tuples in memory FIRST; verify each `afterSha256`; write each via temp-then-rename per spec/13 AC-22 (link-don't-restate) using `<path>.riseup-fix.<bundleId>.tmp` + `fsync` + `rename()`; per-file atomic, not bundle-transactional; mid-bundle rename failure → STOP, emit `DEFERRED_NETWORK` with partially-applied paths, NO automatic rollback. FORBIDDEN: in-place writes, shelling out to `git apply`, auto-mkdir of missing parents, chmod beyond preserving mode.
  6. **Post-apply pointer-file refresh** — on `APPLIED` only: re-derive `currentHead` (belt-and-braces invariant); update `.riseup-git-logs.json` per §41 atomic write; set `uploadedAt` + `expiresAt` + `autofixDownloadUrl: null` (URL consumed, MUST NOT be reused); user MUST manually `git add` + `git commit` (CLI does NOT auto-commit). FORBIDDEN: auto-commit, auto-push, mutating any pointer-file field other than the three listed, running any `git` write command inside the auto-fix flow.
  7. **Error envelope (11-row mapping)** — `GL-FIX-URL-STALE` · `GL-FIX-IDENTITY-MISMATCH` · `GL-FIX-HEAD-DRIFT` · `GL-FIX-EXPIRED` · `GL-FIX-DIRTY-TREE` · `GL-FIX-BEFORE-MISMATCH` · `GL-FIX-AFTER-MISMATCH` · `GL-FIX-PAYLOAD-TOO-LARGE` · `GL-FIX-AUTH-INVALID` · `GL-FIX-SERVER-ERROR` (codes owned by spec/22 §15 — link-don't-restate per Lesson #36).

**AND** the J-series sibling slot §45 ships under **AC-87** (Phase J15 — `45-cli-test-plan.md`, AC-80 row 42 added in this phase);

**AND** any of these patterns is FORBIDDEN: (a) restating any of the 7 sub-contracts inline in §40, §41, §42, §43, or §45 — those files MUST cite §44 by section anchor only; (b) restating spec/13 AC-21 `ExitCode` enum or AC-22 atomic-write discipline inside §44 (Lesson #36 violation); (c) emitting any state outside the 4-state closed set; (d) treating `--auto-confirm` as bypassing `major` schema bumps; (e) auto-committing or auto-pushing applied patches; (f) shelling out to `git apply`;

**AND** any auditor finding citing "missing auto-fix protocol" or "unbound fix-bundle apply contract" against this module MUST be classified per Lesson #34 (cache-staleness): verify by `grep -nE "^### AC-86" spec/22-git-logs-v2/97-acceptance-criteria.md && ls spec/22-git-logs-v2/44-cli-autofix-protocol.md` before flagging.

- **Verifies:** the tier-1 binding contract for the J-series auto-fix-protocol file `44-cli-autofix-protocol.md`. Closes the Lesson #19 audit-boundary < verification-boundary gap that would otherwise leave the server→CLI fix-bundle apply surface unbound from §97 by name. Mirror of AC-82/83/84/85 (J10–J13 precedents). Pairs with AC-80 row 38 (sibling-file delegation map) per Lesson #21 + Lesson #39. Codifies **Lesson #36 octuple link-don't-restate** to spec/22 §04 + §05 + §15 + §31 + §41 + §42 + §43 + spec/13 AC-21/AC-22. Codifies the **user-in-the-loop invariant** (no auto-commit, no auto-push, no `--auto-confirm` for major bumps). Until A8 (LLM-gateway re-score) unblocks, this AC declares any "missing auto-fix protocol" or "[D2] unbound apply contract" finding against `44-cli-autofix-protocol.md` a stale-cache artifact per Lesson #34.
- **Source:** `97-acceptance-criteria.md` (this AC); cross-references `44-cli-autofix-protocol.md` (canonical 10-section auto-fix contract), `43-cli-upload-protocol.md` §6 (server response carrying `autofixDownloadUrl`), `41-cli-pointer-file-schema.md` §2 (pointer-file fields refreshed on `APPLIED`), `42-cli-classifier-rules.md` (outcome enum mapped from 4-state set), `40-cli-overview.md` (5-step identity discovery reused for HEAD re-derivation), spec/22 §04 (endpoint catalog), §15 (`GL-FIX-*` codes), §31 (SSH-key Lane B), spec/13 §97 AC-21 (typed `ExitCode` enum), AC-22 (atomic temp-then-rename), AC-78/AC-79/AC-80/AC-81/AC-82/AC-83/AC-84/AC-85 (tier-1 normative-surface closure nonet — AC-86 extends to a decuplet on the J-series auto-fix axis).

---

## AC-82 — CLI Binary Contract (Full Body)

### AC-82 — `riseup-git-logs` CLI binary contract (J-series tier-1 anchor; Lesson #19 + Lesson #21 + Lesson #36)  `[critical]`

- **Given** a new client-side CLI subsystem `riseup-git-logs` is being introduced under spec/22 across slot range §40–§45 (six normative slots: §40 architecture overview · §41 pointer-file JSON Schema · §42 classifier rules · §43 upload protocol · §44 auto-fix protocol · §45 test plan), where slot §40 (`40-cli-overview.md`) is the **tier-1 entry point** that downstream J-series slots delegate to;
- **When** an AI auditor or implementer walks spec/22 §97 (the tier-1 contract surface per AC-34-09) and seeks the binding contract for the `riseup-git-logs` binary identity, wrap-scope, identity-discovery algorithm, first-run bootstrap discipline, and cross-module framework dependencies,
- **Then** the auditor MUST find the following five normative sub-contracts pinned in `40-cli-overview.md` (delegated by name from this AC, not restated here per Lesson #21 intra-module sibling-file delegation discipline):

  1. **Binary identity** — standalone `riseup-git-logs` executable; NOT a subcommand under another binary; ships with its own §27 `riseup-git-logs --help` reference and own §31-style auth-lane-reuse contract (no new auth lane);
  2. **Wrap scope (closed set)** — exactly four read-only git subcommands: `git log`, `git status`, `git diff`, `git show`; the FORBIDDEN list in §40 §2 enumerates 11 mutating subcommands (`push`, `pull`, `fetch`, `commit`, `reset`, `rebase`, `checkout`, `merge`, `stash`, `clean`, `config --set`) any of which MUST surface as `INTERNAL` per spec/13 §97 AC-21 typed `ExitCode` enum;
  3. **Identity discovery (5-step deterministic algorithm)** — (a) `git rev-parse --show-toplevel` to locate root; (b) read `.riseup-git-logs.json` if present per §41 schema; (c) `git rev-parse HEAD` for `currentHead` (full 40-char SHA); (d) `repoIdentityHash = sha256(<canonical-remote-url>)[0:32]` where canonicalisation = lowercase + strip trailing `.git` + strip trailing `/`; (e) bind `(repoIdentityHash, currentHead)` to every NDJSON frame uploaded per §43;
  4. **First-run bootstrap** — if pointer file absent, refuse upload until `riseup-git-logs init` runs (idempotent); `init` writes minimal pointer with identity fields only; `uploadUrl` / `autofixDownloadUrl` / `uploadedAt` / `expiresAt` populate on first successful upload per §43; the CLI MUST NOT mutate `.gitignore`;
  5. **Cross-references (Lesson #36 link-don't-restate)** — six surfaces are cited by anchor only and **restating them in §40–§45 is a SPEC VIOLATION**: spec/13 §97 AC-21 (typed `ExitCode` enum), spec/13 §97 AC-22 (DB+file concurrency — atomic temp-then-rename for `.riseup-git-logs.json` + PID-locked update file under `~/.local/state/riseup-git-logs/`), spec/13 §97 AC-23 (subcommand checklist binding), spec/22 §04 (REST upload destination), spec/22 §39 (split-DB log storage server-side scoping), spec/22 §31 (SSH-key Lane B auth lane reuse).

**AND** the J-series sibling slots §41–§45 ship in subsequent phases (J11–J15) under their own §97 ACs (AC-83..AC-87 reserved); each new slot MUST add a row to the AC-80 Sibling File Delegation Map in the same phase as the file edit (Lesson #21 audit-boundary == verification-boundary invariant);

**AND** any of these patterns is FORBIDDEN: (a) inlining a "concurrency strategy" or "exit-code matrix" subsection into §40 (Lesson #36 violation — those are spec/13 §97 surfaces); (b) extending wrap scope beyond the 4-subcommand closed set without a new §97 AC + §40 §2 update in the same PR; (c) writing `.riseup-git-logs.json` BEFORE the 5-step identity algorithm completes step 4 (atomicity violation per spec/13 AC-22); (d) auto-modifying `.gitignore` from the CLI; (e) restating any spec/13 framework AC body inside spec/22 §40–§45;

**AND** any auditor finding citing "missing CLI binary contract" or "unbound `riseup-git-logs` interface" against this module MUST be classified per Lesson #34 (cache-staleness): verify by `grep -nE "^### AC-82" spec/22-git-logs-v2/97-acceptance-criteria.md && ls spec/22-git-logs-v2/40-cli-overview.md` before flagging.

- **Verifies:** the tier-1 binding contract for the J-series CLI subsystem entry-point file `40-cli-overview.md`. Closes the Lesson #19 audit-boundary < verification-boundary gap that would otherwise leave `40-cli-overview.md` (a new normative interface-contract file) unbound from §97 by name. Mirror of **spec/12 AC-10** (interface-contract binding pattern: `11-technical-interface.md` bound from spec/12 §97 by name with explicit GWT covering all normative subsections — Phase 153 Task A24-fu4 precedent) and of **spec/14 AC-23** (update-interface-contract pattern — Phase J8 precedent). Pairs with AC-80's new row 34 (sibling-file delegation map) and AC-79's external-citation map (rows for spec/13 framework citations). Codifies **Lesson #21 intra-module axis** (delegation-map row + governing AC ship in the same PR per Lesson #39 second-axis instance) and **Lesson #36** (cross-module concerns to spec/13 are linked, never restated). Until A8 (LLM-gateway re-score) unblocks, this AC declares any "missing CLI overview" or "[D2] unbound interface-contract" finding against `40-cli-overview.md` a stale-cache artifact per Lesson #34.
- **Source:** `97-acceptance-criteria.md` (this AC); cross-references `40-cli-overview.md` (canonical architecture overview), spec/13 §97 AC-21 (typed `ExitCode` enum), spec/13 §97 AC-22 (DB+file concurrency), spec/13 §97 AC-23 (subcommand checklist), spec/22 §04 (REST endpoints), spec/22 §31 (SSH-key Lane B), spec/22 §39 (split-DB log storage), AC-78/AC-79/AC-80/AC-81 (tier-1 normative-surface closure quartet — AC-82 extends to a quintet on the J-series CLI axis).

---

## AC-83 — Pointer-File Wire Format (Full Body)

### AC-83 — `.riseup-git-logs.json` pointer-file wire format (J-series tier-1; Lesson #19 + Lesson #21 + Lesson #36)  `[critical]`

- **Given** the `riseup-git-logs` CLI (per AC-82) writes a JSON pointer file at `<git-root>/.riseup-git-logs.json` to record repo identity, current head, server upload endpoint, expiry, and auto-fix download URL across invocations;
- **When** an AI auditor or implementer walks spec/22 §97 and seeks the binding contract for the pointer-file wire format — schema shape, field semantics, validation discipline, forbidden fields, SemVer evolution rules,
- **Then** the auditor MUST find the following five normative sub-contracts pinned in `41-cli-pointer-file-schema.md` (delegated by name from this AC, not restated here per Lesson #21 intra-module sibling-file delegation discipline):

  1. **File location & lifecycle** — single file at `<git-root>/.riseup-git-logs.json`; UTF-8 LF mode `0644`; atomic temp-then-rename per spec/13 §97 AC-22 (link-don't-restate); first-write source `riseup-git-logs init`; subsequent writes from §43 upload-protocol response handler; CLI MUST NOT mutate `.gitignore`;
  2. **Field inventory (closed set of 8)** — `schemaVersion` (SemVer string, required) · `repoIdentityHash` (64-hex sha256, required, immutable) · `repoCanonicalRemote` (URL, required, auditability only) · `currentHead` (40-hex git SHA, required) · `uploadUrl` (https URL, optional) · `uploadedAt` (RFC 3339 UTC, optional, paired with `uploadUrl`) · `expiresAt` (RFC 3339 UTC, optional, paired with `uploadUrl`) · `autofixDownloadUrl` (https URL or null, optional). `additionalProperties: false`. Adding a 9th field requires a follow-up §97 extension AC.
  3. **JSON Schema Draft-07** — `41-cli-pointer-file-schema.md` §3 carries the verbatim Draft-07 schema as the single source of truth; CLI implementations MUST embed it verbatim and validate on every read AND every write; divergence between embedded schema and §41 §3 = SPEC VIOLATION;
  4. **Forbidden fields** — `token`, `apiKey`, `secret`, `password`, `sshKey`, `privateKey`, `userId`, `userEmail`, `ownerEmail`, `env`, `platform` (last three mirror spec/22 §07 locked-decision-12 forbidden set per Lesson #36); presence MUST trigger `INTERNAL` exit + `GL-POINTER-FORBIDDEN-FIELD` per spec/22 §15;
  5. **Validation discipline & SemVer rules** — per §41 §5 trigger×action table (read at startup → first-run bootstrap on violation; write before atomic-rename → `INTERNAL`; `MAJOR` schema-version mismatch → refuse upload until `init --force`; `repoIdentityHash` mismatch → refuse upload); per §41 §6 SemVer rules (PATCH = clarifications; MINOR = optional field add; MAJOR = required field, tightened pattern, removed/renamed field, or new forbidden-field) — every MAJOR bump MUST ship its own §97 extension AC + AC-80 row 35 annotation.

**AND** the J-series sibling slots §42–§45 ship in subsequent phases (J12–J15) under their own §97 ACs (AC-84..AC-87 reserved); each new slot MUST add a row to AC-80 Sibling File Delegation Map in the same phase as the file edit (Lesson #21 invariant);

**AND** any of these patterns is FORBIDDEN: (a) restating the JSON Schema body inline in any other spec/22 file (§40, §42, §43, §44, §45) — those files MUST cite §41 §3 by anchor only; (b) writing the pointer file without prior schema validation; (c) adding a 9th top-level field without a §97 extension AC + §41 §2 update + AC-80 row 35 update in the same PR; (d) using `0600` mode (the file carries no secrets — `0600` would be a misleading hardening signal); (e) restating spec/13 AC-22 atomic-write discipline inside §41 (Lesson #36 violation);

**AND** any auditor finding citing "missing pointer-file schema" or "unbound `.riseup-git-logs.json` wire format" against this module MUST be classified per Lesson #34 (cache-staleness): verify by `grep -nE "^### AC-83" spec/22-git-logs-v2/97-acceptance-criteria.md && ls spec/22-git-logs-v2/41-cli-pointer-file-schema.md` before flagging.

- **Verifies:** the tier-1 binding contract for the J-series wire-format file `41-cli-pointer-file-schema.md`. Closes the Lesson #19 audit-boundary < verification-boundary gap that would otherwise leave the pointer-file JSON Schema (a new normative interface-contract surface) unbound from §97 by name. Mirror of **AC-82** (J-series CLI-binary contract — Phase J10 precedent) and of **spec/14 AC-23** (update-interface-contract pattern — Phase J8 precedent for atomic-pointer-file shape). Pairs with AC-80's new row 35 (sibling-file delegation map) per Lesson #21 intra-module axis + Lesson #39 second-axis instance. Codifies **Lesson #36** (atomic-write discipline owned by spec/13 AC-22; pattern reference owned by spec/14 AC-23 — both linked, never restated). Until A8 (LLM-gateway re-score) unblocks, this AC declares any "missing pointer-file schema" or "[D2] unbound wire-format" finding against `41-cli-pointer-file-schema.md` a stale-cache artifact per Lesson #34.
- **Source:** `97-acceptance-criteria.md` (this AC); cross-references `41-cli-pointer-file-schema.md` (canonical schema + field inventory + validation discipline), `40-cli-overview.md` §3–§4 (5-step identity algorithm + first-run bootstrap that consume these fields), spec/13 §97 AC-22 (atomic write discipline — link-don't-restate), spec/14 §97 AC-23 (pattern reference: update-interface-contract pointer-file shape), spec/05 §97 AC-SD-23 (TTL/`ExpiresAt` precedent for `expiresAt` semantics), spec/22 §07 (locked-decision-12 forbidden fields source), spec/22 §15 (`GL-POINTER-FORBIDDEN-FIELD` error code), AC-78/AC-79/AC-80/AC-81/AC-82 (tier-1 normative-surface closure sextet — AC-83 extends to a septet on the J-series wire-format axis).

---

## AC-84 — Classifier Rules (Full Body)

### AC-84 — `riseup-git-logs` CLI classifier rules (J-series tier-1; Lesson #19 + Lesson #21 + Lesson #36)  `[critical]`

- **Given** the `riseup-git-logs` CLI (per AC-82) wraps a closed set of 4 git subcommands (`log`, `status`, `diff`, `show`) and MUST emit a structured `ClassifierOutcome` per output frame to drive both shell exit code (per spec/13 AC-21) and uploaded NDJSON outcome (per AC-85, J13);
- **When** an AI auditor or implementer walks spec/22 §97 and seeks the binding contract for the classifier — outcome state set, deterministic precedence, per-subcommand rule tables, forbidden non-determinism, classifier versioning,
- **Then** the auditor MUST find the following six normative sub-contracts pinned in `42-cli-classifier-rules.md` (delegated by name from this AC, not restated here per Lesson #21 intra-module sibling-file delegation discipline):

  1. **Outcome state set (closed set of 4)** — `NORMAL` / `WARN` / `ERROR` / `INTERNAL`; each maps to a typed `ExitCode` enum value owned by spec/13 §97 AC-21 (link-don't-restate per Lesson #36); every frame MUST be uploaded (server-side scoping decides retention, NOT the CLI); `INTERNAL` frames carry `redact: true`. Adding a fifth outcome name = SPEC VIOLATION.
  2. **Deterministic precedence (5-step strict order)** — forbidden-subcommand check → identity-discovery failure → exit-code-non-zero ERROR-pattern lookup → exit-code-zero WARN-pattern lookup → fallback NORMAL; non-deterministic tiebreakers (random/clock/locale/env beyond canonical `GIT_*`) FORBIDDEN; classifier MUST be byte-deterministic given identical input + identical classifier version.
  3. **Per-subcommand rule tables A/B/C/D** — Section A `git log` (3 NORMAL/WARN + 3 ERROR rules), Section B `git status` (3 + 2 rules), Section C `git diff` (3 + 2 rules), Section D `git show` (split-at-`diff --git` + Section A/C piggyback + 4 promotion rules). Adding a row = §97 extension AC + `MINOR` slot bump; removing/rewording a row = `MAJOR` bump + extension AC.
  4. **Forbidden non-determinism patterns** — no `~/.gitconfig` reads; no system-clock reads (`uploadedAt` set in §43); no cross-invocation cache; MUST set `LANG=C` for every wrapped invocation; no outcome outside the 4-state set; no skipping outcome emission.
  5. **Classifier versioning** — slot banner version IS the classifier version; NDJSON frames per §43 MUST carry `classifierVersion` matching the banner; server-side mismatch is a soft signal (logged, not rejected); `MAJOR` bump = coordinated upgrade event under follow-up §97 extension AC.
  6. **Frame-level severity ordering** — `NORMAL < WARN < ERROR < INTERNAL`; `git show` frame outcome is the highest-severity of the two split halves; this ordering is the authoritative comparator for any future cross-frame aggregation.

**AND** the J-series sibling slots §43–§45 ship in subsequent phases (J13–J15) under their own §97 ACs (AC-85..AC-87 reserved); each new slot MUST add a row to AC-80 Sibling File Delegation Map in the same phase as the file edit (Lesson #21 invariant);

**AND** any of these patterns is FORBIDDEN: (a) restating any per-subcommand rule row inline in spec/22 §40, §41, §43, §44, or §45 — those files MUST cite §42 Section A/B/C/D rule IDs by anchor only; (b) restating spec/13 AC-21 `ExitCode` enum body inside §42 (Lesson #36 violation); (c) emitting any outcome name outside the 4-state closed set; (d) classifying without setting `LANG=C` first; (e) suppressing upload of any classified frame at the CLI layer (server decides retention, NOT the CLI);

**AND** any auditor finding citing "missing classifier contract" or "unbound `git log/status/diff/show` outcome rules" against this module MUST be classified per Lesson #34 (cache-staleness): verify by `grep -nE "^### AC-84" spec/22-git-logs-v2/97-acceptance-criteria.md && ls spec/22-git-logs-v2/42-cli-classifier-rules.md` before flagging.

- **Verifies:** the tier-1 binding contract for the J-series classifier-rules file `42-cli-classifier-rules.md`. Closes the Lesson #19 audit-boundary < verification-boundary gap that would otherwise leave the per-subcommand classifier surface (a new normative decision-function file) unbound from §97 by name. Mirror of **AC-82** (J-series CLI-binary contract — Phase J10 precedent) and **AC-83** (pointer-file wire format — Phase J11 precedent). Pairs with AC-80's new row 36 (sibling-file delegation map) per Lesson #21 intra-module axis + Lesson #39 second-axis instance. Codifies **Lesson #36** (`ExitCode` enum owned by spec/13 AC-21; `GL-CLASSIFIER-*` error codes owned by spec/22 §15 — both linked, never restated). Until A8 (LLM-gateway re-score) unblocks, this AC declares any "missing classifier rules" or "[D2] unbound decision-function" finding against `42-cli-classifier-rules.md` a stale-cache artifact per Lesson #34.
- **Source:** `97-acceptance-criteria.md` (this AC); cross-references `42-cli-classifier-rules.md` (canonical 4-outcome closed set + 5-step precedence + Section A/B/C/D rule tables + forbidden non-determinism + classifier versioning), `40-cli-overview.md` §2 (closed wrap-set the classifier consumes) + §3 step 1 (identity-discovery failure → `INTERNAL` precedence rule 2), `41-cli-pointer-file-schema.md` (pointer file consumed independently — no classifier coupling), spec/13 §97 AC-21 (typed `ExitCode` enum — link-don't-restate), spec/22 §15 (`GL-CLASSIFIER-*` error codes — owning surface), AC-78/AC-79/AC-80/AC-81/AC-82/AC-83 (tier-1 normative-surface closure septet — AC-84 extends to an octet on the J-series classifier axis).

---

## AC-85 — Upload Protocol (Full Body)

### AC-85 — `riseup-git-logs` CLI upload protocol (J-series tier-1; Lesson #19 + Lesson #21 + Lesson #36)  `[critical]`

- **Given** the `riseup-git-logs` CLI (per AC-82) classifies frames per AC-84 and MUST stream them to the spec/22 plugin via a deterministic NDJSON-over-HTTPS wire protocol with explicit idempotency, retry, and partial-progress resumption;
- **When** an AI auditor or implementer walks spec/22 §97 and seeks the binding contract for the upload protocol — transport shape, per-frame envelope, idempotency key, retry/backoff, server response, error envelope,
- **Then** the auditor MUST find the following seven normative sub-contracts pinned in `43-cli-upload-protocol.md` (delegated by name from this AC, not restated here per Lesson #21 intra-module sibling-file delegation discipline):

  1. **Transport** — POST `/wp-json/git-logs/v1/upload` (canonical path owned by spec/22 §04 per Lesson #36 link-don't-restate); `https`-only; SSH-key Lane B auth per spec/22 §31 (link-don't-restate); `Content-Type: application/x-ndjson` LF-terminated; `Content-Encoding: gzip` REQUIRED for bodies > 4 KiB; `application/json` response. FORBIDDEN: multipart, chunked + `Content-Length` co-presence, top-level JSON arrays.
  2. **Per-frame envelope (closed 9-field set)** — `frameId` (UUIDv4) · `repoIdentityHash` (64-hex sha256, lowercase) · `currentHead` (40-hex git SHA) · `subcommand` (enum {`log`,`status`,`diff`,`show`}) · `outcome` (enum per AC-84) · `classifierVersion` (SemVer) · `payload` (string or null) · `emittedAt` (RFC 3339 UTC) · `redact` (boolean). `additionalProperties: false`. 16-entry forbidden-field list (mirrors §41 §2 11-entry list + 5 host-fingerprinting fields `clientIp`/`hostname`/`cwd`/`username`/`homeDir`).
  3. **Idempotency** — `Idempotency-Key: <batchId>` UUIDv4 generated **per CLI invocation, not per frame**; first request with key → process + cache; same key + same body sha256 → return cached response; same key + different body sha256 → 409 + `GL-UPLOAD-IDEMPOTENCY-CONFLICT`; 24h server retention; frame-level `frameId` dedup is belt-and-braces.
  4. **Retry & Backoff** — NO retry on 4xx contract violations; 6-attempt exponential-backoff-with-full-jitter on 5xx/network: base 1s/2s/4s/8s/16s × cap 1.5/3/6/12/24× with jitter `delay = base * (0.5 + cryptoRand())`; cryptographically-random PRNG mandatory (`math/rand` FORBIDDEN); identical body + identical Idempotency-Key on retry; re-deriving frames between attempts FORBIDDEN; after attempt 6 give up + emit `INTERNAL` + leave pointer-file fields UNTOUCHED per §41.
  5. **Server response (200 OK, closed 6-field set)** — `batchId` (echoes Idempotency-Key) · `framesReceived` (int ≥ 0) · `framesDeduplicated` (int ≥ 0) · `uploadedAt` (RFC 3339 UTC, server-authoritative) · `expiresAt` (RFC 3339 UTC, server-authoritative TTL mirroring spec/05 AC-SD-23) · `autofixDownloadUrl` (https or null; null iff batch had 0 ERROR outcomes). These flow back into `41-cli-pointer-file-schema.md` per AC-83.
  6. **Error envelope (7-row mapping)** — 400 `GL-UPLOAD-MALFORMED-NDJSON` · 400 `GL-UPLOAD-FORBIDDEN-FIELD` · 401 `GL-UPLOAD-AUTH-INVALID` · 409 `GL-UPLOAD-IDEMPOTENCY-CONFLICT` · 413 `GL-UPLOAD-PAYLOAD-TOO-LARGE` · 429 `GL-UPLOAD-RATE-LIMITED` · 5xx `GL-UPLOAD-SERVER-ERROR`. Codes are owned by spec/22 §15 (link-don't-restate per Lesson #36).
  7. **Forbidden patterns** — in-memory full-batch buffering (CLI MUST stream); frame re-derivation between retries; trusting CLI-supplied `emittedAt` for server-side ordering (server uses its own `uploadedAt`); sending pointer-file fields in the request body; restating spec/22 §04/§05/§15/§31/§39 inside §43.

**AND** the J-series sibling slots §44–§45 ship in subsequent phases (J14–J15) under their own §97 ACs (AC-86..AC-87 reserved); each new slot MUST add a row to AC-80 Sibling File Delegation Map in the same phase as the file edit (Lesson #21 invariant);

**AND** any auditor finding citing "missing upload protocol" or "unbound NDJSON wire format" against this module MUST be classified per Lesson #34 (cache-staleness): verify by `grep -nE "^### AC-85" spec/22-git-logs-v2/97-acceptance-criteria.md && ls spec/22-git-logs-v2/43-cli-upload-protocol.md` before flagging.

- **Verifies:** the tier-1 binding contract for the J-series upload-protocol file `43-cli-upload-protocol.md`. Closes the Lesson #19 audit-boundary < verification-boundary gap that would otherwise leave the CLI→server transport surface unbound from §97 by name. Mirror of AC-82 (CLI-binary, J10), AC-83 (pointer-file, J11), AC-84 (classifier, J12). Pairs with AC-80 row 37. Codifies **Lesson #36 quintuple link-don't-restate** to spec/22 §04 + §05 + §15 + §31 + §39. Until A8 (LLM-gateway re-score) unblocks, this AC declares any "missing upload protocol" or "[D2] unbound transport contract" finding against `43-cli-upload-protocol.md` a stale-cache artifact per Lesson #34.
- **Source:** `97-acceptance-criteria.md` (this AC); cross-references `43-cli-upload-protocol.md` (canonical 7-section transport + envelope + idempotency + retry + response + errors + forbidden patterns), `40-cli-overview.md` §2 (closed wrap-set producing the frames), `42-cli-classifier-rules.md` (outcome enum), `41-cli-pointer-file-schema.md` (pointer-file fields the response refreshes), spec/22 §04 (endpoint catalog), §05 (payload caps + rate limits), §15 (`GL-UPLOAD-*` codes), §31 (SSH-key Lane B), §39 (split-DB scoping), AC-78/AC-79/AC-80/AC-81/AC-82/AC-83/AC-84 (tier-1 normative-surface closure octet — AC-85 extends to a nonet on the J-series transport axis).

---

## AC-87 — Test Plan (Full Body)

### AC-87 — `riseup-git-logs` CLI test plan (J-series tier-1 closure; Lesson #19 + Lesson #21 + Lesson #36)  `[critical]`

- **Given** the J-series CLI subsystem ships five tier-1 contract surfaces (§40 binary identity / §41 pointer-file / §42 classifier / §43 upload / §44 auto-fix) bound by AC-82..AC-86, and these surfaces require a **closed test-coverage contract** so that any AI implementer can verify behavioural conformance from the spec alone — without re-deriving test cases from prose;
- **When** an AI auditor or implementer walks spec/22 §97 and seeks the binding contract for J-series test coverage — test layer architecture, the closed 5-suite test matrix (T-CLI-IDENT, T-CLI-PTR, T-CLI-CLASSIFY, T-CLI-UPLOAD, T-CLI-AUTOFIX), per-suite Given/When/Then enumerations, fixture taxonomy, coverage targets, CI binding,
- **Then** the auditor MUST find the following six normative sub-contracts pinned in `45-cli-test-plan.md` (delegated by name from this AC, not restated here per Lesson #21 discipline):

  1. **Two-layer architecture (§2)** — L1 (Go `testing` + `httptest`, ≤30s per push) owns per-package contract assertions; L2 (Bats + stub `git-logs-server` mock, ≤90s per push) invokes the built binary and asserts exit codes per spec/13 AC-21, NDJSON wire shapes, atomic on-disk pointer writes per spec/13 AC-22 (link-don't-restate). Property/fuzz tests deferred to spec/13 framework — NOT duplicated.
  2. **Closed 5-suite matrix (§3)** — `T-CLI-IDENT` (binds AC-82, ≥7 cases) · `T-CLI-PTR` (binds AC-83, ≥9 cases) · `T-CLI-CLASSIFY` (binds AC-84, ≥11 cases) · `T-CLI-UPLOAD` (binds AC-85, ≥13 cases) · `T-CLI-AUTOFIX` (binds AC-86, ≥12 cases). Closed set — additions require AC-87 amendment + AC-80 row update in the same phase (Lesson #21 invariant). Min cases are floors; implementations MUST NOT remove.
  3. **Per-suite GWT enumerations (§4.1–§4.5)** — every suite carries a Given/When/Then table where each row binds 1:1 to a sub-section of its owning §97 AC (e.g. `PTR-05` → §41 §3 lexicographic key order; `AUTOFIX-09` → §44 §5 mid-bundle rename failure → `DEFERRED_NETWORK`). Implementers MAY rename test functions but MUST preserve the case-ID → AC sub-section mapping.
  4. **Fixture taxonomy (§5)** — mock `git-logs-server` Go test binary co-located under `cli/testdata/mock-server/`; sample pointer files (8 valid + 11 forbidden-field variants per §41 §4); sample upload batches; sample fix bundles. §45 does NOT introduce a new fixture skeleton file — fixtures live with the Go cli source per Go convention; §33 (Bats) + §34 (PHPUnit) skeletons remain WP-CLI/PHP-bound (link-don't-restate).
  5. **Coverage targets (§6)** — L1 line coverage ≥ 85 % on `pkg/{identity,pointer,classifier,upload,autofix}`; L2 case count ≥ 52 (sum of §3 floors); every `GL-*` error code cited in §40–§44 has at least one negative-path test (100 %); every `ExitCode` enum value has at least one positive test (100 %). CI binding cites spec/12 §97 AC-10 runner matrix row 1 (`ubuntu-latest` runs both layers) and row 2 (`macos-latest` L1 only — accepted Bats `flock` exception).
  6. **Forbidden patterns (§7)** — restating §41 8-field shape / §43 9-field envelope / §44 7-field bundle / §42 4-outcome set inline (Lesson #36); restating spec/13 AC-21 ExitCode / AC-22 atomic-write / spec/12 AC-10 runner matrix; adding a 6th tier-1 suite without AC-87 amendment; removing any Min cases floor; hard-coding mock-server port; coupling L1 + L2 fixtures.

**AND** AC-87 is the **J-series tier-1 closure AC** — with §40–§45 all bound from §97, the J-series sextet is contract-complete; no further reservations exist for AC-NN binding to a §4N J-series file (the J-series numeric range terminates at §45 / AC-87 by design, mirror of K-series §46–§49 / AC-88..AC-91);

**AND** any of these patterns is FORBIDDEN: (a) restating any of the 6 sub-contracts inline in §40, §41, §42, §43, §44, or §32 — those files MUST cite §45 by section anchor only; (b) restating spec/13 AC-21 ExitCode or AC-22 atomic-write inside §45 (Lesson #36 violation); (c) adding a 6th tier-1 suite to the closed matrix without AC-87 amendment + AC-80 row update; (d) merging §45 (CLI-binary client-side) and §32 (WP-CLI/PHP server-side) into a single test-plan file — they own orthogonal surfaces; (e) decreasing any Min cases floor;

**AND** any auditor finding citing "missing CLI test plan" or "unbound test-coverage contract for §40–§44" against this module MUST be classified per Lesson #34 (cache-staleness): verify by `grep -nE "^### AC-87" spec/22-git-logs-v2/97-acceptance-criteria.md && ls spec/22-git-logs-v2/45-cli-test-plan.md` before flagging.

- **Verifies:** the tier-1 binding contract for the J-series test-plan file `45-cli-test-plan.md`. Closes the Lesson #19 audit-boundary < verification-boundary gap that would otherwise leave the J-series test-coverage contract surface unbound from §97 by name. **J-series tier-1 closure** — sixth and final J-series binding (AC-82..AC-87 sextet now complete); pairs with AC-80 row 42 per Lesson #21 + Lesson #39. Codifies **Lesson #36 septuple link-don't-restate** to spec/22 §32 + §33 + §41 + §42 + §43 + §44 + spec/13 AC-21/AC-22 + spec/12 AC-10. Closes inherited backlog item **J15** (memory-trail "spec/22 §45 test-plan T-CLI-* + AC-87" tracked across Phase 154 + R-series sessions). Until A8 (LLM-gateway re-score) unblocks, this AC declares any "missing CLI test plan" or "[D2] unbound test-coverage" finding against `45-cli-test-plan.md` a stale-cache artifact per Lesson #34.
- **Source:** `97-acceptance-criteria.md` (this AC); cross-references `45-cli-test-plan.md` (canonical 7-section test-plan contract), `40-cli-overview.md` (binary identity surface under test), `41-cli-pointer-file-schema.md` (pointer wire format under test), `42-cli-classifier-rules.md` (classifier outcomes under test), `43-cli-upload-protocol.md` (upload protocol under test), `44-cli-autofix-protocol.md` (auto-fix protocol under test), `32-cli-test-plan.md` (sibling WP-CLI/PHP test plan — explicitly orthogonal), `33-bats-test-skeleton.md` (reference L2 harness), spec/12 §97 AC-10 (CI runner matrix), spec/13 §97 AC-21 (ExitCode), AC-22 (atomic write), AC-78/AC-79/AC-80/AC-81/AC-82/AC-83/AC-84/AC-85/AC-86 (tier-1 normative-surface closure decuplet — AC-87 extends to an undecuplet on the J-series test-plan axis and closes the J-series sextet).
