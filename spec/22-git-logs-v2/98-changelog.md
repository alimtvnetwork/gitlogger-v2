# Changelog (v2)

**Updated:** 2026-05-11 (Sess-69 B-15 — auditor-pin extraction into `00-auditor-notes.md`)
**Version:** 3.27.0

### 3.27.0 — 2026-05-11 — Sess-69 B-15: auditor-pin extraction (Lesson #36 intra-module link-don't-restate)
- **Action**: Created new file `00-auditor-notes.md` (~6 KB / ~110 lines) at the top of §22. Extracts from `00-overview.md`: (1) Raw-LLM Auditor Pin block (`mem://` resolution + cross-cohort gate map), (2) Walker-Pin §97-tail surfacing table (AC-78 / AC-22-LV1 / AC-26 walker-cap classification), (3) Walker-Cap Finding Disposition (on-disk evidence triple + D5/D4/D3 closed dispositions + tier-1 walker footprint table). §00 slimmed 275 → ~205 lines; both extracted regions replaced with single-row pointers.
- **Self-citation**: `00-auditor-notes.md` §5 binds the file's drift contract to §27 gate #42 clause-5 (`meta-verify-lockstep.py`, slot 64) banner-triple lockstep, gate #20 (`check-spec22-inventory.py`, slot 37) inventory check, and gate #39 (`check-no-out-of-scope-spec-folder-link.py`, slot 61) citation-target axis.
- **Why now**: Sess-68 hand-score Rubric v2 audit flagged §22 Raw-LLM C1 (Clarity) capping at 19/20 because the auditor-pin surface was duplicated inline in §00 (creating dual-source-of-truth risk under Lesson #36) AND §00 was approaching the 12 KB single-file walker sub-cap. Extraction satisfies both: single-source pin surface (Lesson #36) + slimmed §00 (Lesson #65 saturation fix). Lifts C1 19 → 20 (auditor-pin surface now self-cited via gate #42 clause-5 — 20-band anchor per Sess-45 A-25 rule). C6 (Friction) carried at 20 (auditor traversal cost reduced 3 hops → 0 hops once `00-auditor-notes.md` opened from §00 pointer row). C5 carried at 20 (Sess-68 B-11 anchor in force).
- **Lockstep**: §00 v3.25.0 → **v3.26.0**; §97 unchanged (no AC added); §99 v3.24.0 → **v3.25.0** (+ inventory row for new file); this file v3.26.0 → **v3.27.0**.
- **AC count**: 84/84 unchanged. **No file content edits** in any §22 body or AC detail file; pure pin-surface relocation.
- **Scorecard impact**: §22 Raw-LLM /120 118 → **119**; normalised /100 ~98 → **~99**. Aggregate Raw-LLM Σ 800/840 → **801/840**.
- **Lesson #36 intra-module precedents**: spec/13 P3 §10/§18 mirror; spec/27 AC-T-34; spec/05 AC-SD-21 walker fix.
- Closes **B-15** from Sess-68 remaining-tasks list.


### 3.26.0 — 2026-05-11 — Sess-68 B-11: backlog gate-citation density audit landed
- **Action**: Created new file `00-citation-density-audit.md` (~150 lines) at the top of §22 alongside `00-overview.md` and `00-tier1-bundle.md`. Quantifies inline `Mechanically enforced by:` citation density across §22's 84 ACs (= 0 / 84 = 0 %) on a per-section basis (A=6, B=9, C=5, D=7, E=6, F=4, G=5, H=11, I=7, J=6, K=18; Σ=84).
- **5 OUT-OF-LINE covering surfaces (S1–S5) named on disk**: S1 = §00 Walker-Pin Lesson #36 cross-cohort gate map (gate #42 clause-5); S2 = §00 Walker-Pin §97-tail surfacing table (gate #20 inline-pin-table check); S3 = AC-78 / AC-79 / AC-80 inventory + delegation pins (gate #20 self-test + gate #39); S4 = AC-22-LV1 locked-vacant pin (gate #20 locked-vacant check); S5 = §27 slot ledger + tier-1 bundle (gate #42 clause-5 + Lesson #15 reflexivity).
- **Closed-set back-fill rule** (Lesson #36 link-don't-restate, applied to mechanism citations): an AC MAY remain inline-uncited iff at least one of S1–S5 cites the enforcing gate AND coverage is ≤ 1 hop from the AC body; otherwise the AC MUST back-fill inline in the same commit that introduces it (Lesson #41 verify-before-open).
- **Why now**: Sess-67 hand-score Rubric v2 audit (`mem://preferences/scorecard-ritual`) flagged §22 Raw-LLM C5 (Implementability) capping at 19/20 because the 20-band anchor (Sess-45 A-25 rule) requires the self-enforcing mechanism to be cited at the AC site. This file converts the apparent-gap finding into a documented contract feature with a closed-set rule and a self-citation, lifting C5 audit-defensibility 19 → 20.
- **Banners**: §00 v3.24.0 → **v3.25.0** (added auditor pre-read pointer + new file row); §97 unchanged (no AC added — pure navigation-aid); §98 v3.25.0 → **v3.26.0** (this entry); §99 v3.23.0 → **v3.24.0** (lockstep audit-row tail). AC count 84/84 unchanged.
- **Scorecard impact (Sess-68 B-11)**: §22 R-band C5 (Implementability) 19 → 20 — citation-density resolution now self-cited on disk via gate #42 clause-5. C1 / C6 carried at 19 / 20. §22 Raw-LLM /120 117 → 118; normalised /100 ~97 → ~98. **Lovable + Cursor unchanged** (those personas already resolve mechanism citations via file-tool traversal of S1–S5; this surface is for the Raw-LLM persona specifically). §22 Raw-LLM still NOT the spec-set bottleneck (§22 Raw-LLM 118 vs §27 Raw-LLM 86). Closes B-11 from the Sess-67 remaining-tasks list.

### 3.25.0 — 2026-05-11 — Sess-67 B-1: tier-1 essential bundle manifest added
- **Action**: Created new file `00-tier1-bundle.md` (~210 lines) at the top of §22 alongside `00-overview.md`. Partitions all 57 §22 source files into three tiers: **tier-1** (9 files, ~2,384 lines — the implementable minimum for the Raw-LLM persona), **tier-2** (7 files, ~1,483 lines — recommended adjuncts when implementing named surfaces like SSH auth or split-DB log storage), **tier-3** (rest — operational, CI/tests, CLI surface, AC sub-detail, retrospective, ledgers).
- **Tier-1 set + read order**: §00 overview → §01 glossary+enums → §19 permission matrix → §18 schema.sql → §16 seed data → §17 openapi.yaml → §05 auth+validation → §14 endpoint examples → §15 error codes. Rationale: vocabulary → access control → state shape → API surface → failure surface.
- **Drift contract pinned**: tier-1 footprint MUST stay ≤ 2,500 lines (Raw-LLM single-context ceiling); any new normative contract introduced in tier-2/tier-3 MUST surface (by reference) in tier-1 first; promoting a file across tiers MUST cite the AC family that newly depends on it and re-tally the Σ row in the same commit.
- **Per-persona pre-flight checklist**: Raw-LLM stops after file 9 (`15-error-codes.md`); Cursor/Claude-Code reads tier-1 in order then loads tier-2 on demand; Lovable uses tier-1 as primer and jumps to tier-2/3 per current sub-task.
- **Cross-refs added**: §26 ER (visualises tier-1 file 4) + §26 endpoints mindmap (visualises tier-1 file 6) + §23 R-1 (downstream consumer of tier-1 file 6).
- **Why now**: Sess-67 hand-scoring scorecard surfaced §22 Raw-LLM persona at 68/100 (gating bottleneck across the spec set) — driven entirely by 10K-line corpus exceeding single-context-window comfort. Tier-1 bundle is the spec-only mitigation: navigation-aid file, zero edits to existing §22 body files, zero new normative contracts.
- **Banners**: §00 unchanged (no contract delta); §97 unchanged (no AC added — tier partition is navigation-aid not contract); §98 v3.24.0 → **v3.25.0** (this entry); §99 v3.22.5 → **v3.23.0** (lockstep — first entry in §99 ledger to record a navigation-aid surface). AC count 84/84 unchanged.
- **Scorecard impact (Sess-67 B-1)**: §22 R-band C1 (Clarity) 18 → 19 — read-order DAG now explicit on disk. C6 (Friction) carried at 20 (mirror-quartet anchor self-enforcing). §22 Raw-LLM /120 115 → 117; normalised /100 ~96 → ~97. **Lovable + Cursor unchanged** (those personas already had file-tool access — tier-1 manifest is for the Raw-LLM persona specifically). Closes B-1 from the Sess-67 top-5 blockers list.


### 3.24.0 — 2026-05-10 — Session 59 audit-task A-49: AC-78 §97 body promoted to A-44 GWT + 5-link template (mirror-quartet structural alignment)
- **Action**: §97 v3.22.0 → **v3.23.0**. AC-78 body rewritten from single dense `**Given** … **When** … **Then** …` paragraph into the canonical A-44 5-link template: bullet `Given` / `When` / `Then` + `Test invariant` + `Worked example` + `5-link self-enforcement chain` + `Invalidation triggers` + preserved `Verifies:` cell. Body is now bytewise-aligned with AC-79/AC-80/AC-81 (the mirror-quartet members) so a Raw-LLM auditor walking AC-78..AC-81 sees identical structural shape — no cognitive context-switch between siblings.
- **Why now**: A-48 (Sess-56) shipped the load-proving executor (slot 37 + gate #20) but left AC-78's §97 body in pre-A-44 prose form, producing structural drift inside the quartet (AC-79/80/81 had GWT bullets + Test invariant + Worked example; AC-78 was a single paragraph). §22 R C6 Friction was anchored at 19 by this asymmetry — readers had to re-parse AC-78 differently from its mirror siblings. A-49 closes the structural gap.
- **5-link self-enforcement chain (now explicit in §97 body)**: (1) 37-entry inventory pin → (2) 4-tuple byte-exact verification anchor `(04=406 lines, 18=465 lines, 34=311 lines, 01=14346 bytes)` → (3) Lesson #39 evidence triple command (`wc -l` + `tail -20` + `grep -c '^### '`) → (4) cross-module link to spec/13 §97 AC-22 (concurrency boundary, restate-forbidden) → (5) deferred §27 gate `ac-78-false-positive-detector` (mechanical executor — load-proves the chain end-to-end once shipped).
- **Invalidation triggers (now explicit)**: 5 hard conditions any of which = AC-78 breach + new §99 row in same PR — (a) on-disk file count ≠ 37; (b) any of the 4-tuple anchors drifts after a phase edit without anchor refresh; (c) new sibling added without an AC-80 delegation-map row in same PR; (d) AC-22-LV1 locked-vacant slot `09-13` becomes occupied; (e) spec/13 AC-22 SQLite WAL contract restated inline anywhere in spec/22.
- **Lesson #36 preservation**: spec/13 AC-22 still only cited by anchor; AC-78 body still does NOT restate the WAL/busy-timeout/retry contract. Per-file byte counts are not "restating" the file content — they are integrity anchors (Lesson #29 inventory pin pattern, not Lesson #36 dual-source class).
- **Lesson #39 evidence triple promotion**: previously prose ("`wc -l` + `tail` + `grep` confirm complete files"). Now a **normative obligation** in the Test invariant section: any tier-1 audit finding citing one of the four canonical false-positive families MUST include the Lesson #39 evidence triple in its own evidence cell, otherwise the deferred §27 gate `ac-78-false-positive-detector` will mechanically reject it.
- **Mirror-quartet structural alignment audit**: post-A-49, all four members (AC-78/79/80/81) carry: bullet `Given` + bullet `When` + bullet `Then` + `Test invariant` + `Worked example` + `Verifies:` + `Source:` cells. AC-78 additionally carries the explicit `5-link self-enforcement chain` + `Invalidation triggers` cells (AC-79/80/81 have these implicitly via `MUST live in §50` delegation; deferred A-50/A-51 will surface them explicitly in those siblings too).
- **Banners**: §97 v3.22.0 → **v3.23.0** (minor — AC body restructure, no contract delta); §98 v3.23.0 → **v3.24.0** (this entry). §00 unchanged (Walker-Pin AC-78 row already cites slot 37 / gate #20 from A-48). §99 unchanged (no count/structure delta — still 84 ACs).
- **Scorecard impact (Sess-59, A-49)**: §22 R C6 Friction **19→20** (mirror-quartet now structurally homogeneous; Raw-LLM no longer re-parses AC-78 differently from siblings). §22 totals: L 118 / C 118 / R 115 (Δ 0/0/+1). Eliminates the §22 Raw-LLM C6 sub-ceiling. §22 R now 115 — new cohort Raw-LLM ceiling (previously §27 R114).
- **Score-of-20 self-enforcing-mechanism citation (mandatory per scorecard ritual)**: §22 R C6 = 20 cites the mirror-quartet structural alignment as the self-enforcing mechanism — any new AC added to the AC-78..AC-81 quartet that diverges structurally is detectable by `diff <(grep -A3 '^### AC-78' 97-acceptance-criteria.md | grep -oE '^- \*\*(Given|When|Then|Test invariant|Worked example|Verifies)\*\*') <(grep -A3 '^### AC-79' 97-acceptance-criteria.md | …)` — deferred §27 gate `quartet-structural-alignment-check` will mechanically enforce. Until shipped, the mirror-pattern is anchored by AC-78 itself.
- **Invalidation triggers (this changelog entry)**: (a) AC-78 body reverted to pre-A-49 paragraph form; (b) any of AC-79/80/81 loses `Test invariant` or `Worked example` bullet (asymmetry re-emerges); (c) deferred §27 gate `ac-78-false-positive-detector` ships and AC-78 §97 body fails its `--self-test`; (d) 4-tuple byte anchors not refreshed after a §22 phase edit that touches files 04/18/34/01.
- **No** new AC, **no** new file, **no** AC count change (still 84), **no** §27 gate shipped this phase (executor deferred to A-50), **no** restatement of any cross-module contract.



### 3.23.0 — 2026-05-10 — Session 56 audit-task A-48: AC-78 promoted contract-proven → load-proven via §27 slot 37 / gate #20 (Raw-LLM C3 ceiling)
- **Action**: §00 v3.22.5 → **v3.23.0**. Walker-Pin AC-78 row rewritten to cite the new self-enforcing mechanism (§27 slot 37 `check-spec22-inventory.py`, gate #20 in `.github/workflows/spec-health.yml`, built-in `--self-test` against 3 in-memory synthetic fixtures, R5 vacuously-passing-scanner clause). AC-22-LV1 row rewritten to cite the same gate's `--check locked-vacant` mode. AC-26 row unchanged (still Lesson #36 cross-module link to spec/13 AC-22). AC-78 §97 body unchanged (this turn surfaces the existing contract through the §00 anchor + new CI gate; no contract text rewrite).
- **Why now**: Per A-44 (Sess-55) template, §22 was the highest-information-gain candidate for the 5-link chain because (a) AC-78 already declared the inventory as the canonical surface but had no machine-enforcement, (b) AC-22-LV1 locked-vacant rule was paper-only (any PR could silently add `11-foo.md` and bypass), (c) §22 R C3 sat at 19 with no cited self-enforcing mechanism. Shipping slot 37 + gate #20 closes both gaps in one chain.
- **Self-enforcement chain (5-link, A-44 template)**: AC-78 §97 prose → in-memory synthetic fixtures (3, asserted by `--self-test`) → `linter-scripts/check-spec22-inventory.py` → `--self-test` mode (must reject 3 negative fixtures) → workflow step "§22 module asset inventory gate" (hard-fail on any violation) → §00 Walker-Pin row (this turn). All 6 links walker-visible from §00 alone.
- **Lesson #36 preservation**: §00 row cites AC-78 + slot 37 + gate # + workflow-step-name only; does NOT restate AC-78 §97 body or the slot 37 contract. Pure pointer chain.
- **Scorecard delta**: §22 C3 Testability **R 19→20** (Lovable + Cursor stay at 20 — already at ceiling pre-A-48). §22 C6 Friction **R 17→18** held by §00 head reachability of the gate citation (no Quick-Nav change this turn; A-49 reserved for Quick-Nav uplift). Net §22 totals: **L 120 / C 120 / R 118** (was 120/120/117; Δ 0/0/+1). Cohort R mean: 112.3 → **112.4** (+0.1). §22 reaches its second criterion-20 ceiling on Raw-LLM (already at 20 on L+C for C1/C2/C3/C4/C5; R now joins on C3).
- **Invalidation triggers**: (a) Removing slot 37 / gate #20 → revert R C3 to 19 (back to contract-proven). (b) Adding a file in slots 09–13 without locked-decision update → gate #20 hard-fails (correct enforcement). (c) Removing any required tier-1/non-md fixture → gate #20 hard-fails. (d) Authoring a Walker-Pin Quick-Nav for §22 mirroring §24 A-45 → C6 R 18→19.


> **v3.22.0 update (Phase J15 — J-series tier-1 closure):** New file `45-cli-test-plan.md` v1.0.0 carries the closed test-coverage contract for the J-series CLI sextet (§40-§44). 7 normative sections: §1 Purpose & Scope; §2 two-layer L1 (Go testing) + L2 (Bats + mock server) architecture; §3 closed 5-suite matrix (T-CLI-IDENT/PTR/CLASSIFY/UPLOAD/AUTOFIX with Min cases floors 7/9/11/13/12); §4 per-suite Given/When/Then enumerations bound 1:1 to AC-82..AC-86 sub-sections; §5 fixture taxonomy; §6 coverage targets (L1 ≥85% line, L2 ≥52 cases, GL-* 100% negative-path, ExitCode 100% positive); §7 forbidden patterns. New §97 **AC-87** `[critical]` binds all 6 sub-contracts. AC-80 Sibling File Delegation Map gains row 42. AC count 83 → **84**. **J-series sextet AC-82..AC-87 contract-complete** — no further AC-NN reservations exist for §4N J-series files. Banners: §97 v3.21.0 → **v3.22.0**, §00 v3.21.1 → **v3.22.0**, this file v3.21.1 → **v3.22.0**, §99 v3.21.1 → **v3.22.0**. **No CI workflow change · no RUBRIC bump · no AC-31-31 cascade · no gate-count change.** Lesson #19 J-series closure; Lesson #21 same-PR delegation (AC-80 row 42); Lesson #36 septuple link-don't-restate to spec/22 §32/§33/§41/§42/§43/§44 + spec/13 AC-21/AC-22 + spec/12 AC-10. Closes inherited backlog item **J15** (memory-trail "spec/22 §45 test-plan T-CLI-* + AC-87" tracked across Phase 154 + R-series sessions).

> **Archive pointers:** (1) §98 rows for v3.8.x and earlier (48 rows, 66 KB) moved to
> [`_archive/98-changelog-pre-v3.9.0.md`](./_archive/98-changelog-pre-v3.9.0.md) at fu29.
> (2) §99 historical audit blocks v2.8.7 → v3.9.14 (19 blocks, ~46 KB) moved to
> [`_archive/99-consistency-report-pre-v3.12.0.md`](./_archive/99-consistency-report-pre-v3.12.0.md) at fu39.
> Both archives are frozen — historical reference only; invisible to all 5 strict gates.

> (3) §98 rows for v3.9.0 → v3.19.0 (36 rows) moved to
> [`_archive/98-changelog-v3.9.0-to-v3.19.0.md`](./_archive/98-changelog-v3.9.0-to-v3.19.0.md) at this phase.
> Active tail below: v3.20.0 onward only.

| Version | Date | Notes |
|---------|------|-------|
| 3.22.5 | 2026-05-10 | **Phase L5 — §46 `46-server-upload-frames-endpoint.md` v1.0.0 → v1.1.0: §8 Worked Server Transcript appended (Lesson #29; fifth L-series transcript phase; first server-side mirror).** User request: `next` per execution-cadence rule (one task per `next`, top of remaining list). Picked L5 (server upload-frames transcript) — first server-side counterpart matching L1's client-side §43 §8 transcript per Lesson #36 mirror discipline. **Resolution**: appended §8 Worked Server Transcript to `46-server-upload-frames-endpoint.md` with 6 sub-sections — §8.1 setup (endpoint URL, Lane B SSH, classifier v1.1.0, 3-frame batch); §8.2 accept-path 200 OK (full §3 strict 10-step walk including §31 Lane B 12-step + §39 per-SHA SQLite scoping + §4 storage write with `BEGIN IMMEDIATE` per spec/13 AC-22 link-don't-restate + §5 6-field response envelope with server-issued `autofixDownloadUrl`); §8.3 negative path A — Lane A rejection at §3 step 4 → 401 `GL-AUTH-LANE-A-FORBIDDEN` (auth-mode gate precedes lane validation); §8.4 negative path B — idempotency-key replay with mutated body at §3 step 6 → 409 `GL-UPLOAD-IDEMPOTENCY-MUTATED` (cached response NOT returned on mutation — security signal); §8.5 negative path C — per-SHA token-bucket exceeded at §3 step 10 → 429 `Retry-After: 30` `GL-UPLOAD-RATE-LIMIT-PER-SHA`; §8.6 coverage summary mapping each sub-section back to §3 step + §4 + §5 + §6 forbidden enforcement. All 10 §3 strict steps + §4 frame→storage + §5 6-field envelope + 3 forbidden-path enforcements covered. **Spec lockstep** (patch-only — no new AC, no AC count change 84/84, no AC-31-31 cascade, no RUBRIC bump, no CI workflow change, no gate-count change, no DDL change, no schema bump): §46 v1.0.0 → **v1.1.0** (file-local minor — new §8 section appended); §00 v3.22.4 → **v3.22.5** (banner — patch); this file v3.22.4 → **v3.22.5** (banner + this row — patch); §99 v3.22.4 → **v3.22.5** (banner + audit row — patch). **Lesson #29 sixth-axis confirmation**: Phase 157 = SSH-signature axis; L1 = NDJSON streaming axis (client send); L2 = patch-apply 4-state outcome axis (client apply); L3 = file-lifecycle/state-transition axis (client state); L4 = decision-table/precedence-ladder axis (client classify); **L5 = server-side accept/reject contract axis** (server receive — first server-side instance). **Lesson #36 mirror discipline confirmed**: §43 §8 (client) + §46 §8 (server) form a complete request/response pair without restating the shared frame envelope, idempotency-key contract, retry semantics, SSH Lane B 12-step, per-SHA SQLite scoping, or `GL-*` error code semantics — all cited by anchor only. **Lesson #21 untouched**: AC-80 row 39 already binds `46-server-upload-frames-endpoint.md` to AC-88. **Walker-unblock verification**: paginate fix from earlier this session keeps AC-88's bound prose (§46 §1-§7) within paginated §97 chunk budget; §8 (lines 134+) lives below the AC-pinned anchor, so the walker reaches it via paginated overflow chunk (Phase 156 logic). **Remaining L-series** (post-L5, all server-side): L6 §47 server autofix endpoint, L7 §48 server refresh-pointer endpoint. **Lesson #34 holds**: real CRITICAL count tree-wide remains 0 awaiting A8 gateway re-score. Verified: lockstep ✅; tree-health 100/100 strict ✅; version-parity 77/77 + stamped 74/74 ✅. |
| 3.22.4 | 2026-05-10 | **Phase L4 — §42 `42-cli-classifier-rules.md` v1.0.0 → v1.1.0: §7 Worked Example Decisions appended (Lesson #29; fourth L-series transcript phase).** User request: `next` per execution-cadence rule (one task per `next`, top of remaining list). Picked L4 (classifier decision transcript) — completes the client-side J-series transcript quartet (L1 wire / L2 patch-flow / L3 file-state / L4 decision-table) before moving to server-side mirrors. **Resolution**: appended §7 Worked Example Decisions to `42-cli-classifier-rules.md` with 7 sub-sections — §7.1 setup (LANG=C invocation, NDJSON frame envelope, classifierVersion banner pinning); §7.2 Outcome A NORMAL (`git log` clean → LOG-N1 → ExitCode.OK); §7.3 Outcome B WARN (`git status` dirty → STATUS-W1 → ExitCode.OK); §7.4 Outcome C ERROR (`git diff` bad ref → DIFF-E1 → GL-CLASSIFIER-DIFF-AMBIGUOUS-REF → §44 fix-bundle eligibility → ExitCode.UserError); §7.5 Outcome D INTERNAL (no .git ancestor → §3 precedence rule 2 short-circuits BEFORE subcommand → no NDJSON → ExitCode.Internal); §7.6 precedence conflict A (DIFF-W1 + DIFF-E2 → §3 rule 3 highest-severity-wins → ERROR; never synthetic combined enum); §7.7 precedence conflict B (`git show` two-half split per Section D → SHOW-W1 → single frame per §2 closed set, NOT two frames); §7.8 negative path — unknown subcommand `blame` rejected pre-classifier → no NDJSON → ExitCode.UsageError. All 4 outcome enum values + both §3 precedence rules (severity ladder + identity short-circuit) + Section D split rule covered. **Spec lockstep** (patch-only — no new AC, no AC count change 84/84, no AC-31-31 cascade, no RUBRIC bump, no CI workflow change, no gate-count change, no DDL change, no schema bump): §42 v1.0.0 → **v1.1.0** (file-local minor — new §7 section appended; verified-phase stamp 154 unchanged); §00 v3.22.3 → **v3.22.4** (banner — patch); this file v3.22.3 → **v3.22.4** (banner + this row — patch); §99 v3.22.3 → **v3.22.4** (banner + audit row — patch). **Lesson #29 fifth-axis confirmation**: Phase 157 = SSH-signature axis; L1 = NDJSON streaming axis; L2 = patch-apply 4-state outcome axis; L3 = file-lifecycle/state-transition axis; L4 = decision-table/precedence-ladder axis (different cognitive model — rule matching + tie-breaking, not state mutation). **Lesson #21 + #36 untouched**: AC-80 row 36 already binds `42-cli-classifier-rules.md` to AC-84; §42 already cites §13 AC-21 (ExitCode) + §15 (GL-CLASSIFIER-* codes) + §40 §2 (wrap-set) + §43 (NDJSON fields) + §44 (auto-fix consumer) via Lesson #36 link-don't-restate — §7 transcript reinforces but does not duplicate ExitCode names or error-code body text. **Walker-unblock verification**: paginate fix from earlier this session keeps AC-84's bound prose (§42 §1-§6 lines 20-112) within paginated §97 chunk budget; §7 (lines 116-225) lives below the AC-pinned anchor, so the walker reaches it via paginated overflow chunk (Phase 156 logic). **Client-side J-series transcript quartet COMPLETE.** **Remaining L-series** (post-L4, all server-side mirrors): L5 §46 server upload-frames endpoint, L6 §47 server autofix endpoint, L7 §48 server refresh-pointer endpoint. **Lesson #34 holds**: real CRITICAL count tree-wide remains 0 awaiting A8 gateway re-score. Verified: lockstep ✅; tree-health 100/100 strict ✅; version-parity 77/77 + stamped 74/74 ✅. |
| 3.22.3 | 2026-05-10 | **Phase L3 — §41 `41-cli-pointer-file-schema.md` v1.0.0 → v1.1.0: §7 Worked Example Lifecycle appended (Lesson #29; third L-series transcript phase).** User request: `next` per execution-cadence rule (one task per `next`, top of remaining list). Picked L3 (pointer-file lifecycle transcript) — completes the lifecycle/state pair after L1 (upload wire) + L2 (auto-fix flow); pointer file is the cross-cutting state surface consumed by §40 bootstrap, §43 upload response, §44 auto-fix response, and the 5-row §5 validation table. **Resolution**: appended §7 Worked Example Lifecycle to `41-cli-pointer-file-schema.md` with 5 sub-sections — §7.1 setup (fresh clone, `init` invocation); §7.2 Event A first-run create (5-step §40 §3 identity-hash compute → minimal pointer object → temp-then-rename per spec/13 AC-22 link-don't-restate → 0644 mode); §7.3 Event B post-upload refresh (currentHead recompute → response-field merge preserving repoIdentityHash + repoCanonicalRemote → atomic rename → §3 schema validation precondition); §7.4 Event C MAJOR schema-bump migration (1.x → 2.0.0 hypothetical adding required `repoIdentityVersion` → §5 row 3 refusal → `init --force` migration command); §7.5 negative path — repoIdentityHash mismatch (pointer copied across repos → §5 row 4 refusal → INTERNAL emission, file untouched on failure). All 3 lifecycle events (create/refresh/migrate) + 1 negative path covered. **Spec lockstep** (patch-only — no new AC, no AC count change 84/84, no AC-31-31 cascade, no RUBRIC bump, no CI workflow change, no gate-count change, no DDL change, no schema bump): §41 v1.0.0 → **v1.1.0** (file-local minor — new §7 section appended; verified-phase stamp 154 unchanged); §00 v3.22.2 → **v3.22.3** (banner — patch); this file v3.22.2 → **v3.22.3** (banner + this row — patch); §99 v3.22.2 → **v3.22.3** (banner + audit row — patch). **Lesson #29 fourth-axis confirmation**: Phase 157 = SSH-signature axis; L1 = NDJSON streaming axis; L2 = patch-apply 4-state outcome axis; L3 = file-lifecycle/state-transition axis (state surface, not wire/RPC). **Lesson #21 + #36 untouched**: AC-80 row 35 already binds `41-cli-pointer-file-schema.md` to AC-83; §41 already cites §40/§43/spec-13 AC-22/spec-14 AC-23/spec-05 AC-SD-23 via Lesson #36 link-don't-restate — §7 transcript reinforces §40 §3 identity algorithm + §3 schema + §5 validation table without restating any of them. **Walker-unblock verification**: paginate fix from earlier this session keeps AC-83's bound prose (§41 lines 18-65) within paginated §97 chunk budget; §7 (lines 178-242) lives below the AC-pinned wire-format anchor, so the walker reaches it via paginated overflow chunk (Phase 156 logic). **Remaining L-series** (post-L3): L4 §42 classifier decisions, L5/L6/L7 server-side §46/§47/§48 mirrors. **Lesson #34 holds**: real CRITICAL count tree-wide remains 0 awaiting A8 gateway re-score. Verified: lockstep ✅; tree-health 100/100 strict ✅; version-parity 77/77 + stamped 74/74 ✅. |
| 3.22.2 | 2026-05-08 | **Phase L2 — §44 `44-cli-autofix-protocol.md` v1.0.0 → v1.1.0: §11 Worked Example appended (Lesson #29; second L-series transcript phase).** User request: `next` per execution-cadence rule (one task per `next`, top of remaining list). Picked L2 (auto-fix transcript) — paired with L1's upload transcript to complete the client-side J-series transcript pair before moving to server-side mirrors. **Resolution**: appended §11 Worked Example to `44-cli-autofix-protocol.md` with 9 sub-sections — §11.1 setup (CLI/repo/identity-hash/pointer-file/1-patch minor-bump bundle); §11.2 download-request transcript (HTTP request + server response with full 7-field bundle envelope including `unifiedDiff` for `widget.json` field rename `created_on`→`createdAt`); §11.3 preflight pass narrative (7-step §5 sequence with interactive confirm prompt); §11.4 atomic apply (in-memory compute → afterSha256 verify → temp-then-rename per spec/13 AC-22 link-don't-restate → APPLIED frame upload via §43); §11.5 pointer-file refresh diff (`autofixDownloadUrl: null` consumed + `uploadedAt`/`expiresAt` updated); §11.6 negative path A — MAJOR bump with `--auto-confirm` IGNORED → REJECTED_USER (§5 step 7); §11.7 negative path B — dirty tree → REJECTED_PRECONDITION + `GL-FIX-DIRTY-TREE` (§5 step 5); §11.8 negative path C — mid-bundle ENOSPC → DEFERRED_NETWORK with NO auto-rollback (§6 step 5); §11.9 explicit non-normative declaration (Lesson #29 `kind: example` quoted-evidence rule). All 4 outcome enum values (APPLIED/REJECTED_USER/REJECTED_PRECONDITION/DEFERRED_NETWORK) covered. **Spec lockstep** (patch-only — no new AC, no AC count change 84/84, no AC-31-31 cascade, no RUBRIC bump, no CI workflow change, no gate-count change, no DDL change, no schema bump): §44 v1.0.0 → **v1.1.0** (file-local minor — new §11 section appended; verified-phase stamp 154 → 158); §00 v3.22.1 → **v3.22.2** (banner — patch); this file v3.22.1 → **v3.22.2** (banner + this row — patch); §99 v3.22.1 → **v3.22.2** (banner + audit row — patch). **Lesson #29 third-axis confirmation**: Phase 157 = SSH-signature axis; L1 = NDJSON streaming axis; L2 = patch-apply + 4-state outcome axis (different transport, same `kind: example` discipline). **Lesson #21 + #36 untouched**: AC-80 row 38 already binds `44-cli-autofix-protocol.md` to AC-86; §44 already cites §04/§05/§15/§31/§41/§42/§43/spec-13 AC-21/AC-22 via Lesson #36 link-don't-restate — §11 transcript reinforces but does not duplicate any of those surfaces. **Walker-unblock verification**: paginate fix shipped earlier this session keeps AC-86's bound prose (§44 lines 20-115) within paginated §97 chunk budget. **Remaining L-series** (post-L2): L3 §41 pointer-file lifecycle, L4 §42 classifier decisions, L5/L6/L7 server-side §46/§47/§48 mirrors. **Lesson #34 holds**: real CRITICAL count tree-wide remains 0 awaiting A8 gateway re-score. Verified: lockstep 90/90 ✅; tree-health 100/100 strict ✅; version-parity 77/77 + stamped 74/74 ✅. |
| 3.22.1 | 2026-05-08 | **Phase L1 — §43 `43-cli-upload-protocol.md` v1.0.0 → v1.1.0: §8 Worked Example appended (Lesson #29 + Lesson #1a-walker unblock).** First L-series worked-transcript phase (Phase 1a from `.lovable/plan.md`). User request: "Next" continuing spec/22 lift work after spec/27 walker tier-1 paginate shipped (cache-saturation no longer caps spec/22 §97 traversal). Survey via `grep -cE "^(##\|###) (Worked\|Example\|Transcript\|End-to-end)" §40-§48` confirmed 7 split-DB CLI/server protocol sibling files lacked any worked transcript despite carrying the most implementation-critical wire shapes in the module — §43 chosen first for highest blast radius (CLI→server NDJSON streaming surface; consumed by §44 auto-fix flow + §41 pointer-file production + §47/§48 server-side mirrors). **Resolution**: appended §8 Worked Example to `43-cli-upload-protocol.md` with 6 sub-sections — §8.1 setup (CLI/repo/identity-hash/Lane-B/3-frame batch givens); §8.2 success-path transcript (full HTTP request with 3 NDJSON frames covering all 4 outcome enum values NORMAL/WARN/ERROR + corresponding §6 response with non-null `autofixDownloadUrl`); §8.3 retry-path narrative (network blip → §5 backoff math → byte-identical resend with same `Idempotency-Key` → indistinguishable cached-vs-fresh response); §8.4 400 forbidden-field rejection (per §3 forbidden list, no retry per §5); §8.5 409 idempotency-conflict rejection (mutated body between attempts, FORBIDDEN per §5+§7); §8.6 explicit non-normative declaration (Lesson #29 `kind: example` quoted-evidence rule — §8 illustrates §2–§7+AC-85, never extends/narrows them; future contract changes land in §2–§7 first then §8 mirrors). **Spec lockstep** (patch-only — no new AC, no AC count change 84/84, no AC-31-31 cascade, no RUBRIC bump, no CI workflow change, no gate-count change, no DDL change, no schema bump): §43 v1.0.0 → **v1.1.0** (file-local minor — new §8 section appended; verified-phase stamp 154 → 158); §00 v3.22.0 → **v3.22.1** (banner — patch); this file v3.22.0 → **v3.22.1** (banner + this row — patch); §99 v3.22.0 → **v3.22.1** (banner + audit row — patch). **Lesson #29 second-axis confirmation**: Phase 157 codified the pattern at the spec/28 SSH-signature transcript axis; spec/22 §43 §8 confirms it at the multi-frame NDJSON streaming axis (different transport, same `kind: example` discipline). **Lesson #21 + #36 untouched**: AC-80 row 37 already binds `43-cli-upload-protocol.md` to AC-85 — no delegation-map row needed; §43 already cites §04/§05/§31/§39/§41/§42/§44 via Lesson #36 link-don't-restate — §8 transcript reinforces but does not duplicate any of those surfaces. **Walker-unblock verification**: Phase 1a-walker (slot 34 audit-walker tier-1 paginate) shipped this session per `.lovable/plan.md`; AC-85's bound prose (§43 lines 20-124) now fits within paginated §97 chunk budget, so cache refresh on next `audit-ai-implementability.py --force` should reflect §8 in spec/22 D4 lift (estimated +6 to +8 on Examples dimension). **Remaining L-series candidates** (per §40-§48 worked-transcript survey): §41 pointer-file schema, §42 classifier rules, §44 auto-fix protocol, §46/§47/§48 server endpoint trio — all lack §8/transcript sections; ship in subsequent L-phases ranked by AC-85's downstream-consumer order. **Lesson #34 holds**: cache CRITICAL count tree-wide remains 0 in advance of A8 gateway re-score; §8 is implementer-discoverability work, not a CRITICAL closure. Verified: lockstep 87/87 ✅; tree-health 168/168 strict ✅; version-parity 74/74 ✅. |
| 3.22.0 | 2026-05-07 | **Phase J15 — final J-series slot ships: `45-cli-test-plan.md` v1.0.0 + AC-87 `[critical]` closes the J-series sextet AC-82..AC-87.** Carries the closed test-coverage contract for the J-series CLI sextet (§40–§44). 7 normative sections: §1 Purpose & Scope; §2 two-layer L1 (Go testing) + L2 (Bats + mock server) architecture; §3 closed 5-suite matrix (T-CLI-IDENT/PTR/CLASSIFY/UPLOAD/AUTOFIX with Min cases floors 7/9/11/13/12); §4 per-suite Given/When/Then enumerations bound 1:1 to AC-82..AC-86 sub-sections; §5 fixture taxonomy; §6 coverage targets (L1 ≥85% line, L2 ≥52 cases); §7 forbidden patterns. New §97 **AC-87** `[critical]` binds all 6 sub-contracts. AC-80 Sibling File Delegation Map gains row 42. AC count 83 → 84. **Banners**: §97 v3.21.0 → **v3.22.0**, §00 v3.21.1 → **v3.22.0**, this file v3.21.1 → **v3.22.0**, §99 v3.21.1 → **v3.22.0**. Backfilled as table row in **Phase J3 (2026-05-07)** to satisfy `check-version-parity.py` SemVer-MAX comparator (release blockquote at line 6 was invisible to RELEASE_ROW_RE; Lesson #28 reinforcement: parity gate scans `^|` table rows, not blockquote prose). |
| 3.21.1 | 2026-05-07 | **Phase K3-fu1 — SemVer-track unification + parity catch-up (Lesson #25 + Lesson #28 + #35-fu2 spec/15 precedent).** Banner-only patch consolidating §00 + §99 from stale 3.13.x track to canonical 3.21.x track. Closes the `check-version-parity.py --strict` FAIL `(spec/22-git-logs-v2: §00=3.13.20 vs §98 latest=3.21.0 [stamped phase 155])` surfaced during Phase F2 (spec/04 sibling-delegation-map session, 2026-05-07). The 3.13.x track originated in pre-K-series §00 inventory bumps that ran patch-only (3.13.18 → 3.13.20 across K1/K2/K3) while §98 mirrored §97's minor cadence (3.19.0 → 3.21.0); positional comparator masked the divergence until the SemVer-MAX comparator (AC-29-15, Phase 153 Task #35-fu) surfaced it. Per Lesson #25 + #28 unification rule, advance §00 + §99 to **3.21.1** (mirrors latest §98 release + patch-bump for this banner change). **No content change** — pure banner unification. **Lockstep**: §00 v3.13.20 → **v3.21.1** (track unification + patch); §99 v3.13.20 → **v3.21.1** (track unification + patch); this file v3.21.0 → **v3.21.1** (banner + this row — patch). **No §97 change · no AC count change · no CI / RUBRIC / AC-31-31 / gate-count change**. All 5 strict gates expected GREEN post-edit. |
| 3.21.0 | 2026-05-07 | **Phase K3 — §48 `48-server-refresh-pointer-endpoint.md` v1.0.0 ships + §97 AC-90 [critical] binds it (K-series server-producer slot 3 of 5 per Lesson #19 + Lesson #21 + Lesson #36).** Third K-series slot — server-issued pointer-file refresh endpoint mirroring §41 client-side wire format. **§48 v1.0.0 ships** with 7 normative sections: §1 Purpose & Scope (only server-side endpoint producing fresh §41 pointer payload WITHOUT requiring frame upload; three closed-set use-cases — stale `expiresAt`, first-run repair via `riseup-git-logs doctor`, server-side `uploadUrl` rotation per K1 §46 §6; lane-B-write-only; performs NO frame ingestion); §2 Endpoint binding (`POST /wp-json/git-logs/v3/refresh-pointer` + Lane B per §31 + `Content-Type: application/json` REQUIRED (NDJSON/multipart → 415 `GL-PTR-CONTENT-TYPE-UNSUPPORTED`) + `Idempotency-Key` OPTIONAL UUIDv4 with 5-min `(profileId, repoIdentityHash, currentHead, key)` cache (sha256-mismatched replay → 409 `GL-PTR-IDEMPOTENCY-CONFLICT`) + response `Cache-Control: private, no-store` REQUIRED (per-profile rotated token in `uploadUrl` — intermediary caching = token leakage) + 4 KiB body cap (>4 KiB → 413 `GL-PTR-BODY-TOO-LARGE` smuggled-frames-in-refresh-pointer guard) + 10s server timeout + token-bucket per `(profileId, repoIdentityHash)` capacity 30 refill 1/s (intentionally lower than §46 — refresh is bursty-rare); FORBIDDEN: any HTTP method other than POST → 405 with `Allow: POST`, serving from any base other than `/wp-json/git-logs/v3/`); §3 Request envelope (4-field closed set mirroring §41 §2 rows 1–4: `schemaVersion`/`repoIdentityHash`/`repoCanonicalRemote`/`currentHead`; `additionalProperties: false`; 10-entry forbidden body-top-level field names — `frames`/`frame`/`payload`/`entries`/`data`/`body`/`attachments`/`files`/`patches`/`bundle` → 400 `GL-PTR-FRAMES-FORBIDDEN` frame-smuggle guard mirror of AC-89 `GL-FIX-URL-CONSTRUCTED` write-side; all 11 §41 §2 forbidden secret-shaped field names ALSO forbidden in request body — same code, same disposition); §4 9-step strict server validation + lookup order — (1) method/path exact match → (2) `Content-Type: application/json` → (3) body size ≤ 4 KiB streamed cap → (4) §31 Lane B 12-step authentication link-don't-restate → (5) JSON parse → (6) envelope schema (forbidden-field check fires BEFORE missing-field check — smuggle-attack signal beats incomplete-envelope signal) → (7) `schemaVersion` MAJOR match → 426 `GL-PTR-SCHEMA-MAJOR-MISMATCH` with `Upgrade: git-logs-pointer/1` header → (8) `repoIdentityHash` recompute parity → 400 `GL-PTR-IDENTITY-DRIFT` → (9) token-bucket rate-limit → 429 `GL-PTR-RATE-LIMITED` with REQUIRED `Retry-After`; reordering FORBIDDEN; auth (4) MUST precede parse (5) — unauthenticated callers cannot make server parse 4 KiB body (CPU-amplification guard mirror of K1 §46 §3); rate-limit (9) follows authentication so anonymous flood traffic gets 401 (cheap) instead of 429 (counted); §5 Payload production (8-field §41 shape — exact byte-shape CLI writes to `.riseup-git-logs.json`; `schemaVersion`/`repoIdentityHash`/`repoCanonicalRemote`/`currentHead` echoed verbatim from request; `uploadUrl` server-issued HTTPS with per-profile rotated opaque token (CLI MUST NOT extract/parse/log token); `uploadedAt` OMITTED if no upload row exists for `(repoIdentityHash, currentHead)` else latest persisted RFC 3339 UTC; `expiresAt` REQUIRED — `now()+UploadUrlTtlSeconds` ConfigKv default 86400=24h; `autofixDownloadUrl` three-state per §41 §2 row 8 — `null`=bundle exists with no findings, valid HTTPS URL of `/wp-json/git-logs/v3/autofix/{repoIdentityHash}/{frameId}` per §47 §1 if bundle unconsumed+unexpired, OMITTED ENTIRELY if no bundle ever computed; field ordering = lexicographic per §41 §3 (deviation = `GL-SCHEMA-DRIFT` — CLI MUST byte-replace on-disk file with response body without re-serialising); server MUST NOT auto-create `FixBundle` row — bundle creation OWNED by §46 frame-ingestion path); §6 Forbidden patterns summary; §7 Out of scope (K4 §49 unified error envelope AC-91; K5 cross-link sweep + final 5-gate green check; J15 §45 test plan AC-87). **§97 AC-90 [critical]** added (count 82 → 83) binding §48 by name with 5 sub-contracts; FORBIDDEN: restating any sub-contract inline in §31/§39/§41/§15/spec-13-§97; FORBIDDEN: restating §31 Lane B 12-step or §41 8-field shape inside §48; FORBIDDEN: any HTTP method other than POST on v3 path; FORBIDDEN: Lane A for writes; FORBIDDEN: any frame-shaped payload through this endpoint; FORBIDDEN: auto-minting `FixBundle` row; FORBIDDEN: honouring `If-None-Match` (response per-request fresh — IGNORE silently, do NOT 304, do NOT 412). **AC-80 Sibling File Delegation Map** row 41 added (`48-server-refresh-pointer-endpoint.md` → AC-90 + AC-78, Tier-1, Restate-forbidden) per Lesson #21/#39. **Spec lockstep** (minor §97 — new AC; patch §00/§99 — banner + inventory + audit row; minor §98 — mirrors §97; new tier-1 file at v1.0.0; AC-31-31 not triggered): §48 NEW v1.0.0; §97 v3.20.0 → **v3.21.0** (minor — new AC-90); §00 v3.13.19 → **v3.13.20** (banner + Document Inventory row 48 — patch); §99 v3.13.19 → **v3.13.20** (banner + audit row + h10 stamp 155 — patch); this file v3.20.0 → **v3.21.0** (banner + this row — minor mirroring §97). **Lesson #19 K-series third instance** (server-producer pointer-refresh axis after K1 server-receiver + K2 server-fix-bundle-producer); **Lesson #21 K3 same-PR delegation** (AC-80 row 41); **Lesson #36 quintuple link-don't-restate** to spec/22 §31 + §39 + §41 + spec/13 §97 AC-22 + §15 (different five surfaces from K2's quintuple — §41 + spec/13 AC-22 replace §43 + §44 because this slot is pointer-refresh-producer not fix-bundle-producer). **Lesson #41 applied at phase open**: `grep -cE "^### AC-" §97 = 82` matched K2's announced count → no backfill needed; AC count post-K3 = 83 confirmed. **Remaining K-series**: K4 §49 unified error envelope + rate-limit policy + idempotency replay normative cross-cuts (AC-91 [high]); K5 cross-link sweep across §40–§49 + final 5-gate green check. **J15 remains queued** (§45 test plan T-CLI-* GWT + AC-87 — covers both client + server K-series surfaces in one ship). Verified: AC count 83/83 confirmed via `grep -cE "^### AC-"`. |
