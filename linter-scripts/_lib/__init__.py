# Marker file: `linter-scripts/_lib/` is a Python package root reserved
# for shared cross-gate code per the §27 §00 "Shared harness library
# contract" (A-31, Sess-51). Per-gate logic stays in top-level
# `linter-scripts/check-*.py`; only cross-gate primitives live here.
