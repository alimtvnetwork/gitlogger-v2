Create `readme.txt` at the project root containing a single line:

```
let's start now 10-May-2026 {current Malaysia time in 12-hour format}
```

The time will be computed at implementation using Asia/Kuala_Lumpur timezone (e.g., `3:42 PM`).